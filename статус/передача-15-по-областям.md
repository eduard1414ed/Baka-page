# Передача после ревизии: что делать дальше, по областям

Составлено 15 августа 2026, в конце задачи 15. Это **рабочее задание
для будущих сессий**, а не пересказ реестра: реестр отвечает на вопрос
«что нашли», этот файл — на вопрос «что теперь делать и как понять,
что сделано».

Находки с номерами — из [реестра ревизии](задача-15-ревизия-кода.md).
Номера не менять: на них ссылаются оба документа.

---

> ## 🚨 ПРОЧТИТЕ ЭТО ПЕРВЫМ, ИНАЧЕ ПОТЕРЯЕТЕ ЧАС
>
> **ВЫКЛАДКА СЕЙЧАС ТОЛЬКО РУЧНАЯ. Пуш на сайт ничего не выкладывает.**
> На аккаунте GitHub пометка «спам» ([задача-20](задача-20-флаг-github.md)),
> и вместе с ней выключены роботы и автосборка Cloudflare. После каждой
> правки, которая касается сайта:
>
> ```
> git pull                                  # исход посмотреть отдельно
> npm run build
> npx wrangler deploy                       # основной bakapodcast.com
> set -a && . ./.env && set +a
> node scripts/mirror-sync.mjs              # зеркало ru.bakapodcast.com
> ```
>
> **Проверять обе копии по СОДЕРЖИМОМУ, а не по имени файла.** 15 августа
> три починки подряд пролежали невыложенными, заказчик дважды смотрел
> на старый сайт и дважды сказал «не работает» — а работало.
>
> **ЕСТЬ ВТОРАЯ ДОРЕВИЗИЯ.** Она в отдельном файле —
> [задача-15-доревизия](задача-15-доревизия.md), находки 19–38, и часть
> из них уже починена. Раскладка оставшейся работы (сессии I и J) —
> в конце того файла, а не здесь.

---

---

## Как этим пользоваться

**Одна сессия — одна область.** Не потому, что иначе не успеть, а потому,
что у области один способ проверки: сессия про молчание роботов проверяется
падением робота, сессия про уборку — сборкой и поиском по коду. Смешаешь —
и проверять придётся всё сразу, то есть никак.

**Порядок между сессиями почти свободен.** Жёстких зависимостей две:
сессия B опирается на заслоны, поставленные в задаче 15 (они уже стоят),
а сессия E (доревизия) полезнее всего последней — когда починенное осело.

**Перед каждой сессией — прочитать три вещи:** `CLAUDE.md` целиком,
`статус/хвосты-и-болячки.md` целиком и раздел этого файла про свою область.
Реестр — по ссылкам из раздела, целиком читать не обязательно.

**Правило ревизии больше не действует.** Задача 15 не чинила нарочно, чтобы
дойти до конца. Эти сессии — чинят.

---

## Что уже сделано (не переделывать)

15 августа, после реестра, по прямому распоряжению заказчика починено пять
находок. Подробности — в разделе «Что починено» реестра, здесь только чтобы
следующая сессия не начала это заново.

| Что | Где живёт теперь |
|---|---|
| Галочка «не искать в расшифровках» переживает обновление тайтла | `scripts/anime-lib.mjs` |
| Поход к RSS с тремя повторами | `src/lib/podcastFeed.mjs` |
| Сборка не выкладывается без плееров | `scripts/check-players.mjs`, шаг в `npm run build`, аварийный ключ `ALLOW_NO_PLAYERS=1` |
| Черновик выпуска не пишется, пока шапка не прочиталась | `scripts/sync-episodes.mjs` + `scripts/sync-episodes.test.mjs` |
| Все восемь роботов пишут письмо, когда падают | шаги «Сказать, если робот упал» в `.github/workflows/*.yml` |
| `set -o pipefail` у двух роботов, честный заголовок письма у падежей | `anime-candidates.yml`, `anime-cases.yml` |
| Документы говорят правду про видео | `СТАТУС.md`, решение 60 в хвостах |

**15 августа, вторая половина дня — сделано ещё, второй доревизией**
(подробности в [её реестре](задача-15-доревизия.md)):

| Что | Где живёт теперь |
|---|---|
| Расшифровка оживает на первой открытой странице | `PlayerBar.astro` + `scripts/transcript-player-order.test.mjs` |
| Пайплайн не платит второй раз: три источника вместо журнала | `scripts/transcribe/pipeline.py` + `test_paid_ledger.py` |
| Сверка с DTF снова умеет краснеть и молчать | `scripts/dtf/verify.mjs` |
| Самопроверки переживают собственный успех | `archive-hashtags.mjs`, `archive-subheads.mjs` |
| Сборка называет битую разметку ссылки | `post-links-integration.mjs` + `scripts/link-markup.test.mjs` |
| Три битые ссылки на опубликованных страницах | посты `ep-67`, `ep-97`, `ep-106` |
| Кнопка-строка описана один раз | `SupportStrip.astro` берёт `.btn-large` |
| Вид марки тайтла под сверкой | `scripts/stamp-look.test.mjs` |
| Поля «Ссылка на ролик» и «Своё видео» сняты, «Расшифровка» заведена | схема, админка, `CLAUDE.md` |

**15 августа, сессия I — сведены шесть копий правил в скриптах**
(находки 25, 26, 27, 31, 32, 36) и починена находка 39. Подробности
в [реестре доревизии](задача-15-доревизия.md), там же список домов и проверок:

| Что | Где живёт теперь |
|---|---|
| «меня запустили напрямую или подключили» | у всех шестнадцати скриптов + `archive-entry-guard.test.mjs` |
| Сорт пары дубликатов — один на отчёт и на удаление | `сортПары` в `archive-duplicates.mjs` |
| Повтор при сбое сети — копий было ТРИ, не две | `scripts/retry.mjs` + `retry-home.test.mjs` |
| «Шапка читается обратно» — у всех четверых, кто её собирает | `scripts/frontmatter-guard.mjs` |
| Кавычка в атрибуте директивы | `src/lib/directiveAttr.mjs` |
| Жирный внутри заголовка — снимается ВЕЗДЕ | `scripts/heading-bold.mjs` + шаг в `npm run build` |

**Решение 60 («видео на зеркале нет») перерешивать нечем.** Замер 15 августа:
своих видеофайлов в сборке нет ни одного, все пять роликов вставлены блоком
`::video` и лежат в Yandex Object Storage — то есть открываются на обеих
копиях. Решение устарело целиком, в сессии J переезжает в архив хвостов.

---

## Сессия A. Пределы: что зависает и что молчит — СДЕЛАНА 15 августа 2026

> **СДЕЛАНО ВСЁ ЧЕТЫРЕ ПУНКТА, кроме одного, который оказался вашим решением,
> а не работой.** Подробности — в реестрах; здесь коротко:
>
> 1. **Предел по времени** у всех восьми роботов. **Журнал прогонов взять было
>    нечем:** пометка «спам» унесла историю, `gh run list` отвечает «прогонов 0»
>    при восьми живых воркфлоу. Поэтому основание у каждого числа своё
>    и записано прямо в файле робота.
> 2. **Срок запроса** — 30 секунд, одно число на проект, рядом с повтором
>    в `scripts/retry.mjs`. Стоял у двух мест из четырнадцати, и числа были разные.
> 3. **Замер краёв** — ключ `--образцы`, 1,6 секунды против двадцати минут.
> 4. **Мелочи находки 15** — две из трёх. Третья (`publish-queue.json`
>    и `admin-posts.json` лежат на основном сайте открыто) не тронута: это
>    не опечатка, а ваше решение — прятать их или нет.
>
> **ПОПУТНО НАЙДЕНА ЖИВАЯ ПОЛОМКА** — находка 40: скрипт падежных форм падал,
> и робот, который его гоняет, не отработал бы. Нашлась замером времени,
> а не чтением: 0,14 секунды на 655 карточек — слишком быстро для морфологии.
>
> Ниже — прежний текст задания.


**Находки:** 5 (нет `timeout-minutes` и таймаутов), 15 (мелочи, которые
ломаются молча), 17 (замер краёв не прогоняется целиком).

**Зачем.** Роботы теперь умеют сказать, что упали. Но зависший робот
не падает — он висит, и очередь за ним стоит сутки. А замер краёв, который
идёт двадцать минут, не гоняет никто.

**Что делать.**

1. **`timeout-minutes` каждому воркфлоу.** Числа брать не с потолка,
   а по журналу прошлых прогонов: смотреть на вкладке Actions, сколько
   робот идёт обычно, и брать с запасом втрое. У расшифровки запас нужен
   больше — там пятнадцать минут работы.
2. **Таймаут каждому внешнему запросу.** `AbortSignal.timeout(...)` уже
   стоит у двух разовых скриптов — взять оттуда и поставить всем походам
   к Telegram, Shikimori, Mave, Yandex. Число одно на проект, и жить ему
   рядом с `сПовторами` в `scripts/retry.mjs`: там уже живёт правило «что
   считать сбоем сети», и разъезжаться этим двум нельзя.
3. **Замеру краёв — страницы-образцы.** Ключ вида `--образцы`: по одной
   странице каждого вида (главная, архив, раздел, каталог, поиск, 404,
   «Поддержать», «О проекте», статья, выпуск, бонус, тайтл). Выборка,
   сделанная руками в задаче 15, заняла двадцать секунд против двадцати
   минут полного обхода.
4. **Мелочи из находки 15:** испорченный `search-external.json` должен
   ронять сборку, а не молча уносить 70 внешних статей из поиска.

**Как проверить, что сделано.**

- Уронить робота нарочно (испортить строку в скрипте в отдельной ветке)
  и убедиться, что письмо пришло и что прогон кончился по своему пределу,
  а не висел.
- Замер краёв с новым ключом обязан пройти меньше чем за минуту и назвать
  те же оси, что записаны в реестре: у материалов 220 и 370, общая 60.

**Сколько.** Одна сессия. Больше половины — проверка, а не правка.

---

## Сессия B. Проверки, которые могут ослепнуть — СДЕЛАНА 15 августа 2026

> **СДЕЛАНЫ ВСЕ ТРИ ПУНКТА.** Счётчик охвата в каждой из трёх проверок;
> непрочитанный файл стилей — громкая ошибка; ноль в любом счётчике роняет
> проверку. Спрошены двумя подлогами в песочнице
> (`scripts/checks-blind.test.mjs`): уводом файлов стилей и живым нарушением,
> дописанным в собранный CSS. **Первый подлог на сетке не ловился — и виновата
> была не проверка, а подлог:** пункт проверки про `auto-fit`, а не про сетку
> вообще. Ниже — прежний текст задания.


**Находка:** 6 (три проверки собранного сайта молчат, когда слепнут).

**Зачем.** За проект шесть раз случалось, что проверка отвечала «всё
хорошо», будучи сломанной, и каждый раз именно в эту сторону. Сейчас
`check-css` и `check-hidden` молча пропускают файл стилей, который
не прочитался, и ни одна из трёх не говорит, сколько она разобрала.

**Что делать.**

1. **Счётчик охвата в каждую проверку.** Не «пусто — нарушений нет»,
   а «разобрано 2 файла стилей, 5 блоков внутри страниц, 1483 правила;
   нарушений нет». Число, которое вдруг стало нулём, видно сразу.
2. **Непрочитанный файл стилей — громкая ошибка**, а не `catch {}`.
   Пропуск законен ровно в одном случае: файла нет, потому что его
   и не должно быть (`pagefind`, внешние адреса) — это уже отфильтровано
   отдельно, до чтения.
3. **Спросить обе проверки подлогом** — тем же приёмом, что у `check-assets`
   и `check-broken-links`: дописать в собранный CSS правило, нарушающее
   каждый пункт, убедиться, что поймано, вернуть файл. Правило подлога
   писать так, как пишут в жизни, а не переписывать формулировку проверки.

**Как проверить.** Проверка обязана краснеть на подлоге и молчать
на здоровом. И то и другое — в один прогон, чтобы «пусто» перестало быть
неотличимым от «сломана».

**Сколько.** Одна сессия, скорее короткая.

---

## Сессия C. Доступы (РЕШЕНО 15 августа: пока ничего не менять)

> **ЗАКАЗЧИК ОТВЕТИЛ 15 августа 2026: пока ничего не менять.** Версию админки
> не закреплять, SFTP-учётку на Hetzner не закрывать, способ входа не трогать.
> **От сессии осталась одна строка кода:** дописать `"workers_dev": true`
> в `cms-auth/wrangler.jsonc` — документы уверяют, что она там есть, а её нет,
> и первый же деплой воркера входа может отключить служебный адрес, то есть
> вход в админку. Ниже — прежний текст со всеми доводами, на случай если
> решение захочется пересмотреть.


**Находки:** 8 (`workers_dev` у воркера входа), 9 (админка тянет движок
с unpkg без версии), 10 (право `repo,user` шире, чем надо), плюс SFTP-учётка
на Hetzner из находки 7.

**Зачем.** Это единственная область, где ошибка стоит не «пропала картинка»,
а «чужой человек пишет в репозиторий».

**Что решать вам, до всякого кода.**

1. **Закрепить ли версию админки.** Сейчас `sveltia-cms.js` тянется с unpkg
   всегда последней. Закрепим — перестанут приезжать поломки и новые
   возможности разом; обновлять придётся руками, и об этом надо будет
   помнить. Не закрепим — живём как есть.
2. **Менять ли способ входа в админку.** Нынешний воркер (чужой, Sveltia)
   просит право на ВСЕ ваши репозитории. Уже — только на `Baka-page`
   (вы подтвердили 15 августа), но само право широкое. Сузить можно только
   переходом на GitHub App, и это отдельная работа с риском остаться
   без входа на время переделки.
3. **Нужна ли SFTP-учётка на Hetzner.** Ею заливали видео до переезда
   в Яндекс; сайт ею не пользуется ни в одном посте. Оставить — жив ещё
   один вход с паролем. Закрыть — если видео опять понадобится, поднимать
   заново.

**Что делать в коде** (мало и без решений): дописать `"workers_dev": true`
в `cms-auth/wrangler.jsonc` — сейчас документы уверяют, что строка там есть,
а её нет.

**Как проверить.** После любой правки воркера — **войти в админку**
и на `/admin/tools/`: вход держится именно на этом служебном адресе,
и проверяется он только руками.

**Сколько.** Полсессии на код, остальное — ваши решения.

---

## Сессия D. Уборка

**Находки:** 11 (задача 19 нигде не описана), 12 и 12а (копии правил),
13 (мёртвое), 14 (`npm audit`), 16 (числа в документах).

**Зачем.** Ни одна из этих находок ничего не ломает сегодня. Все они ломают
завтрашнюю сессию: она поверит документу и пойдёт не туда.

**Что делать.**

1. **Описать задачу 19.** Десять скриптов (`archive-*`), семь коммитов,
   снос 18 дубликатов. Завести `статус/задача-19-...md`, вписать в карту
   кода и поставить ссылку в `СТАТУС.md`. Материал брать из шапок самих
   скриптов — там всё записано, включая замеры, на которых стоят правила.
2. **Свести список полей, правленых руками** (`['titleRu','synopsis',
   'poster']`) — он записан трижды: в роботе, в админке и в схеме. Свести
   в одно место нельзя (админка вне сборки), значит поставить пометки
   с обеих сторон и **проверку, которая сверяет три списка** — пометка
   сама по себе не удерживает ничего, это записанный урок.
3. **Починить палитру служебных страниц** (`--muted` в
   `public/admin/tools/tools.css` не совпадает с сайтом). Заодно —
   та же проверка сверкой, что и у списка полей: обе копии разбираются
   и сравниваются по значению.
4. **Снести мёртвое:** `parseTimecode`, `public/baka-cover.jpg`,
   `@astrojs/markdown-remark` из зависимостей. Перед сносом — убедиться
   поиском, что зовущих правда нет.
5. **Обновить Astro** 7.1.6 → 7.2.2 (закрывает `nanoid`), пересобрать,
   прогнать все проверки. `js-yaml` 4 → 5 не трогать: смена главной версии.
6. **Починить числа в документах:** «680 кандидатов» в `СТАТУС.md` (правило
   проекта запрещает текучие числа в сводке — заменить на «сколько осталось,
   видно на экране разбора»); текст хвоста 79 переехал в архив хвостов.

**Как проверить.** Сборка, три проверки, поиск по коду на снесённое.
И перечитать `СТАТУС.md` целиком: он живёт дольше подробностей и врёт
незаметнее всех.

**Сколько.** Одна сессия.

---

## Сессия E. Доревизия: что ревизия не успела посмотреть

Задача 15 прошла области 1–5 и часть 6. Осталось пять кусков; порядок —
по цене ошибки.

1. **Разовые скрипты задач 16–19** (≈1 сессия). Шестнадцать файлов
   `archive-*` и `scripts/dtf/*`. Свою работу они сделали, но именно там
   живут правила, по которым архив выглядит как выглядит, — и часть
   из них нигде не описана. Смотреть: что они делают с постами при
   повторном запуске, есть ли у каждого заслон перед записью, ловятся ли
   их проверки подлогом.
2. **Вёрстка и стили** (≈1 сессия). Искать не «некрасиво» (дизайн принят
   и трогать его нельзя), а **разъехавшиеся копии правил вида**: одно
   число или цвет в двух файлах. Приём, который уже сработал в задаче 15:
   разобрать обе копии и сравнить по значению, а не глазами.
3. **Страницы админки целиком** (≈0,5 сессии). `public/admin/index.html`,
   1800 строк своих полей. Смотреть: что происходит с полем, когда Sveltia
   присылает пустоту, `null` и `false` (в постах уже лежит `bonusLinks: null`
   у 674 штук).
4. **Питон расшифровки** (≈0,5 сессии). `scripts/transcribe/`, 2944 строки.
   Смотреть прежде всего денежное: где платим, что делаем до оплаты, что
   после, что будет при обрыве посередине.
5. **Доступы на сервере изнутри** (≈0,5 сессии, нужен ваш ssh). Что правда
   разрешено жильцу `tgexport`, что раздаёт Caddy, жив ли `videouploader`.
   Снаружи проверено: листинг закрыт, корень отдаёт 404.

---

## Общее для всех сессий

- **Правило «работает → выложено → рассказано → записано»** никто
  не отменял: сначала код и выкладка, документы после.
- **Каждая проверка обязана уметь провалиться.** «Пусто» ничего не значит,
  пока не показано, что она умеет находить.
- **Числа брать замером в ту минуту, когда они понадобились.** Всё, что
  записано числом в документах, устаревает: постов, тайтлов, файлов
  в сборке.
- **В конце сессии:** `npm run build`, затем `check-css`, `check-hidden`,
  `check-anime-links` и `check-players`; замер краёв — по образцам.
  Всё закоммичено, запушено, проверено на GitHub.
