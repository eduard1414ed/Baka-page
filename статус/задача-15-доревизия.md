# Задача 15. Доревизия: реестр находок

Сессия 15 августа 2026, вторая по счёту. Первая
([реестр ревизии](задача-15-ревизия-кода.md)) прошла области 1–5 ТЗ и часть
шестой и честно записала, чего не успела. Эта сессия смотрела **ровно то,
что осталось**, в порядке из
[передачи по областям](передача-15-по-областям.md) — по цене ошибки.

**Номера сквозные с первым реестром.** Там 1–18, тут 19 и дальше: на номера
ссылаются оба документа, и повторять их нельзя.

**Правила те же.** Ревизия не чинит: починено ровно одно место, где выбора
не было (раздел «Что починено»). Принятое решение находкой не считается —
перед тем как назвать что-то находкой, оно искалось в хвостах и болячках,
в исключениях части 11 ТЗ и в карте кода; найденное там в реестр не попало.

---

## Что просмотрено в этой сессии

| Область из передачи | Как смотрелось |
|---|---|
| 1. Разовые скрипты задач 16–19 | 12 скриптов `archive-*`, `scripts/dtf/`, `scripts/osen-2022/` — чтением и **прогоном всех самопроверок** |
| 2. Вёрстка и стили | четырьмя замерами по исходникам: переломы, цвета, повторяющиеся числа, **одинаковые наборы свойств в разных файлах** |
| 3. Страницы админки | схема против `config.yml` **сверкой полей**, обработка пустоты и `null`, копии правил |
| 4. Питон расшифровки | денежный путь: где платим, что до, что после, чем отвечает вопрос «уже оплачено» |
| 5. Доступы на сервере изнутри | **не смотрелось — нужен ваш ssh** |

---

## Реестр, по важности

### 19. Пайплайн расшифровки не помнит, за что уже заплачено, — и на этой машине не помнит почти ничего

**Где.** `scripts/transcribe/pipeline.py`, функции `load_state` / `save_state`
и проверка «уже обработан» (строки 762, 1031, 1259).

**Что.** Единственный ответ на вопрос «за этот выпуск уже платили?» —
файл `scripts/transcribe/state.json`. Он **в `.gitignore`**, то есть второй
копии у него нет нигде. Готовые расшифровки при этом лежат в репозитории —
142 файла `src/content/transcripts/<guid>.json`, и **имя файла и есть тот
самый guid**, по которому пайплайн ищет в журнале. Он их не спрашивает.

**Чем найдено.** Замер: в журнале на вашей машине **одна запись из 142**,
папка `output/` пуста, а путь `transcriptFile` в этой записи ведёт на файл,
которого нет. Работа шла на Hetzner, и полный журнал остался там.

**Чем грозит.** Запусти пайплайн отсюда — он честно скажет «уже сделано: 1»
и пойдёт платить заново за 141 выпуск. Защиты нет: ключ `--yes` для
`nohup` предусмотрен, подтверждение спрашивается, но сумму оно назовёт
как за новую работу, и выглядеть это будет нормально. То же случится
на любой новой машине и на пересозданном сервере.

**Ещё полшага в ту же сторону.** Порядок записи такой: сначала транскрипт
на диск, потом пословные данные, **потом журнал**. Умри прогон между первым
и третьим — файл есть, в журнале его нет, и следующий заход заплатит второй
раз. `save_state` вдобавок пишет поверх себя не атомарно: обрыв ровно в этот
миг оставляет обрезанный журнал, а `load_state` читает его без всякой
защиты и падает.

**Оценка.** Риск, и самый дорогой из найденного: цена измеряется деньгами
и часами. Правило проекта «Результат платной операции записывать на диск
сразу» выполнено, а «чем отвечать на вопрос уже оплачено» — нет.

**ПОЧИНЕНО 15 августа 2026** — см. раздел «Что починено», пункт 19.
**Цена находки измерена:** по старому правилу смета на этой машине запросила
бы **$25.64 за 108 выпусков, которые уже оплачены**; по новому — $0.

---

### 20. Сверка перенесённых с DTF постов сегодня КРАСНАЯ, и это ложная тревога; её самопроверка вообще падает

**Где.** `scripts/dtf/verify.mjs`.

**Что.** Три беды разом.

1. **Обычный прогон выходит с ошибкой.** `node scripts/dtf/verify.mjs` →
   код 1, «Оси но ко» и трагедии: ✗ «пропал tgId — импорт заведёт анонс
   из телеграма заново».
2. **Это неправда.** В самом сборщике `build-oshi-no-ko.mjs` написано:
   «ЗДЕСЬ АНОНСА ПОД АДРЕСОМ НЕТ — впервые за четыре последних переноса».
   У поста номера телеграм-сообщения не было НИКОГДА (проверено историей
   git: `tgId: null` стоит с самого первого коммита переноса). А общая
   проверка правленого поста требует номер у ВСЕХ — её писали по трём
   постам, у которых анонс был, и четвёртый в неё не поместился.
3. **`--selftest` падает совсем.** Подлог целит в строку
   `*Осторожно: дальше спойлеры*`, а такой строки в посте больше нет:
   при сохранении в админке курсив переписался со звёздочек
   на подчёркивания. Замер по всему архиву: курсива звёздочкой **0
   вхождений**, подчёркиванием — **27 в 14 постах**.

**Чем найдено.** Прогоном. Раньше его никто не гонял: `verify.mjs`
не зовут ни `npm run build`, ни один из восьми роботов.

**Чем грозит.** Проверка, которая ругается всегда и на здоровое, — это
проверка, которую выключат. А выключат вместе с ней и те девять постов,
про которые она говорит правду. Плюс отдельно: **правило «подлог целит
в текст, который админка перепишет» касается не только этого файла** —
любое место, где мы ищем `*курсив*`, ослепло молча.

**Оценка.** Сломанная проверка + ложная тревога. Данные целы.
**ПОЧИНЕНО 15 августа 2026** — см. «Что починено», пункт 20.

---

### 21. Две самопроверки разовых скриптов больше не проходят — потому что их правка УЖЕ применена

**Где.** `scripts/archive-hashtags.mjs --selftest`,
`scripts/archive-subheads.mjs --selftest`.

**Что.**
- у хештегов провален 1 подлог из 15;
- у подзаголовков самопроверка **падает** с «в архиве нет ни одного блока
  ссылок — проверять нечем».

Причина у обеих одна и хорошая по замыслу: пример берётся **из живых
данных**, а не вписан именем. Но правку эти скрипты уже сделали — блоков
ссылок в архиве не осталось, хештегов тоже, — и пример брать неоткуда.
У хештегов вдобавок сам отбор примера сломан: строка ищется глобальной
регуляркой через `.test()`, а та запоминает позицию между вызовами,
и в примеры попала строка, где решётка стоит внутри адреса
(`…/anime-autumn-2025/#six`).

**Чем найдено.** Прогоном всех девяти самопроверок задач 16–19: семь
зелёные, две красные.

**Чем грозит.** Ничем сегодня. Но красная проверка, которую нечем сделать
зелёной, живёт до первой уборки и потом исчезает вместе с правилом,
которое стерегла. Это оборотная сторона записанного урока «в заслон нельзя
ставить утверждения, растущие вместе с данными»: тут утверждение
**убывает** вместе с данными.

**Оценка.** Сломанные проверки. Обе падают ГРОМКО — то есть врут
в безопасную сторону.
**ПОЧИНЕНО 15 августа 2026** — см. «Что починено», пункт 21.

---

### 22. Крупная кнопка-строка описана дважды, и вторая копия стоит на шести страницах

**Где.** `src/styles/global.css`, класс `.btn-large` — и
`src/components/SupportStrip.astro`, правило `.support-links a`.

**Что.** Шестнадцать объявлений, совпадающих **байт в байт**: высота 56,
раскладка, зазор 20, поля, рамка, цвет, начертание, капслок, трекинг,
переход. Плюс отдельно скопированы наведение и кегль стрелки.

Карта кода говорит: «Крупная кнопка-строка — `.btn-large`. Мест ТРИ:
кнопки площадок на «Поддержать», кнопки внутри плашки подписки у бонуса
и две кнопки на странице 404». Мест **четыре**, и четвёртое — не класс,
а копия.

Punchline: рядом с `.btn-large > span { font-size: 17px }` в `global.css`
стоит комментарий «трёх одинаковых семнадцаток быть не должно».
Четвёртая семнадцатка лежит в `SupportStrip.astro`.

**Чем найдено.** Замер: разбор всех блоков `{…}` в исходниках стилей
и сравнение по НАБОРУ «свойство: значение», а не по виду правила.

**Чем грозит.** Сегодня — ничем, копии совпадают. Завтра — блок
«Поддержать «Баку!»» стоит на ШЕСТИ страницах (главная, каждая страница
материала кроме бонуса, страница тайтла, каталог, «О проекте»), и любая
правка кнопки обойдёт его стороной. Расхождение будет видно только глазами
и только если открыть две страницы подряд.

**Оценка.** Разъехавшаяся копия, пока не сработавшая. Из области 3 ТЗ —
той, которую проект нарушал чаще прочих.
**ПОЧИНЕНО 15 августа 2026** — см. «Что починено», пункт 22.

---

### 23. Вид марки тайтла описан в ЧЕТЫРЁХ файлах, а карта кода знает про два

**Где.** `src/components/AnimeBadge.astro`, `src/components/AnimeCard.astro`,
`src/pages/anime/[slug].astro`, `src/pages/search.astro`.

**Что.** Замер по файлам:

| Файл | Правил про марку | Объявлений |
|---|---|---|
| `AnimeBadge.astro` | 7 | 34 |
| `AnimeCard.astro` | 10 | 39 |
| `anime/[slug].astro` | 9 | 33 |
| `search.astro` | 9 | 33 |

Перфорация («зубцы» по краям марки) записана **двумя размерами, и каждый
дважды**: мелкий (зубец 4 px, шаг 16×10) — в `AnimeBadge` и `search`,
крупный (зубец 5 px, шаг 18×12) — в `AnimeCard` и на странице тайтла.
Копии совпадают до последней цифры. Стрелка «→» описана трижды одинаково
(`font-size: 20px; line-height: 1; color: var(--accent-text)`), строка
меты — дважды.

Карта кода: «Марок ровно две формы, третьей не бывает… Цвет обе берут
из общего `animeStamp.mjs`». Цвет — да. Всё остальное — четыре дома.

**Чем найдено.** Тот же замер по наборам свойств плюс отдельный подсчёт
правил марки в каждом файле.

**Чем грозит.** Это ровно та беда, про которую в `CLAUDE.md` записан урок
про стрелку ↗ в пяти местах: «мелочь не спасает от правила „одно описание —
одно место“: она его усугубляет». Способ лечения в проекте уже есть
и применён к соседней задаче: плашка «Бонус» вынесена в `global.css`
именно потому, что её рисует и карточка, и выдача поиска.

**Оценка.** Разъехавшаяся копия, пока не сработавшая.
**ВЗЯТА ПОД СВЕРКУ 15 августа 2026, НО НЕ СВЕДЕНА** — см. «Что починено», пункт 23.

---

### 24. Три поля модели данных читаются кодом, не заполнены ни у кого и заполнить их нечем

**Где.** `src/content.config.ts` — поля `youtube`, `video`, `transcript`;
`public/admin/config.yml` — их там нет.

**Что.** Замер по всем 747 постам: `youtube` заполнен у **0**, `video` —
у **0**, `transcript` — у **0**. В админке полей для них нет вовсе.
При этом страница материала их читает по-настоящему:
`src/pages/posts/[slug].astro:119` достаёт `youtube` и `video` из шапки,
рисует по ним окно ролика в шапке материала и включает перемотку
по таймкодам; `postCardMedia.mjs` берёт по `youtube` картинку для карточки.
`CLAUDE.md` описывает `youtube` и `video` как часть модели поста.

**Чем найдено.** Сверка полей админки со схемой сборки — обе стороны
разобраны и сравнены списками.

**Чем грозит.** Само по себе ничем: ролики видеоэссе живут блоком
`::video` в теле, и код перемотки это умеет (`[slug].astro:837` берёт
первый ролик на странице, каким бы способом его ни вставили). Плохо
другое: **в модели данных числятся три возможности, которых у заказчика
нет.** Захотите поставить ролик в шапку материала — в админке для этого
нет поля, и узнать об этом можно только отсюда.

**Оговорка.** Поля `audioGuid` в админке тоже нет, но это **записанное
решение** (хвост 32), и находкой не считается.

**Оценка.** Недоделка.
**РЕШЕНО И СДЕЛАНО 15 августа 2026** — см. «Что починено», раздел 24.
Заказчик решил: «Ссылку на ролик» и «Своё видео» убрать, «Расшифровку»
завести в админке с объяснением, что она на случай, когда записи в RSS нет.

---

### 25. Четыре скрипта, которые пишут во ВСЕ посты, не защищены от случайного подключения

**Где.** `scripts/archive-hashtags.mjs`, `archive-subheads.mjs`,
`archive-headings.mjs` (пишут в посты) и `archive-rules-scan.mjs`
(только читает).

**Что.** У восьми их соседей последней строкой стоит проверка «меня
запустили напрямую или подключили из другого файла?». У этих четырёх её
нет: файл начинает работать от простого `import`. Довод записан прямо
в коде соседа, `archive-anime-links.mjs`: «Без этой проверки простой
`import { readAnime }` из соседнего скрипта прогонял всю разведку
и печатал её отчёт посреди чужого замера — наступил в тот же день».

**Чем найдено.** Разбор концовки всех двенадцати скриптов; отдельно
проверено поиском, что сегодня их никто не подключает.

**Чем грозит.** Сегодня — ничем, подключающих нет. Но подключи их
завтрашняя сессия ради одной функции — и скрипт **прогонит правку
по всему архиву**; а запусти тот скрипт с ключом `--write`, ключ достанется
и подключённому: он смотрит на общую командную строку. Это перезапись
всех постов без единого вопроса.

**Оценка.** Риск. Вероятность низкая, цена — весь архив.
**ПОЧИНЕНО 15 августа 2026, сессия I** — заслон дописан всем четверым, плюс
двум проверкам сборки. Проверка `scripts/archive-entry-guard.test.mjs`
подключает каждый из шестнадцати скриптов из чужого процесса и требует
тишины; подлог — копия скрипта с вырезанной развилкой.

---

### 26. Сорт пары дубликатов считается ДВАЖДЫ, и от него зависит, какой пост удалить

**Где.** `scripts/archive-duplicates.mjs`, функция `diffNote` (отчёт) —
и `scripts/archive-dedupe-apply.mjs`, функция `kindOf` (удаление).

**Что.** Обе решают одно: пара — это «дословная копия», «почти копия»,
«один длиннее» или «разные материалы». Порог общих слов **90 % записан
в обеих**, логика переписана заново. А решение зависит от сорта напрямую:
у дословных копий удаляется НОВЫЙ пост, у почти-копий — СТАРЫЙ.

Это в том же файле, где двадцатью строками выше стоит комментарий про
ровно эту болезнь: «Пока список был написан дважды, они разошлись:
у отчёта было четыре признака, у удаления два, и пара… пропала И из плана,
И из заслона». Список признаков свели в один `PAIR_KEYS`. Сорт пары —
нет.

**Чем найдено.** Чтением обоих файлов подряд.

**Чем грозит.** Разъедься пороги — заказчик принимает решение по одному
списку, а удаляется другой пост из пары. Задача 19 уже снесла этим
скриптом 18 постов.

**Оценка.** Разъехавшаяся копия. **НЕ «пока сходится» — уже разошлась**, и это
выяснилось при сведении: отчёт сравнивал с порогом ОКРУГЛЁННЫЙ процент
(89,6 % → 90 → «почти копия»), а удаление саму долю (0,896 → «разные
материалы»). Пара в этой щели читалась бы в отчёте копией, а не удалялась.
**ПОЧИНЕНО 15 августа 2026, сессия I** — один `сортПары`, проверка
`scripts/dedupe-kind.test.mjs`. Отчёт и план удаления после правки совпали
с прежними байт в байт.

---

### 27. «Повтор при сбое сети» живёт не в одном месте, а в трёх, и две копии уже разъехались

**Где.** `scripts/retry.mjs` (дом), `scripts/dtf/source.mjs`,
`scripts/dtf/fetch-images.mjs`.

**Что.** Карта кода: «**ПОВТОР ПРИ СБОЕ СЕТИ — `сПовторами` в
`scripts/retry.mjs`, одно место на проект**… Пользуются трое». На деле
пользователей четверо, и четвёртый — папка `scripts/dtf/` — держит **две
свои копии** и цикла повторов, и правила «что считать сбоем сети».

Правила уже разные:

```
retry.mjs      fetch failed | timeout | timed out | ECONN | ENOTFOUND |
               EAI_AGAIN | socket | network | terminated | 502 | 503 | 504
dtf/*.mjs      fetch failed | timeout | network | ECONN | socket
```

То есть в `dtf/` **не считаются сетевыми**: отказ DNS (`ENOTFOUND`,
`EAI_AGAIN`), оборванное соединение (`terminated`) и ответы 502/503/504 —
самые частые временные отказы чужого сервера.

**Чем найдено.** Чтение обоих файлов и посимвольное сравнение выражений.

**Чем грозит.** Пересборка поста с DTF падает на моргнувшем сервере там,
где остальной проект переждал бы и пошёл дальше. И карта кода про это
врёт — а её читают первой.

**Оценка.** Разъехавшаяся копия + неправда в карте кода.
**ПОЧИНЕНО 15 августа 2026, сессия I. КОПИЙ ОКАЗАЛОСЬ ТРИ, А НЕ ДВЕ** —
третья жила в `scripts/dtf/collect-studios.mjs` со своей веткой на 429.
Чтобы её не потерять, `429` поднят в общее правило: это не отказ по существу,
а «сейчас не могу, спроси позже», то есть тот же случай, что 502–504.
Паузы у AniList остались длиннее (2/4/6 с) — их называет вызывающий.
Проверка — `scripts/retry-home.test.mjs`: ищет вторые копии по всем
исходникам и гоняет путь DTF на подделке сети.

---

### 28. Защита ручных правок тайтла молча выключается, если один файл не отдался

**Где.** `public/admin/index.html`, `getAnimeFieldHashes()`.

**Что.** Админка сама ставит галочку «правлено руками», сравнивая
сохраняемое значение со слепком из `/anime-fields.json`. Файл берётся
с сайта так:

```js
fetch('/anime-fields.json')
    .then((res) => (res.ok ? res.json() : {}))
    .catch(() => ({}))
```

Не отдался, отдался с ошибкой, испорчен — **вместо ошибки получается
пустой список слепков**, и дальше по коду это неотличимо от «тайтл
появился после последней сборки». Тогда не помечается ничего.

**Чем найдено.** Чтение кода админки.

**Чем грозит.** Вы правите русское название или описание тайтла, галочка
не ставится, и ближайший поход робота за данными из Shikimori **молча
затирает вашу правку**. Ровно то, ради чего галочка заведена. Ни строчки
в лог, ни вида в интерфейсе.

**Оценка.** Риск. Молчание в сторону «всё хорошо» — тот самый класс, что
шесть раз ловили за проект.

---

### 29. Разведка вставки роликов падала на посте без ролика

**Где.** `scripts/archive-youtube-embed.mjs`, показ плана.

**Что.** Строка печатала название ролика безусловно, а у части записей
ролика нет вовсе — и это не край, а решение заказчика 13 августа:
«распознанное эссе получает свою категорию даже без ролика».

**Оценка.** Поломка в одном месте, выбора нет.
**ПОЧИНЕНО В ЭТОЙ СЕССИИ** — см. последний раздел.

---

### 30. Раздел отчёта «битая разметка ссылки» не может напечататься никогда

**Где.** `scripts/archive-source-insets.mjs`, строки 313 и 368–374.

**Что.** Массив `broken` объявлен пустым и **не заполняется ни разу**.
Значит раздел отчёта «БИТАЯ РАЗМЕТКА ССЫЛКИ — N. НЕ ТРОГАЮ, чинить руками»
не печатается никогда, и читается это как «битой разметки нет».

**Чем найдено.** Чтение кода.

**Чем грозит.** Тем же, чем обещанный, но не написанный ключ `--selftest`
у замера краёв (записанный урок): **обещание проверки без проверки — это
ложь в сторону «всё хорошо»**. Спрашиваешь «есть ли битые ссылки?» —
слышишь молчание, а молчать тут нечему.

**Оценка.** Недоделка. Лечится либо тем, что раздел начнёт заполняться,
либо тем, что его уберут.
**ПОЧИНЕНО 15 августа 2026, И ОНО НАШЛО ТРИ НАСТОЯЩИЕ БЕДЫ** —
см. «Что починено», пункт 30.

---

### 31. Заслон «шапка читается обратно» стоит у троих из пятерых, кто правит шапку поста

**Где.** Правят шапку: `telegram-import.mjs` (заслон есть),
`telegram-inner-links.mjs` (есть), `sync-episodes.mjs` (поставлен вчера),
**`archive-season-titles.mjs` (нет)**, **`archive-external-posts.mjs`
(нет)**.

**Что.** Правило проекта: кривая шапка роняет сборку ВСЕГО САЙТА, поэтому
перед записью её читают настоящим `js-yaml` — тем же, каким её будет
читать сборка. Два разовых скрипта правят шапку строкой и пишут файл
без этой проверки. У заголовков это смягчено (значение готовит сам
`js-yaml`), у внешних ссылок адрес подставляется в строку как есть.

**Чем найдено.** Сравнение всех мест, которые пишут в `src/content/posts/`.

**Чем грозит.** Низкой вероятностью и высокой ценой: сайт не собирается,
пока не найдёшь виноватый файл среди сотен.

**Оценка.** Риск. Тот же, что был находкой 3 первого реестра, — правило
выведено, но пройдено не по всем местам.
**ПОЧИНЕНО 15 августа 2026, сессия I, И НЕ ДОПИСЫВАНИЕМ ЧЕТВЁРТОЙ КОПИИ.**
Правило вынесено в `scripts/frontmatter-guard.mjs` и его взяли все четверо,
кто собирает шапку сам. У разовых скриптов порция теперь собирается
в памяти: не прочиталась одна шапка — не пишется НИЧЕГО. Проверка —
`scripts/frontmatter-guard.test.mjs`, подлог возвращает прежнее «своё правило
кавычек», не видевшее двоеточия в конце заголовка.

---

### 32. Кавычку в подписи экранируют трое, не экранируют семеро

**Где.** Экранируют: админка (`public/admin/index.html`),
`scripts/archive-source-insets.mjs`; обратно разбирают четыре плагина
в `src/plugins/`. Не экранируют: семь сборщиков `scripts/dtf/build-*.mjs`
и `scripts/osen-2022/build.mjs`.

**Что.** Прямая кавычка внутри `caption="…"` ломает разбор блока, поэтому
её заменяют на `&quot;`. Сборщики подставляют подпись в строку как есть.

**Чем найдено.** Поиск всех мест, где собирается атрибут директивы,
плюс замер по архиву: блоков-директив **1530**, с экранированной кавычкой
**2**, с непарными кавычками **0**. Отдельно проверены исходники
с DTF: подпись там всего одна, кавычек в ней нет.

**Чем грозит.** Пока ничем — не выстрелило. Выстрелит, когда в подпись
попадёт кавычка: блок на странице покажется словами
`::image{src=… caption=…}`.

**Оценка.** Разъехавшаяся копия, цена низкая.
**ПОЧИНЕНО 15 августа 2026, сессия I.** Дом — `src/lib/directiveAttr.mjs`,
его зовут четыре плагина сборки, разовый скрипт врезок и восемь сборщиков
переносов. **Мест, собиравших подпись мимо правила, оказалось ВОСЕМЬ, а не
семь:** проверка нашла ещё подпись врезки в `dtf/build-podborki-2023.mjs`.
Копия в админке неизбежна (она вне сборки) и потому СВЕРЯЕТСЯ проверкой
`scripts/directive-attr.test.mjs`, а не помечается «поправьте и там».

---

### 33. Старый слой переменных: семь имён из девяти не читает никто

**Где.** `src/styles/tokens.css`.

**Что.** Файл сам объявляет слой временным и даже даёт команду проверки:
«Старые имена удаляем по одному, когда на них не останется ссылок».
Замер по всему `src/` и `public/`:

| Имя | Кто пользуется |
|---|---|
| `--color-bg`, `--color-bg-elevated`, `--color-border`, `--color-text`, `--color-text-muted`, `--color-accent-2`, `--font-heading` | **никто** |
| `--color-accent`, `--color-accent-hover` | одно правило `a {}` в `global.css` |

Рядом с этим правилом висит комментарий «В части 3, когда компоненты
научатся красить свои ссылки сами, здесь останется `color: inherit`».
Часть 3 закончилась задолго до задач 12–22.

**Чем найдено.** Замер: разобраны все имена-псевдонимы и посчитаны
обращения к каждому.

**Чем грозит.** Ничем, кроме правила проекта «поле, которое никто
не читает, живёт дольше правды». По нему уже удалили старую шкалу
отступов 10 августа.

**Оценка.** Мёртвое.

---

### 34. Мёртвое: копия списка, два неиспользуемых имени и один класс

- **`scripts/archive-duplicates.mjs`, строка 158.** Рядом с рабочим
  `const groups = PAIR_KEYS` в ту же строку вписан
  `const unusedGroups = [ …те же четыре признака… ]` — **дословная копия
  списка**, который двумя десятками строк выше объявлен единственным
  и снабжён рассказом о том, как копия уже разъезжалась. Читать это
  следующей сессии опасно: копия выглядит живой.
- **`scripts/dtf/source.mjs`, `ARTICLES`.** Экспортируется, не читается
  никем — и **уже неправда**: перечисляет 6 адресов при десяти собранных
  из DTF постах.
- **`scripts/archive-anime-links.mjs`, строка 442.** `void fold;` —
  импорт, оставленный ради того, чтобы не ругался линтер.
- **`src/styles/global.css`, `.long-text`.** Класс из дизайн-системы,
  не поставленный ни в одной разметке (проверено по всем `.astro`, `.ts`,
  `.mjs`, `.html` в `src/`, `scripts/` и `public/admin/`).

**Оценка.** Мёртвый код.

---

### 35. Неправда в документах

- **`статус/карта-кода.md`:** «Шесть постов сайта собраны оттуда
  скриптами» — постов **десять** (семь сборщиков по посту плюс
  `build-podborki-2023.mjs`, который делает три). И в перечне файлов
  `scripts/dtf/` **нет `guard.mjs`** — того самого заслона, который
  не даёт пересборке стереть вашу правку; в карте описано правило
  про шапку, а про тело нет ни строчки.
- **`scripts/dtf/README.md`:** «Заслон стоит у всех семи сборщиков» —
  сборщиков **восемь** (девять с осенним).
- **`scripts/dtf/captions.mjs`:** «соберите заново скриптом `studios.mjs`» —
  такого файла нет, есть `collect-studios.mjs`.
- **`src/styles/global.css`,** комментарий над правилом ссылок: «карточки
  и ленты ещё не переверстаны… В части 3 здесь останется `color: inherit`».
  Части 3 давно нет.

**Оценка.** Неправда в документах.
**ТРИ ПЕРВЫЕ ПОПРАВЛЕНЫ В ЭТОЙ СЕССИИ** — в карте кода теперь стоит «десять
постов» с объяснением, откуда десять при восьми сборщиках, и добавлен абзац
про `guard.mjs`; в README дописано «восьми… и у девятого, осеннего»;
в `captions.mjs` имя скрипта исправлено на настоящее. **Четвёртая
не тронута:** переписать комментарий про «часть 3» — значит объявить новый
план для цвета ссылок, а это ваше решение, а не уборка.

---

### 36. Одно правило заказчика — два разных ответа: жирный в заголовке внутри цитаты

**Где.** `scripts/archive-headings.mjs` и `scripts/archive-subheads.mjs`.

**Что.** Оба снимают жирный внутри заголовка — это решение заказчика
от 12 августа. Но первый смотрит только заголовки **верхнего уровня**
дерева, а второй обходит дерево целиком. Значит заголовок, стоящий внутри
цитаты, спойлера или пункта списка, первый не трогает (и у него на это
есть подлог с ожиданием «не трогать»), а второй трогает.

**Чем найдено.** Чтение обоих; подлоги у них противоположны по смыслу.

**Чем грозит.** Ничем сегодня. Но «жирного внутри заголовка не бывает» —
это одно правило, а ответов на него два, и какой сработает, решает то,
какой скрипт запустили.

**Оценка.** Разъехавшаяся копия правила.
**ПОЧИНЕНО 15 августа 2026, сессия I. РЕШЕНИЕ ЗАКАЗЧИКА: СНИМАТЬ ВЕЗДЕ** —
и внутри цитаты, и внутри спойлера, и внутри пункта списка. Правило вынесено
в `scripts/heading-bold.mjs`, его зовут оба разовых скрипта и новая проверка
сборки. **Расхождение было ВТОРОЕ, и реестр его не называл:** первый скрипт
снимал ЛЮБОЙ жирный внутри заголовка, второй — только у заголовка, жирного
ЦЕЛИКОМ, то есть «Часть **жирная**, часть нет» пропускал молча.
Замер на живом архиве: заголовков внутри цитат, списков и спойлеров
**ноль из 471**, так что решение — про будущее, а не про сегодняшние данные.
Проверка — `scripts/heading-bold.test.mjs`: сверяет, что все трое отвечают
одинаково, и роняется подлогом «прежнее правило».

---

## Что починено прямо здесь

### 29. Разведка вставки роликов больше не падает

`scripts/archive-youtube-embed.mjs`: строка показа спрашивает, есть ли
у записи ролик, и печатает «ролика нет — меняется только категория»,
если его нет. Одна строка, одно место, выбора не было: прежний код
ронял **весь показ** — тот самый режим, который запускают первым.

**Проверено обеими сторонами.** На живых данных эта ветка недостижима
(черновиков с такой категорией не осталось — «Постов к правке: 0»),
поэтому проверялась **временной подменой**: в план подкладывалась запись
без ролика.

- с прежней строкой — прогон падает: `TypeError: Cannot read properties
  of null (reading 'title')`, код возврата 1;
- с правкой — печатает строчку и выходит с нулём.

Подмена убрана, в файле её не осталось (проверено поиском). Самопроверка
скрипта после правки по-прежнему зелёная: 15 подлогов, провалов нет.

### 19. Пайплайн спрашивает про оплаченное три источника вместо одного

**Что сделано.** Появилась одна функция `paid_ledger` в
`scripts/transcribe/pipeline.py`, и через неё проведены **все пять мест**,
где раньше спрашивали журнал по отдельности: прогон архива, смета, разбивка
на части, список выпусков и обработка одного выпуска. Два ответа на один
вопрос — это то, из-за чего смета говорила бы одно, а прогон делал другое.

Источников теперь три:

| Источник | Что знает | Зачем он |
|---|---|---|
| `state.json` | журнал прогонов | как было |
| папка `output/` | что легло на диск в этот прогон | закрывает щель: транскрипт пишется ПЕРВЫМ действием после оплаты, журнал — двумя действиями позже |
| `src/content/transcripts/*.json` | всё, что доехало до репозитория | единственная копия, переживающая потерю сервера; имя файла И ЕСТЬ guid |

**«Источника нет» и «не оплачено» — разные ответы.** На сервере папки
репозитория нет вовсе (туда копируют только `scripts/transcribe/`), и это
законно — но теперь об этом говорится вслух отдельной строкой, а не молча
превращается в «значит, не платили».

**Журнал пишется целиком или никак.** `save_state` пишет во временный файл
рядом и переставляет имя: переименование внутри папки атомарно. Прежняя
прямая запись сначала обрезала файл, а потом наполняла, — обрыв ровно в этот
миг оставлял огрызок, который не читается, и восстановить его было нечем.

**Цена измерена, а не оценена.** На машине заказчика:

```
К распознаванию всего: 108
СТАРОЕ правило (только журнал): к оплате 108, 73.3 ч → $25.642
НОВОЕ правило (три источника):  к оплате   0,  0.0 ч → $0.0
```

**Проверка — `scripts/transcribe/test_paid_ledger.py`**, десять проверок
на подложенных папках (ответ известен заранее, от работы заказчика
не протухает) плюс замер по живому репозиторию, который ничего не утверждает,
а печатает числа. Ключ `--selftest` **возвращает прежнее правило** и
показывает разницу: старое знало бы 0, новое — 2. Соседняя проверка
`test_script_align.py` после правки по-прежнему зелёная.

**Чего эта починка НЕ делает.** Подсчёт «сколько кредитов уже потрачено»
для сметы квоты по-прежнему берётся только из журнала: длительность аудио
записана там, и в репозитории её нет. На этой машине он покажет заниженное
число — но теперь рядом печатается, сколько знает каждый источник, так что
расхождение видно глазом, а не прячется.

---

### 24. Два поля сняты, третье заведено в админке

**Это уже не «починка без решения», а ваше прямое распоряжение
15 августа 2026**, данное после того, как реестр объяснил, что это за поля.

**Сняты «Ссылка на ролик» и «Своё видео».** Ушли из схемы
(`src/content.config.ts`) и вместе с ними — весь путь, который их читал:
две ветки разметки и стили `.youtube-wrap`, `.youtube-frame`, `.own-video`
в `posts/[slug].astro`; ступень «кадр с ютюба» и её импорт
в `postCardMedia.mjs`; `getYoutubeThumbnailUrl` в `youtube.mjs`; строка
про поля в `remark-lead-cover.mjs`. **Кнопка «Видео» в редакторе не тронута** —
она ставит блок `::video` в текст и к этим полям отношения не имеет.

**Заведено поле «Расшифровка»** (`public/admin/config.yml`) с подсказкой
своими словами: оставьте пустым — почти всегда так и надо, заполняйте
только если записи в ленте подкаста нет вовсе. Проверки на вид у поля нет
нарочно: имя файла — это номер с хостинга подкаста, и придумывать ему
образец значило бы завести второе мнение о том, как он выглядит.

**ЧЕМ ДОКАЗАНО, ЧТО СНЯТИЕ НИЧЕГО НЕ СЛОМАЛО.** Отпечатки всех файлов
собранного сайта сняты ДО правки и сверены ПОСЛЕ — оба раза в одном
состоянии репозитория, как велит записанный урок. Файлов 9364 и там,
и там; **не изменилась ни одна из 1402 страниц**. Изменились ровно три
вещи, и все три объяснены:

| Что | Почему |
|---|---|
| `_astro/posts-slug.css` | из него ушли правила снятых окошек |
| `admin/config.yml` | в нём появилось новое поле |
| файлы поиска `pagefind/*` | **к правке отношения не имеют:** два прогона поискового индекса подряд, без единой правки, дают им новые имена сами по себе — проверено |

Плюс: настройка админки читается настоящим `js-yaml` (кривая убила бы вход
в админку целиком), поле в ней на месте, кнопка «Видео» на месте.

---

### 20. Сверка с DTF снова умеет и краснеть, и молчать

Четыре разные причины, и все одного рода: **правило выведено, а до тех, кто
его стережёт, не дошло.**

1. **Требование номера телеграм-сообщения теперь называет вызывающий**
   (`checkEdited(raw, { ждётсяTgId })`). Значения по умолчанию нет: забыл
   ответить — функция падает. Три поста, собранные поверх анонса, отвечают
   «да», «Оси но ко» — «нет».
2. **Подлог со спойлером** искал курсив звёздочками; само сравнение про
   подчёркивания знало и снимало оба знака, а подлог — нет. Теперь ловит оба.
3. **Подлог с кадром** выписывал строку картинки целиком, а заказчик подписал
   кадры. Теперь ловит по адресу файла.
4. **Два поста, которые правил заказчик,** спрашиваются тем же путём, каким
   их спрашивает настоящий прогон, а не сверкой с первоисточником в обход
   развилки.

**Проверено:** обычный прогон зелёный (10 ✓), `--selftest` — 44 подлога
пойманы, 9 здоровых файлов молчат.

---

### 21. Самопроверки переживают собственный успех

`archive-hashtags.mjs` и `archive-subheads.mjs`: отсутствие живого примера
теперь **говорит о себе вслух** и берётся записанный образец, а случай
«настоящий пост архива» пропускается совсем — подделать его значило бы
проверять свою выдумку. Подпись случая меняется вместе с источником примера,
чтобы отчёт не врал про «настоящую строку».

Попутно починен отбор примера у хештегов: он спрашивал глобальную регулярку
через `.test()` (та помнит позицию между вызовами) и находил решётку в якоре
адреса. Теперь спрашивает **само правило** — если `stripHashtags` говорит
«тут есть что убрать», строка годится в пример по определению.

**Проверено:** обе самопроверки зелёные, 15 и 18 подлогов.

---

### 22. Крупная кнопка-строка сведена в одно описание

`SupportStrip.astro` больше не хранит вида кнопки вовсе — берёт общий
`.btn-large`. Месту осталась раскладка: сколько кнопок в ряду и какой ширины.

**Доказано замером, а не глазами.** Набор свойств, достающийся кнопке
на собранной странице, снят ДО и ПОСЛЕ: **93 свойства и там, и там,
различий ноль**. Мерилось в одном состоянии репозитория — прежняя версия
возвращена, собрана, измерена, потом правка возвращена и собрана заново
(записанное правило про «до» и «после»). Кегль стрелки и наведение
проверены отдельно по собранному CSS: замер не умеет дочернего комбинатора
и состояний, и это записано в нём же.

Инструмент замера остаётся в проекте: `scripts/measure-computed.mjs`.

---

### 23. Вид марки взят под сверку, но НЕ сведён — и вот почему

**Сводить не стал.** Пока разбирался, выяснилось: **сборщик склеивает
правила с одинаковыми объявлениями**, поэтому четыре копии из исходников
лежат в сборке одним правилом с четырьмя селекторами. Лишнего веса на сайте
нет, цена находки — не сегодня, а тот день, когда копии разойдутся. А сама
правка трогает принятый дизайн в четырёх файлах и в скрипте выдачи поиска,
и проверить её нечем: вид марки держится на псевдоэлементах, которых замер
не видит.

**Поэтому невидимый риск сделан громким.** `scripts/stamp-look.test.mjs`
сверяет ПО ЗНАЧЕНИЮ: зубчики (два размера, шесть мест) и стрелку (три места)
— и падает, если внутри размера появилось второе описание.

**Проверка дважды поймала саму себя, и оба раза это записано в ней:**

- читала только файлы стилей и не видела правил, которые Astro вставляет
  прямо в страницу, — марок с главной и каталога не видела вовсе, но отвечала
  «сходится» (записанный урок проекта, наступили снова);
- ключом стрелки было имя класса, и две марки затирали друг друга: мест три,
  показывала два.

**И подлог был негодный:** первый менял значение во всех копиях сразу, то есть
расхождения не создавал и не ловился. Теперь дописывает своё описание одной
марке.

**Решать вам:** сводить ли всё-таки в общий класс. Я бы оставил как есть —
сверка закрывает риск, а правка трогает принятый вид.

---

### 30. Обещанный раздел заработал и нашёл три настоящие беды

Правило написано и поселено **в сборке**, рядом с соседним вопросом про
ссылки в никуда (`сломаннаяРазметкаСсылок` в
`src/plugins/post-links-integration.mjs`). Разовый скрипт задачи 19 теперь
зовёт его же — своя копия отвечала бы про правило, которого на сайте нет.

Правил два, и оба выведены из настоящих случаев, а не придуманы про запас:
незакрытая круглая скобка и разметка, затёкшая в адрес блока.

**Нашлось три беды, все на ОПУБЛИКОВАННЫХ страницах, все починены:**

| Где | Что видел читатель |
|---|---|
| `ep-106` | Скобка уехала на строку ниже: лишние скобки в тексте, ссылка на битый адрес и одинокая «)» абзацем ниже |
| `ep-97` | В адрес врезки затёк кусок markdown — ссылка вела в никуда |
| `ep-67` | У врезки **подпись и адрес поменяны местами** |

**Проверка** — `scripts/link-markup.test.mjs`: три настоящих случая записаны
дословно, девять здоровых образцов, подлоги на оба правила. Отдельно
проверено, что **сам хук сборки печатает предупреждение**: в живой пост
подложена прежняя строка, хук сказал, файл возвращён.

---

### 35. Три неправды в документах приведены к факту

По вашему правилу «документы трогаем только если ревизия нашла в них
неправду»: `статус/карта-кода.md` («шесть постов с DTF» → десять, плюс
абзац про `guard.mjs`), `scripts/dtf/README.md` («семи сборщиков» →
восьми и девятому), `scripts/dtf/captions.mjs` (несуществующий
`studios.mjs` → `collect-studios.mjs`). Числа проверены пересчётом
сборщиков и их адресов, а не взяты на глаз.

**Больше не починено ничего.** Всё остальное либо требует вашего решения,
либо трогает больше одного места.

---

## Находки, всплывшие уже во время починки

### 37. Расшифровка была мертва на ПЕРВОЙ открытой странице выпуска

**Нашли вы**, открыв `ep-101` по прямой ссылке: нет кнопки «Следить
за плеером», нажатие на реплику не перематывает. На `ep-152` работало.

**Причина не в видеоэссе и не в конкретной странице, а в порядке.** Панель
плеера объявляла себя (`window.__bakaPlayer`) только внутри своего
обработчика `astro:page-load`, а обработчики бегут в порядке тегов
`<script>`. В собранной странице выпуска расшифровка идёт ПЕРЕД панелью —
значит на первой загрузке она спрашивала плеер раньше, чем тот объявлялся,
и получала «плеера нет»: плашки времени гасятся, кнопка не показывается.

**При хождении по сайту беды не видно вовсе:** переходы идут без
перезагрузки, объявленное живёт на `window` и переживает их. Ломалась ровно
та страница, которую открыли первой — по ссылке из мессенджера, из поиска,
из письма. То есть у всех 140 страниц с расшифровками, а не у одной.

**Починено:** панель объявляет себя при исполнении своего модуля, до любых
обработчиков. Одна строка.

**Проверка** — `scripts/transcript-player-order.test.mjs`: гоняет ДВА
настоящих собранных скрипта на подделке окна и спрашивает, видит ли плеер
обработчик расшифровки. `--selftest` возвращает прежнее правило (вырезает
у панели вызов) и обязан покраснеть.

**Проверка дважды переписывалась, и оба раза по своей же вине:** сперва
подделка окна была так бедна, что плеер не успевал объявиться НИ ПРИ КАКОМ
порядке (поймал её собственный `--selftest`); потом подлог менял скрипты
местами, а после починки порядок перестал решать — подлог обязан отменять
ПРАВИЛО, а не переставлять то, от чего правило избавило.

**Оценка.** Поломка, видимая читателю. Жила незамеченной, потому что
проявлялась только на холодном открытии.

---

### 38. Документы обещали автоматическую выкладку, которой нет

**Где.** `CLAUDE.md`, самое начало: «ВЫКЛАДЫВАЕТ САЙТ ПУШ, А НЕ ТЫ…
Cloudflare собирает и выкладывает сайт САМ при каждом пуше».

**Что.** С 13 августа на аккаунте GitHub висит пометка «спам», и вместе
с ней выключены роботы И автосборка Cloudflare — это записано
в [задаче 20](задача-20-флаг-github.md). А инструкция в начале `CLAUDE.md`
осталась прежней, и читается она первой в каждой сессии.

**Чем грозило — и уже стоило.** 15 августа три починки подряд лежали
на GitHub невыложенными. Заказчик дважды открывал сайт, дважды видел старое
и дважды сказал «не работает»; час ушёл на поиск поломки, которой не было.
Указал на причину сам заказчик.

**Оценка.** Неправда в документах, ровно того вида, про который в проекте
записан урок: «УРОК, ОТМЕНЯЮЩИЙ ИНСТРУКЦИЮ, НЕ ОТМЕНЯЕТ ЕЁ — ОНА ПРОДОЛЖАЕТ
ВЫПОЛНЯТЬСЯ». Урок был выведен 13 августа, а инструкция наверху файла
пережила его на два дня.
**ПОЧИНЕНО:** наверху `CLAUDE.md` теперь стоит порядок ручной выкладки
с пометкой «снимут флаг — вернуть прежний», а прежний оставлен ниже.

---

### 39. Правило «жирного в заголовке не бывает» снова нарушено на живом сайте

**Где.** Четыре ОПУБЛИКОВАННЫХ поста: `belyy-albom-triumfalnoe-prodolzhenie`,
`sezon-zlodeek`, `trogatelno`, `unikalnyy-retsept-syonenov`.

**Что.** Решение заказчика 12 августа 2026 — жирного внутри заголовка
не бывает. Разовый скрипт задачи 19 снял его по всему архиву 13 августа
(коммит `69e84fa4`). К 15 августа он стоял снова в четырёх постах.

**Чем найдено.** Замером к находке 36: считались заголовки внутри цитат
и списков (их ноль), а заодно — заголовки с жирным вообще. Их нашлось четыре,
и все на опубликованных страницах.

**Откуда взялись.** Не «скрипт не дошёл»: в состоянии репозитория сразу после
уборки в этих файлах не было НИ ОДНОГО заголовка вовсе. Их дописал заказчик
в админке 13–14 августа (коммиты «Update Пост», «Правки заказчика: 20 подборок
опубликованы, заголовки дописаны») — и написал жирными.

**Чем грозило — и уже стоило.** В `trogatelno` рядом стояли `### Начнём
с сюжета` и `### **А музыка...**`, то есть в ОДНОМ посте обе формы разом —
ровно та беда, из-за которой правило и появилось («Хоримия», «Пара слов
о режиссёре Тацуюки Нагаи»). Читатель это видит.

**Оценка.** Поломка, видимая читателю, и **урок про механизм**: разовая
уборка держит правило до первой правки руками, а узнать о её пропаже
неоткуда.

**РЕШЕНИЕ ЗАКАЗЧИКА 15 августа 2026: снять и предупреждать при сборке.**
**СДЕЛАНО И ВЫЛОЖЕНО** — см. «Что починено», пункт 39.

---

### 40. Скрипт падежных форм ПАДАЛ, и робот, который его гоняет, не отработал бы

**Где.** `scripts/anime-cases.mjs`, сравнение «было/стало» — и через него
робот `anime-cases.yml`.

**Что.** `TypeError: было.every is not a function` на первой же карточке
тайтла, у которой поле-список записано ПУСТОЙ СТРОКОЙ. Так их пишет Sveltia:
очищенное списковое поле она кладёт как `""`, а не пропускает.

**Чем найдено.** Не чтением, а ЗАМЕРОМ ВРЕМЕНИ. Сессия A искала, сколько
робот идёт обычно, чтобы поставить предел; скрипт отработал за 0,14 секунды,
и это было слишком быстро для морфологии по 655 карточкам. Пошёл смотреть
вывод — а там падение. **Число, которого не может быть, — тот же признак
сломанного измерителя, что «ноль там, где ноля быть не может».**

**Замер.** Полей-списков, записанных не списком: **восемь** — `aliasesAuto`
у шести карточек, `aliases` у двух. Обе галочки (`strictQuotes`, `noSpeech`)
булевыми записаны везде, тут беды нет.

**Почему сборка при этом проходила.** Схема сайта эту форму переживает
с восьмого этапа: `z.preprocess(emptyToUndefined, …)` в `src/content.config.ts`.
То есть правило было выведено, записано уроком и применено — к СХЕМЕ.
До скриптов оно не дошло: они читают те же файлы сырыми, мимо схемы.
Ровно тот же вид, что находка 31 («правило выведено, но пройдено не по всем
местам»), только тут между двумя половинами лежит граница «сайт / скрипты»,
и потому она не бросается в глаза.

**Чем грозило.** Робот падежных форм не отработал бы вовсе. Падежи — это то,
чем упоминания тайтлов ловятся в тексте («Ходячего замка»), так что новый
тайтл просто не находился бы в постах, а причину искали бы в поиске упоминаний.

**Оценка.** Поломка, живая на момент находки.
**ПОЧИНЕНО 15 августа 2026, сессия A.** Чиним ЧТЕНИЕ, а не данные: пустую
строку пишет сама админка, и первое же сохранение вернуло бы её назад.
`readAnimeCollection` приводит списки к спискам, а рядом кладёт СЫРОЕ
содержимое — запись идёт из него, иначе приведение типов молча переписало бы
`aliases: ""` в `aliases: []` у восьми чужих карточек. Проверка —
`scripts/anime-card-read.test.mjs`, подлог возвращает прежнее сырое чтение
и роняет ровно то сравнение, на котором падал робот.

---

## Что проверено и находкой НЕ является

Записано, чтобы следующая сессия не искала это заново.

- **Цветов, написанных значением, в исходниках сайта нет ни одного.**
  Замер: 15 токенов-цветов разобраны из `tokens.css`, весь `src/`
  просмотрен на `#hex` и `rgb()` — совпадений ноль.
- **Точки перелома все системные.** Единственная несистемная — 432
  в `CategoryTabs.astro` — записана в карте кода («Ниже 432 зазор 10
  и кегль 11»).
- **Двойной фокус у трёх полей поиска** — записанное решение карты кода,
  а не разъехавшаяся копия.
- **Правый край `6 / 13` на «О проекте» и «Поддержать»** — записанное
  решение.
- **Схема сборки переживает пустоту и `null` в любом необязательном
  поле**, а у галочек «Черновик», «только в кавычках» и «не искать
  в расшифровках» пустота читается в безопасную сторону — разобрано
  явно и с объяснением в `content.config.ts`.
- **Все проверки `pattern` в админке разрешают пустое значение** (`^$|…`) —
  записанный урок пройден по всем девяти полям.
- **Слепок поля тайтла в админке совпадает с `src/lib/animeFieldHash.mjs`**
  посимвольно.
- **`.env` с ключом ElevenLabs лежит в рабочей копии, но в `.gitignore`
  и не отслеживается git** — как и `state.json`, и `__pycache__`.
- **Внешние запросы питона с таймаутами**: скачивание аудио — 120 секунд
  и три повтора, распознавание — час (посчитан по самому длинному выпуску
  архива в 94 минуты). Ключ в сообщениях об ошибке не печатается.
- **Пустой ответ сервиса распознавания роняет прогон громко**, а сверка
  названий стоит ПОСЛЕ записи транскрипта и обёрнута в перехват ошибки.
- **Семь самопроверок из девяти в задачах 16–19 зелёные:**
  `archive-headings`, `archive-season-titles`, `archive-anime-links`,
  `archive-own-links`, `archive-source-insets`, `archive-external-posts`,
  `archive-youtube-embed`.
- **Скрипты, которые ПОДКЛЮЧАЮТ другие** (`archive-rules-measure`,
  `archive-own-links`, `archive-duplicates`, `archive-youtube-match`),
  защиту «запустили напрямую?» имеют все.

---

## Вкусовщина (не находки)

- **`src/components/Header.astro`:** число 821 стоит и в медиазапросе,
  и в `matchMedia` внутри того же файла. Место одно, соседнее, арифметика
  записана в заголовке файла — но число всё-таки два раза.
- **`archive-own-links.mjs` и `archive-source-insets.mjs`** приводят
  `\n{3,}` к двум переводам строки по ВСЕМУ телу поста, а не только там,
  где резали.
- **Подпись под кадром сборника считают две разные функции с одним
  именем** `captionFor`: в `scripts/dtf/captions.mjs` (со студиями
  из `studios.json` и формой «студии X и Y») и в
  `scripts/osen-2022/build.mjs` (со студией из карточки каталога, только
  одна). Обе правы по-своему.
- **`archive-youtube-match.mjs`:** при полностью равной оценке двух
  роликов побеждает тот, что раньше в файле выгрузки. Второй кандидат
  при этом печатается, так что вслепую это не решается.
- **Админка не экранирует кавычку в `src` и `source-url`** — только
  в подписи и альт-тексте. В адресе кавычка не встречается, но правило
  разное у соседних строк.
- **Самопроверка `archive-hashtags`** ищет пример глобальной регуляркой
  через `.test()` — та помнит позицию между вызовами.

---

## Чего НЕ просмотрено

Честный список.

1. **Доступы на сервере изнутри** (≈0,5 сессии, **нужен ваш ssh**).
   Что на самом деле разрешено жильцу `tgexport`, что раздаёт Caddy,
   жив ли `videouploader`. Снаружи проверено прошлой сессией: листинг
   закрыт, корень отдаёт 404.
2. **Полностью прочитаны не все разовые скрипты** (≈0,3 сессии). Читались
   целиком: общая библиотека, все двенадцать `archive-*` кроме
   `archive-rules-scan.mjs` (371 строка) и `archive-rules-measure.mjs`
   (705), `dtf/guard.mjs`, `dtf/source.mjs`, `dtf/fetch-images.mjs`,
   `dtf/captions.mjs`, `dtf/build-oshi-no-ko.mjs`, `osen-2022/build.mjs`.
   Остальные семь сборщиков DTF и `dtf/verify.mjs` (774 строки) смотрены
   выборочно и прогонами.
3. **Стили смотрелись замерами, а не чтением** (≈0,5 сессии). Четыре
   замера отвечают на вопрос «где копия», но не на вопрос «правильно ли
   тут вообще». Дизайн принят, и трогать его ревизия не должна, — но
   компоненты целиком никто не читал.
4. **Админка прочитана частично** (≈0,3 сессии). Из 1849 строк смотрены
   свои кнопки редактора, обработчик сохранения, копии правил и работа
   с пустотой; поля-виджеты подсказок и разбор расшифровок — нет.
5. **Питон прочитан только по денежному пути** (≈0,4 сессии).
   `script_align.py` (411 строк), `script_text.py` (227), `auto.py` (648)
   и `align_essays.py` (140) не смотрены вовсе.

---

### 39. Правило переехало в сборку, а четыре поста починены

**Снято у четверых, проверено идемпотентностью** (повторный прогон меняет
0 постов). Правка — четыре строки, шапки не тронуты; выложено руками
на обе копии и проверено ПО СОДЕРЖИМОМУ живых страниц: жирного в заголовках
ноль. **Сама проверка живой страницы спрошена подлогом**, и первый подлог
оказался негодным — он не заменил ничего (в разметке многоточие стоит одним
знаком `…`, а искали три точки), то есть его «ноль» ничего не значил.
Переписан на настоящую строку живой страницы: на подлоге находит, на здоровом
молчит.

**Правило поселено в `npm run build`** — `scripts/check-heading-bold.mjs`.
Оно **только говорит вслух** и называет пост и заголовок поимённо:

- **чинить молча нельзя** — тихо подменённое слово в тексте заказчика запрещено
  правилом проекта;
- **ронять сборку нельзя** — текст пишет человек, и его правка не должна
  оставлять сайт невыложенным.

Это ровно то устройство, что у соседнего правила про битую разметку ссылок:
разовый скрипт и сборка зовут ОДНУ функцию, своя копия отвечала бы про
правило, которого на сайте нет.

**Заодно эта же работа поймала свежий промах в себе:** новая проверка сборки
не имела развилки «запустили напрямую?», и когда её функцию позвал
`heading-bold.test.mjs`, она приняла чужой ключ `--selftest` за свой
и прогнала свою самопроверку посреди чужой. Нашла это проверка находки 25,
написанная часом раньше.

---

## Как разложить починку по сессиям

По областям, а не одной кучей — так проще проверять. Продолжает разбивку
из [передачи](передача-15-по-областям.md), сессии A–E.

### Сделано 15 августа 2026

| Сессия | Находки | Итог |
|---|---|---|
| **F. Деньги** | 19 | журнал расшифровки спрашивает три источника; измерено: старое правило запросило бы $25.64 за оплаченное |
| **G. Проверки задач 16–19** | 20, 21, 30 | сверка с DTF зелёная в обе стороны; самопроверки переживают свой успех; найдены и починены три битые ссылки на опубликованных страницах |
| **H. Копии правил вида** | 22, 23 | кнопка сведена (доказано: 93 свойства до и после); вид марки взят под сверку, но НЕ сведён — решать вам |
| попутно | 37, 38 | расшифровка ожила на первой открытой странице; `CLAUDE.md` больше не обещает автовыкладку |
| **I. Копии правил в скриптах** | 25, 26, 27, 31, 32, 36 | шесть домов вместо пятнадцати копий; в трёх местах нашлось БОЛЬШЕ, чем называл реестр |
| попутно | 39, 34 (часть) | жирный ушёл из заголовков четырёх опубликованных постов, правило переехало в сборку; снята мёртвая копия `PAIR_KEYS` |

**Что сессия I изменила по существу.** Не «стало красивее»: у каждой из шести
находок появился ОДИН дом и ОДНА проверка, умеющая провалиться. Список домов:

| Правило | Дом | Проверка |
|---|---|---|
| «меня запустили напрямую или подключили» | у всех шестнадцати скриптов | `archive-entry-guard.test.mjs` |
| сорт пары дубликатов | `сортПары` в `archive-duplicates.mjs` | `dedupe-kind.test.mjs` |
| повтор при сбое сети | `scripts/retry.mjs` | `retry-home.test.mjs` |
| «шапка читается обратно» | `scripts/frontmatter-guard.mjs` | `frontmatter-guard.test.mjs` |
| кавычка в атрибуте директивы | `src/lib/directiveAttr.mjs` | `directive-attr.test.mjs` |
| жирный внутри заголовка | `scripts/heading-bold.mjs` | `heading-bold.test.mjs` + шаг сборки |

**ТРИ РАЗА РЕЕСТР НЕДОСЧИТАЛ, И ВСЕ ТРИ РАЗА НАШЁЛ НЕ ГЛАЗ, А ПОИСК
ПО ПРИЗНАКУ**, написанный при сведении:

- повтор при сбое сети — копий было ТРИ, а не две (третья в `collect-studios.mjs`);
- экранирование подписи — мест мимо правила ВОСЕМЬ, а не семь (плюс подпись
  врезки в `build-podborki-2023.mjs`);
- «жирный в заголовке» — расхождений ДВА, а не одно (второй скрипт видел
  только заголовок, жирный целиком).

Отсюда урок в `CLAUDE.md`: число копий, названное реестром, — нижняя оценка.

### Осталось

**Сессия J. Уборка и правда в документах** (находки 33, 34 и остаток 35).

1. **33** — семь имён «старого слоя» в `tokens.css` не читает никто,
   два держит одно правило `a {}` в `global.css`, у которого комментарий
   обещает переделку «в части 3». Часть 3 давно прошла.
2. **34, остаток** — мёртвое: неиспользуемый и уже неправдивый `ARTICLES`
   в `dtf/source.mjs`, `void fold` в `archive-anime-links.mjs`,
   класс `.long-text` в `global.css`. **Копия `PAIR_KEYS` под именем
   `unusedGroups` СНЯТА 15 августа** — она стояла ровно в той строке, которую
   правила находка 26, и оставлять рядом мёртвый дубль списка после сведения
   было бы смешно.
3. **35, остаток** — комментарий в `global.css` про «часть 3»: переписать
   его значит объявить новый план для цвета ссылок, а это ваше решение.
4. Плюс находки из ПЕРВОГО реестра, до которых руки не дошли: **11**
   (задача 19 нигде не описана), **12** и **12а** (список полей и палитра
   служебных страниц), **13** (мёртвое), **14** (обновить Astro), **16**
   (числа в `СТАТУС.md`).

**Как проверить:** сборка, три проверки, поиск по коду на снесённое.
И перечитать `СТАТУС.md` целиком.

### Что заказчик решил 15 августа 2026, сессия I

Спрошено в начале сессии, ответы записаны здесь, чтобы следующая сессия
не спрашивала заново.

| Вопрос | Ответ |
|---|---|
| **39** — четыре поста с жирным в заголовке | снять И предупреждать при сборке |
| **36** — жирный в заголовке внутри цитаты, спойлера, списка | снимать ВЕЗДЕ |
| **23** — сводить ли вид марки тайтла в общий класс | **оставить как есть**, сверка риск закрывает |
| **Сессия C, доступы** | **пока ничего не менять**: версию админки не закреплять, SFTP-учётку на Hetzner не закрывать, способ входа не трогать. Из кода остаётся дописать воркеру входа `"workers_dev": true` — документы уверяют, что строка там есть, а её нет |

**Решение 60 («видео на зеркале нет») перерешивать нечем — оно устарело
целиком.** Замер 15 августа: своих видеофайлов в сборке НЕТ НИ ОДНОГО,
все пять роликов вставлены блоком `::video` и лежат в бакете `baka-media`
в Yandex Object Storage, то есть открываются на обеих копиях одинаково.
Кода это не касается; в сессии J решение переезжает в архив хвостов как
закрытое.

### Ждёт вашего решения, а не сессии

- **Сессии A–E из первой передачи** никуда не делись: таймауты и пределы
  роботов, счётчики охвата у трёх проверок, уборка. Их порядок и содержание —
  в [передаче](передача-15-по-областям.md). Сессия C сведена к одной строке
  кода — см. таблицу выше.
- **Доревизия не закончена:** область 5 (доступы на сервере изнутри)
  не смотрелась вовсе — **нужен ваш ssh к Hetzner**. Что там смотреть: что
  правда разрешено жильцу `tgexport`, что раздаёт Caddy, жив ли
  `videouploader`. Список того, что просмотрено не целиком, — выше,
  в разделе «Чего НЕ просмотрено».
