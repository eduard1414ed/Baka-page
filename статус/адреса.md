# Ключевые адреса

Переехало из [`СТАТУС.md`](../СТАТУС.md) 7 сентября 2026. **Текст перенесён
дословно**, в сводке остались самые ходовые строки и ссылка сюда.

| Что | Адрес |
|---|---|
| **ГЛАВНЫЙ АДРЕС ДЛЯ ПОИСКОВИКОВ** | **https://ru.bakapodcast.com** — `canonical` со ВСЕХ страниц обеих копий ведёт сюда ([задача-14](задача-14-поисковики.md)) |
| Сайт на Cloudflare | **https://bakapodcast.com** (и `www.`) — резерв и место, где живёт админка |
| **Зеркало для России** | **https://ru.bakapodcast.com** — тот же сайт, обновляется сам; админки на нём нет ([задача-13](задача-13-зеркало.md)) |
| Архив | `/archive/`, дальше `/archive/2/`; разделы — `/category/podcast/` |
| Каталог аниме | `/anime/`, страница тайтла — `/anime/naruto/` |
| Поиск | `/search/` |
| Поддержать | `/support/`, тексты — админка, «Страницы» → «Поддержать» |
| О проекте | `/about/` (адрес старый, название новое), тексты — админка |
| Пользовательское соглашение | `/legal/`, текст — админка, «Страницы»; ссылка и пометка 18+ в подвале |
| Карта сайта / robots | `/sitemap.xml`, `/robots.txt` — собираются кодом, `src/pages/` |
| Карта для нейросетей | `/llms.txt` |
| Лента постов сайта | `/rss.xml` (это не RSS подкаста) |
| Счётчики посещаемости | Яндекс.Метрика **111614190**, Google Analytics **G-W9R5T2Y90D** — по одному на обе копии сайта, Вебвизор выключен; код — `src/components/Analytics.astro`, события целей — `src/components/AnalyticsEvents.astro`, номера — `src/lib/analytics.mjs` |
| Веб-аналитика | Программный доступ ко всем четырём источникам: Метрика, Google Analytics, Search Console, Яндекс.Вебмастер. Ключи в `.env`, данные и отчёты — в папке «Аналитика Баки» РЯДОМ с репозиторием. Отчёт: `node scripts/analytics/report.mjs`. 12 целей заведены в обоих сервисах, события шлёт `src/components/AnalyticsEvents.astro`, проверка — `scripts/analytics-events.test.mjs`; список и смысл каждой — [цели-аналитики](цели-аналитики.md), как всё устроено — [задача 27](задача-27-аналитика.md) |
| Репозиторий | https://github.com/eduard1414ed/Baka-page |
| Как git ходит на GitHub | **По SSH**, ключ `~/.ssh/id_ed25519` (с 7 сентября 2026). Нарочно НЕ через `gh`: тот отдаёт ключ только активному аккаунту, и второй проект заказчика гасил первый — [задача-36](задача-36-правило-анонса.md) |
| Админка | `/admin` на сайте, вход через GitHub |
| Воркер входа в админку | https://baki-cms-auth.eduard1414ed.workers.dev |
| Робот-будильник (ручной запуск) | https://baki-publish-cron.eduard1414ed.workers.dev/run |
| Кнопки роботов | `/admin/tools/` |
| Разбор кандидатов в тайтлы | `/admin/tools/candidates/` |
| RSS подкаста (Mave) | https://cloud.mave.digital/33503 |
| Сервер Hetzner | `ssh -i ~/.ssh/hetzner_baki root@167.233.251.252` |
| Роботы на сервере | `systemctl list-timers 'baka-*'` — когда следующие заходы. Позвать: `systemctl start baka-episodes` (выпуски), `baka-telegram` (посты), `baka-posts` (тайтлы), `baka-deploy` (сборка), `baka-cases@dry` / `baka-cases@write` (падежи), `baka-apply` (решения). Копии файлов — в [`hetzner/`](../hetzner/) |
| Обработчик кнопок | `baka-robot-api.service` на сервере, слушает 127.0.0.1:8099; наружу — через Caddy по `/robot/*`. Ключ запуска: `/root/baka-secrets/robot-api.env` |
| **Открыть админку (двойной щелчок)** | **`Открыть-админку.command`** в корне проекта — **забирает свежее из хранилища и говорит, что привезли роботы**, поднимает админку, открывает её и страничку с кнопкой «Выложить». Закрыли окно — админка выключилась ([задача-26](задача-26-ярлык-и-кнопка.md)) |
| **Выложить правки (двойной щелчок)** | **`Выложить-на-сайт.command`** — то же, что кнопка, но без админки: забирает чужое, кладёт своё, собирает и раскладывает на оба адреса ([задача-25](задача-25-запускатор.md)) |
| Быстрая выкладка командой | `npm run deploy` — то же самое, но без забора чужих правок: только для того, у кого всё уже закоммичено и запушено |
| Журналы робота | `/root/baka-logs/` на сервере, старше 30 дней удаляются сами |
| **Свои видеофайлы (как есть сегодня)** | **Yandex Object Storage, бакет `baka-media`** — `https://storage.yandexcloud.net/baka-media/…`; так вставлены все пять роликов в постах, проверено 15 августа 2026 |
| Раздача видео на Hetzner | `https://167-233-251-252.sslip.io/files/…` (Caddy + sslip.io) — **жива, но сайтом не используется**: ни один пост на неё не ссылается |
| Экспорт телеграма на сервере | `/srv/telegram-export/ChatExport_2026-08-10/` — **наружу не раздаётся** (проверено изнутри 15 августа: лежит вне раздаваемой папки, плюс отдельное правило 404 в Caddy) |
| Чужая служба на том же сервере | `tg-translator-bot` в `/opt/tg-translator-bot`, жилец `botuser` — **к сайту отношения не имеет**, работает с 7 августа. Записано, чтобы её не приняли за нашу и не пошли «убирать лишнее» |
| Доступ робота к экспорту | жилец `tgexport`, только чтение; секрет GitHub `TG_EXPORT_SSH_KEY` |
| Бот регулярного импорта | секрет GitHub `TG_BOT_TOKEN` |
| Отметка «докуда забрано» | `src/data/telegramFeed.mjs` — правит робот, руками не трогать |

Старый служебный адрес `baka-page.eduard1414ed.workers.dev` **отключён и отдаёт
404** — так задумано ([домен](домен.md)). `www.bakapodcast.com` показывает
тот же сайт, и это закрыто **тегом canonical** на каждой странице; редирект
решили не делать.
