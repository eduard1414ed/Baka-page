# Этап 8. Новый дизайн — части 1–4

> Это подробности одной сессии. Сводка по всему проекту — в [СТАТУС.md](../СТАТУС.md),
> правила, выведенные из опыта, — в [CLAUDE.md](../CLAUDE.md).

Нормативные документы дизайна лежат в папке `Сборка-18` в корне проекта:
`дизайн-система-1.5.md` (токены, компоненты, правила), `карта-соответствий.md`
(что чему соответствует и где мины), `подготовка-к-реализации.md` (шрифты,
иконки, метатеги, проверки). Плюс четырнадцать HTML-макетов.

Части 1 и 2 сделаны **одной сессией намеренно**: пока шапка не переехала,
остальные страницы выглядят сломанными, и легко решить, что дело в них.
Часть 3 — следующая сессия, её разделы идут ниже, после разделов частей 1 и 2.

## Что сделано — 9 августа 2026

### Часть 1. Токены, шрифты, база

**Светлая палитра.** `src/styles/tokens.css` переписан. Значения — раздел 3.3
дизайн-системы. Тёмной темы больше нет, `prefers-color-scheme` не поддерживается.

**Главная мина сессии обойдена.** В проекте `--space-1` это 8 px, в дизайне
такое же по имени значение — 4 px. Совпадают имена, отличаются значения.
Подмени мы значения — весь существующий код уехал бы вдвое-втрое, и **ни одной
ошибки нигде бы не появилось**. Поэтому:

- новая шкала называется `--sp-1 … --sp-18` (те же номера, что в §3.5 системы);
- старая `--space-1 … --space-5` осталась ровно такой, какой была;
- удалим старую, когда на неё не останется ссылок. Сейчас их около 130
  в 18 файлах. Проверять: `grep -rn "var(--space-" src/`.

Имена выбраны так, чтобы будущее удаление было механической заменой
`sp` → `space` один в один.

**Старые цветовые токены не удалены, а перенаправлены.** У `--color-bg`,
`--color-text`, `--color-border` и прочих больше нет своих значений — каждый
указывает на новый токен. Благодаря этому весь ещё не переверстанный код
(карточки, ленты, плеер, транскрипт, каталог) поехал на светлую палитру,
и ни один из этих файлов трогать не пришлось.

Два перенаправления не очевидны и заслуживают строки:

| Старое | Стало | Почему так |
|---|---|---|
| `--color-accent-hover` | `--ink` | Наведение было светлее акцента. На бумаге светлее — значит бледнее и хуже читается, поэтому темнеет |
| `--color-accent-2` (жёлтый) | `--ink` | Второго акцента в системе нет. Жёлтый стоял фоном у подсветки поиска и у плашек; на бумаге он даёт 1.5:1 и текстом невидим. Чернильный работает в обе стороны: и фоном под бумажным текстом, и сам текстом |
| `--color-bg-elevated` | `--paper` | Поднятого фона в светлой теме нет — плиток в языке сайта не осталось. Плитки исчезнут насовсем в части 3 |

**Шрифты подключены.** `src/styles/fonts.css` — взят из макетов без изменений.
Файлы лежали в `public/fonts/` с прошлой сессии, но ни один `@font-face`
на них не ссылался; теперь ссылается.

Первой строкой `baka.css` в макетах стоит `@import` с `fonts.googleapis.com` —
**в проект он не переносился**. Иначе сайт продолжил бы ходить за шрифтами
на чужой домен, и вся работа по локальным файлам оказалась бы бесполезной.
Проверено: `grep -rn "googleapis\|gstatic" src/ public/` даёт пусто
(единственное совпадение — слово в комментарии, объясняющем, почему его нет).

**База** — `src/styles/global.css`: сброс, типографика элементов по шкале §3.2,
`focus-visible` обводкой на весь сайт, `min-width: 0` для детей сеток,
`overflow-wrap`, `prefers-reduced-motion`, skip-link, технический слой `.tech`,
оболочка `.site`.

Два решения в базе, о которых надо знать:

- **`overflow-x: hidden` с `body` снят.** Он не чинил горизонтальный скролл,
  а прятал его — и заодно срезал фокусную рамку у всего, что близко к краю.
  Это прямой запрет §4.2. **Следствие: если где-то вёрстка поедет вбок,
  теперь это будет видно.** Так и надо: дефект должен быть заметен.
- **`prefers-reduced-motion` сделан через обнуление токена**, а не через
  сброс каждого правила. Обычный приём требует пометки принудительного
  приоритета, а она в системе запрещена. Компоненты берут длительность
  только из `--transition-fast`, и под этим медиазапросом она становится `0s`.

### Часть 2. Шапка, подвал, метаданные

**Шапка переписана целиком.** Два состояния вместо прежних четырёх:

- **до 821 px** — логотип, поиск и кнопка меню; разделы в выпадающем списке;
- **от 821 px** — привычная раскладка с тэглайном, меню в строку.

**Поиск остаётся снаружи меню на всех размерах.** Это главная функция сайта,
прятать её на два касания нельзя.

Меню закрывается по Escape (с возвратом фокуса на кнопку), по клику мимо
и само при переходе на широкий экран. `aria-expanded` отражает состояние.
Активный пункт помечен и цветом, и подчёркиванием, и `aria-current="page"` —
цвет не может быть единственным отличием.

Скрипт меню **не привязывается к конкретной кнопке**: слушатели висят
на `document` и ищут элементы в момент события. Так надо, потому что переходы
между страницами идут без перезагрузки (`ClientRouter`), и сама шапка при
переходе заменяется новой. Привяжись мы к элементу — после первого же перехода
меню перестало бы открываться.

**Логотип пришлось заменить.** Прежний `src/assets/images/logo.png` — белые
буквы на прозрачном фоне, он рисовался под тёмную тему: средний цвет
непрозрачных пикселей ровно `255,255,255`. На бумажном фоне его не было бы
видно вообще. Чернильный вариант того же логотипа лежал в макетах шапки,
вытащен оттуда в `src/assets/images/logo-ink.png` (1068×237, средний цвет
`17,17,15` — это и есть `--ink`). **Белый файл остался в проекте, но больше
нигде не используется.**

**Пункт «Поддержать» ведёт в никуда** — по вашему решению. Страницы `/support/`
на сайте нет, она в части 10 ТЗ; до тех пор клик по пункту даёт 404.

**Подвал.** Копирайт слева, ссылки справа, одна колонка до 821 px. Ссылки
на разделы сайта из подвала убраны — они переехали в меню шапки, которого
раньше не было. Осталось то, чего в шапке нет: площадки подкаста и `/rss.xml`.

**Ссылка «Перейти к содержимому»** — первый фокусируемый элемент каждой
страницы, ведёт на `#main`, видна только в фокусе. Проверяется одним Tab.

**Метаданные** в `Layout.astro` — полный набор из раздела 8.1 подготовки
к реализации: заголовок, описание, четыре иконки, манифест, canonical,
og-теги, `twitter:card`.

Одно отличие от прежнего поведения, заметное глазами: **`og:title` теперь
совпадает с `<title>`**, то есть «Название — Бака!». Раньше в него уходило
голое название без приписки. Так написано в разделе 8.1 дословно. **Заголовок
в превью ссылки станет длиннее** — смотреть в телеграме.

**Файлы в `public/`** из макетов: `favicon.ico`, `apple-touch-icon.png`,
`icon-192.png`, `icon-512.png`, `site.webmanifest`, `character-404.png`,
`character-empty.png`. Две последние — для частей 9 и 10 (заглушка изображения
и страница 404), сейчас лежат без дела.

**Старый `favicon.ico` перезаписан** новым из макетов (буква «Б» чернилами
на бумаге). Прежний лежит в истории git. `public/favicon.svg` остался в папке,
но из `Layout.astro` на него больше не ссылаются: браузеры предпочитают SVG
всем остальным форматам, и он перебивал бы утверждённую иконку.

### Часть 2.4. Картинки превью для соцсетей

Сделаны. Каждая страница получает полосу **ровно 1200×630** по адресу
`/og/{slug}.jpg`, страницы без своей обложки — общую `/og/default.jpg`
(логотип на бумаге).

**Как это устроено — три файла и одно правило, живущее в одном месте.**

| Файл | Что делает |
|---|---|
| `src/lib/postImage.mjs`, функция `socialSource` | Единственное место, где решается, ИЗ ЧЕГО делать превью: обложка поста → обложка выпуска → первая картинка в тексте → ничего |
| `src/pages/og-sources.json.ts` | Список «превью → исходник». Считается теми же `getCollection` и `isPublished`, что и страницы, поэтому в него не может попасть черновик. Служебный файл: сборка удаляет его сразу после того, как нарисует картинки |
| `src/lib/ogImage.mjs` | Адреса и размеры. Их знают и страница, и генератор |
| `src/plugins/og-images-integration.mjs` | Рисует сами файлы после сборки |

Правило «из чего делать превью» вынесено в общую функцию намеренно. Его нужно
знать двоим — странице (она ставит теги) и сборке (она рисует файл). Две копии
разошлись бы молча, и превью показывало бы не ту картинку, на которую ссылается
страница.

**Порядок шагов сборки важен.** Генератор превью зарегистрирован в
`astro.config.mjs` **после** сжатия загруженных картинок: часть исходников —
это как раз jpeg-копии, которые создаёт первый шаг. Поменяете местами —
превью выпусков останутся без исходников. В файле про это стоит комментарий.

**Кадрирование: обрезаем только то, что и так шире.** Порог — 3:2.

Он не 1200:630 (то есть 1.90), хотя правило звучит буквально так. Кадр 16:9 —
это 1.78, до 1.90 он не дотягивает, и при буквальном пороге его вписывало бы
с бумажными полями по бокам. А раздел 4 подготовки к реализации прямо называет
16:9 обрезкой по центру: теряется 6 % высоты, зато карточка заполнена целиком.
3:2 — граница между «пейзажный кадр, переживёт обрезку» и «выстроенная
картинка, резать нельзя». Квадратная обложка выпуска (1:1) и постер аниме (2:3)
остаются по эту сторону и вписываются целиком.

Проверено на реальных файлах: у выпуска `ep-134` на превью целы и название
сверху, и надпись «БОНУС / КЕЙОН VIII» справа — ровно то, что срезала бы
обрезка квадрата до полосы. У статьи `znakomtes-yodzi-takesige` кадр 16:9
обрезан по центру и заполняет карточку целиком.

**Чего сборка не делает и не должна.** Если исходник лежит на чужом сервере
(обложка совсем свежего выпуска, которую робот ещё не скачал), в сеть за ним
никто не идёт: сборка от чужого домена зависеть не должна. Такому материалу
кладётся общая картинка сайта под его именем — ссылка на странице остаётся
рабочей, а в лог уходит строчка, что картинка не своя. Молча битой ссылки
не бывает.

**Файлов делается ровно столько, сколько нужно.** Генератор рисует только те
превью, на которые реально ссылаются собранные страницы, — тот же приём, что
у обложек выпусков в соседнем шаге. Иначе на архиве в полторы сотни постов
сюда посыпались бы файлы для страниц, которых не существует.

**Что изменилось видимого:** превью главной было баннером сайта, стало
логотипом на бумаге — так требует раздел 4 («материал без обложки получает
общую картинку сайта»). Баннер по-прежнему стоит на самой главной, из превью
он ушёл.

**Страницы тайтлов своего превью пока не получили** — им нужно отдельное
пространство имён (`/og/anime-<id>.jpg`), иначе тайтл с идентификатором вроде
`ep-5` перебил бы выпуск. Постеры 2:3 генератор уже умеет вписывать
не обрезая, так что это одна строка в части 7.

## Что проверено и чем — части 1 и 2

- **Сборка проходит** — три полных прогона `npm run build` без ошибок,
  22 страницы.
- **Шрифты подключены по-настоящему** — в собранном CSS пять правил
  `@font-face`, все пять ссылаются на `/fonts/…woff2`; файлы лежат в `dist/fonts/`.
- **Палитра светлая в собранном CSS** — `--paper:#f8f6f0`, `--ink:#151515`,
  `--color-bg:var(--paper)`.
- **Обе шкалы отступов живы и не смешались** — в собранном CSS одновременно
  `--sp-1:4px` и `--space-1:.5rem`.
- **Разметка шапки на месте** — проверена в `dist/index.html`: skip-link первым
  элементом `<body>`, `aria-expanded="false"` на кнопке, `aria-current="page"`
  на активном пункте, `id="main"` на `<main>`.
- **Метатеги на месте** — проверены на главной и на странице выпуска: иконки,
  манифест, canonical, весь набор og, `twitter:card`.
- **Логотип действительно чернильный** — средний цвет непрозрачных пикселей
  посчитан, `17,17,15`.
- **Обращений к Google за шрифтами нет** — `grep` по `src/` и `public/` пуст.
- **Превью нарисованы и померены** — семь файлов, у всех ровно 1200×630,
  вес 21–86 КБ. Проверено, что вписанные лежат на бумаге, а обрезанный
  заполняет карточку. Две картинки открыты глазами.
- **Служебный список исходников на сайт не уезжает** — `dist/og-sources.json`
  после сборки отсутствует.

## Чего НЕ проверено — части 1 и 2

Всё, что живёт в браузере, — по правилу «Как проверяем работу» это за вами.
Ни одна страница глазами не открывалась.

- **Как сайт выглядит.** Он стал светлым, шрифты сменились на три новых,
  заголовки стали другого кегля. Карточки, плеер, транскрипт и каталог ещё
  не переверстаны — они будут выглядеть чужеродно, и это нормально.
- **Меню на телефоне** — открывается, закрывается по Escape и по клику мимо,
  не залипает при переходе между страницами.
- **Tab на любой странице** — первым делом должна появиться чёрная плашка
  «Перейти к содержимому» в левом верхнем углу.
- **Горизонтальный скролл.** `overflow-x: hidden` снят, и если где-то вёрстка
  вылезает за край, теперь это станет видно на узком экране.
- **Превью ссылки в телеграме.** Изменилось двое: заголовок стал длиннее
  («Название — Бака!») и сама картинка стала полосой 1200×630. **Обязательно
  через `@WebpageBot`** — телеграм надолго запоминает старое превью, и без
  сброса кэша вы увидите вчерашнее. Проверять все три вида ссылок: главную,
  выпуск и статью.
- **Иконка во вкладке.**

## Пять проверок раздела 8.4 — после части 2

| Проверка | Результат |
|---|---|
| Размеры шрифта меньше 11 px | пусто |
| Пометки принудительного приоритета | пусто |
| Видимость и раскладка в одном селекторе | пусто |
| `auto-fit` вместо `auto-fill` | **2 строки, `BonusPaywall.astro`** |
| Цвета мимо переменных | **5 строк, `PostCard.astro` и `posts/[slug].astro`** |

Обе непустые проверки — **не то, что сделано в этой сессии**, а то, что лежало
в проекте до неё. Обе живут в файлах, которые эта сессия трогать не должна:
карточка — часть 3, страница материала — часть 5, paywall — часть 10.

- **`auto-fit`** в кнопках paywall. Правильная починка не замена слова,
  а фиксированные 4/2/1 колонки по §15 — это часть 10. Сейчас не видно
  никому: бонусных постов на сайте нет ни одного.
- **Цвета мимо переменных** — белая стрелка ▶ поверх превью видео и две
  полупрозрачные чёрные подложки под ней. На светлой теме они не сломались:
  стрелка лежит на тёмном кадре и на тёмной подложке. `карта-соответствий.md`
  прописывает замену на `--paper` с тенью — вместе с переверсткой карточки
  в части 3.

Скажите, если хотите закрыть их сейчас, — это правка на пять строк, но она
залезает в файлы частей 3, 5 и 10.

## Цифры — части 1 и 2

- Файлов в сборке: **556 из 20 000** (было 544). Прибавили семь файлов
  в `public/` и семь картинок превью; разброс в пару файлов между сборками —
  обычная зависимость превью-обложек от сети, см. [шрифты.md](шрифты.md).
- Вес сборки 40 МБ, папка превью 392 КБ.
- **Когда опубликуете весь архив, превью станет около 150** — по файлу
  на материал, вес порядка 9 МБ. Запас по лимиту Cloudflare это не тронет.
- Ссылок на старую шкалу отступов: около 130 в 18 файлах — столько предстоит
  перевести на `--sp-*` за части 3–11.

## Что сделано — часть 3, 9 августа 2026

### Карточка материала

**Плитки больше нет.** Ни фона, ни рамки, ни скругления — контент лежит
на бумаге, соседние карточки разделяет одна линия. Вместе с рамкой ушёл
`focus-within`, которым показывался фокус; теперь его показывает общая обводка
`:focus-visible` из части 1. **Проверено, что обводка на месте, ДО того как
рамка была снята** — и в исходниках, и в собранном CSS прошлой сборки.
Между этими двумя правками сайт нельзя было бы пройти с клавиатуры.

**Два варианта одного компонента, а не два файла** (`PostCard.astro`):

| Вариант | Где | Что показывает |
|---|---|---|
| `cover` | главная в режиме типа, разделы, страница тайтла | метастрока, обложка, заголовок, подводка |
| `text` | подборка «Из архива» | метастрока, заголовок, подводка, дата |

**Метастрока переехала наверх** и стала единственным местом, где живёт
«сколько это займёт». Серой строки со временем под заголовком больше нет:
один факт — одно место. Формат `[ ПОДКАСТ ] · №135 · 01:06:33`.

Три решения внутри метастроки заслуживают строки:

- **Длительность стала часами-минутами-секундами** (`01:06:33` вместо
  «1 ч 6 мин слушать»). Так требует §10 дизайн-системы и так во всех макетах.
  Время чтения осталось словами — «12 мин чтения»: спутать их нельзя, тип
  материала стоит в той же строке. **На странице выпуска строка пока прежняя**,
  она переверстывается в части 5. Оба написания считает одна функция
  (`src/lib/postTiming.mjs`), разъехаться им неоткуда.
- **Номер выпуска берётся из имени файла** — `ep-135` → `№135`. Отдельного
  поля с номером в данных нет, а имя не выдумано: робот сверки составляет его
  из адреса выпуска на Mave. У выпуска с запасным именем (`ep-a1b2c3d4`) номер
  просто не выводится — придуманный номер хуже отсутствующего.
- **Подпись берётся у РАЗДЕЛА, а не у категории.** У бонуса написано
  «Подкаст», потому что бонус и есть выпуск; что он платный, говорит плашка
  «Бонус» рядом. Это единственное, что предупредит о подписке до перехода.
- **И берётся подпись ОДНОГО материала, а не название раздела.** У категории
  теперь две подписи: `label` — название раздела («Статьи», «Заметки»),
  `labelOne` — название одной вещи («Статья», «Заметка»). Множественное число
  осталось во вкладках ленты, заголовком раздела, в ссылке «← назад»
  со страницы материала и значением фильтра в поиске; единственное стоит
  ровно в одном месте — в метастроке карточки. Обе написаны у всех пяти
  категорий явно, а не выведены запасным правилом: молчаливое правило однажды
  подставит новой категории множественное число, и заметить это можно будет
  только глазами на карточке.

**Подводка обрывается на первом спойлере.** Это делает `excerptFromBody` —
тот же код, что готовит описание страницы для поисковика, так что спрятанное
не попадёт ни в карточку, ни в выдачу, ни в ленту RSS. Абзац со спойлером
режется до его начала, всё, что ниже, не смотрится вовсе: за спойлером текст
идёт уже с оглядкой на него. Прогнано по всем 146 постам: описания у обоих
постов со спойлерами берутся из абзаца ВЫШЕ спойлера, а пост без описания
как был один (`ep-135`), так и остался.

**У выпусков в карточке появилась обложка.** Раньше её не было: поля `cover`
у выпусков архива нет, и карточка выходила голым текстом. Теперь берётся
квадратная обложка выпуска — своя сжатая копия из `public/episodes/`, та же,
что на странице выпуска. Правило «какая картинка в карточке» вынесено
в `src/lib/postCardMedia.mjs`, пропорции задаёт CSS по типу карточки.

**Заглушку не подставляем.** Материал без обложки остаётся текстовой карточкой:
ни пустого места, ни картинки-заменителя. Заглушка есть только у аниме, где
ряд держится на пропорции постера и дыра ломает сетку.

### Логика подбора материалов на главной

Считает `src/lib/homeSlots.mjs` — отдельным модулем намеренно, чтобы её можно
было прогнать обычным `node` на настоящем архиве и посмотреть глазами, что
попало в каждый слот, не поднимая сайт.

**Материал показывается на главной один раз.** Каждый следующий слот выбирает
из того, что ещё не занято. Без этого последнее видеоэссе встало бы и в первый
слот, и в сайдбар — они оба берут «самое свежее», просто с разными условиями.

**Что вышло на живых данных** (8 опубликованных материалов):

| Слот | Что попало |
|---|---|
| 1. Главный материал | `test-vremya-chteniya` — заметка |
| 2. Сайдбар, 3 карточки | `znakomtes-yodzi-takesige`, `ep-135`, `ep-134` |
| 3. Выпуск подкаста | `ep-106` |
| 4. Второй ряд справа | `test-zametka` |
| 5. Третий ряд слева | `ep-102` |
| 6. Короткие заметки | пусто — блока нет |
| «Из архива» | `ep-89`, одна карточка |

Живые данные сразу задели две развилки из ТЗ, и обе отработали:
**видеоэссе нет ни одного** — первый слот занял самый свежий материал любой
категории; **заметок на блок не осталось** — блок не выводится вовсе,
а материал третьего ряда занял всю ширину. Повторов нет ни одного,
все 8 материалов разошлись по слотам и подборке.

**Расхождение в самом ТЗ.** Раздел 3.2 говорит «всего главная показывает
9 материалов плюс до 4 заметок», но его же таблица слотов даёт 7 плюс
до 4 заметок, и ровно столько же в макете. Сделано по таблице и макету.

### Режимы фильтра

**Фильтры переносятся, а не скроллятся.** Горизонтальной карусели нет:
она прячет часть вкладок за краем, и о них не догадаться.

**Каждый режим — своя сетка**, а не одна сетка с переключением видимости:
подкасты по три в ряд с квадратными обложками, статьи и видео по два
с кадром 16:9, заметки по три.

**Как обойдена главная ловушка части** (§2.1 системы). В макете режим делал
две вещи сразу — показывал карточку и назначал ей `display: block`; четыре
селектора давали вес, который перебивал раскладку карточки, и она молча
не применялась. Здесь этой битвы нет по устройству: **у каждого режима своя
сетка, и прячется она атрибутом `hidden`.** Правила видимости карточек
не касаются вовсе, поэтому спорить с раскладкой нечему.

**На этом же месте нашлась настоящая ошибка, и не в макете, а в своём коде.**
`display: grid` у сетки перебивал атрибут `hidden` по весу — селектор правила
компонента длиннее. Разметка была правильная, атрибут стоял, а в браузере
все четыре сетки вывалились бы на страницу разом. Лечится `:not([hidden])`
в правиле раскладки; в файле про это стоит комментарий.

### Подборка «Из архива»

Кнопка «Показать ещё материалы» раскрывает 9 текстовых карточек и той же
кнопкой сворачивает обратно, меняя подпись на «Свернуть архив». Отдельной
кнопки «свернуть» рядом нет — это одна кнопка на том же месте (§8.1).
Третье состояние — «Все материалы показаны», `disabled`, когда раскрывать
нечего изначально.

**Подборка считается отдельно для каждого режима.** «Следующие после всех
показанных выше» — а выше в режиме «Подкасты» стоят совсем другие материалы,
чем в режиме «Все». Одним списком на все режимы половина подборки оказалась бы
повтором того, что уже на экране.

**Ссылку «Весь архив» не делали** — страницы `/archive/` ещё нет, вышла бы
битая ссылка. Она в части 4.

### Что ещё изменилось на главной

**Баннер и блок с обложкой подкаста и кнопками площадок убраны.** В утверждённом
макете `index.html` их нет: страница начинается с фильтров. Ссылки на площадки
остались в подвале. Файлы картинок лежат в репозитории — вернуть блок это
несколько строк.

**Страница раздела** (`/category/…/`) пользуется той же лентой: вкладки там
обычные ссылки, сетка одна. Она станет отфильтрованным видом архива в части 4.

### Две правки по замечаниям заказчика

Найдены глазами на живом сайте, обе настоящие.

**1. Обложку выпуска обрезало, если он попадал в «широкий» слот.** Пропорции
картинки задавал СЛОТ, а не материал: в карточке ленты свои правила, в каждом
ряду главной свои. Пока в слот второго ряда не попал выпуск подкаста, это
работало. Попал — и квадратную обложку срезало до полосы 16:9.

Правило вынесено в отдельный компонент `PostMedia.astro` и живёт теперь
в одном месте: **обложка выпуска подкаста — всегда квадрат и всегда целиком,
`object-fit: contain`, в каком бы слоте выпуск ни оказался**; всё остальное —
кадр 16:9, его обрезать можно. Предел ширины квадрата 390 px (§10.1: на главной
≤390, в архиве ≤420 — одно число подходит обоим, двух на одно правило
не заводим).

Слот теперь решает только, сколько дать картинке места. В стилях страниц
не осталось ни одного `aspect-ratio` и ни одного `object-fit` для картинок
материалов — появятся снова, и правило снова расползётся по слотам.

**2. В подводке карточки печатался код разметки тайтла.** `:anime[Фрирен]{id="…"
source="shikimori" source-id="52991"}` уезжал в карточку как есть. И не только
в карточку: та же функция готовит описание страницы для поисковика, превью
ссылки в мессенджере и ленту RSS.

Причина ровно та, от которой предостерегает `CLAUDE.md`: снятие markdown было
написано ДВАЖДЫ. Счётчик времени чтения метку тайтла снимать умел, подводка —
нет, и копии разъехались молча. Обе переведены на общий `src/lib/plainText.mjs`.

Прогнано по всем 147 постам: подводок с остатками разметки было две, стало ноль;
**время чтения не изменилось ни у одного поста** — общий код считает ровно
то же, что считал прежний.

### Ещё две правки по замечаниям заказчика

**3. В сайдбаре не хватало подводки.** В макете её там не было, и на живых
материалах это стало видно: под коротким заголовком оставалась пустая колонка.
Добавлена — та же подводка и та же обрезка тремя строками, что у остальных
карточек. Своего размера и своего цвета сайдбар не заводит.

**4. Две линии подряд на узком экране.** Прямое нарушение §5 системы: между
двумя соседними секциями всегда одна линия. Нашлось в двух местах, и причина
у обоих одна — линию рисовали и контейнер, и первый элемент внутри него.

- **Сайдбар главной.** Правило «у первой карточки линии сверху нет» стояло
  только в десктопном медиазапросе. На узком экране сайдбар отделяется своей
  линией от главного материала, и первая карточка добавляла вторую. Правило
  перенесено в базу — оно нужно на всех ширинах.
- **Сетки режимов фильтра.** Панель фильтров закрывается линией снизу, первая
  карточка открывается своей сверху. Снято до 761 px, где сетка одноколонная.
  Выше не трогаем: там в первом ряду две-три карточки, их линии идут в ряд —
  как и задумано, и снять её у одной из трёх значило бы сломать ряд.

**5. Многоточие посреди подводки.** У главного материала подводка обрывалась
на «Связан…» в середине фразы, а текст ниже продолжался как ни в чём не бывало.

CSS при этом был правильный: `-webkit-line-clamp: 3` и `overflow: hidden`
на месте. Дело в том, что подводка главного материала — **ячейка сетки**,
а ячейка по умолчанию растягивается на высоту всего ряда. Ряд высокий (рядом
заголовок), значит и коробка высокая, значит обрезать нечего — а многоточие
ограничение всё равно ставит на третьей строке. Лечится `align-self: start`:
коробка становится ровно по своим трём строкам. В обычном блоке это свойство
не меняет ничего, поэтому оно же поставлено и в карточке — на будущее.

**Следствие, заметное глазами: у главного материала подводка стала ровно три
строки** вместо шести. В утверждённом макете она не обрезалась вовсе — если
хочется как там, это одна строка правки.

## Что проверено и чем — часть 3

- **Сборка проходит**, 22 страницы, ошибок нет.
- **Логика слотов прогнана в node на живых данных** — таблица выше. Проверено,
  что повторов нет и что обе развилки (нет видеоэссе, нет заметок) отработали.
- **Обрезка подводки прогнана по всем 146 постам**, а не на паре примеров.
  Сверено с поведением ДО правки: пост без описания как был один, так и остался.
- **Разметка собранной главной разобрана целиком**: метастроки, фильтры,
  четыре скрытые сетки режимов, кнопка, пять списков «Из архива».
- **Правило скрытия проверено в собранном CSS** — `.archive-grid:not([hidden])`,
  `[hidden]{display:none}`.
- **Плашка «Бонус», пост-ссылка на чужой сайт и заметка с обложкой проверены
  временной подменой трёх постов**: в живых данных таких материалов нет ни
  одного. Все три отработали — `[ Подкаст ] · Бонус`, `[ Статьи ] · Кинопоиск`
  со значком ↗ и открытием в новой вкладке, миниатюра сбоку у заметки.
  Посты возвращены как были, изменений в `src/content/posts/` не осталось.
- **Страница тайтла не сломана** — карточка упоминания на месте, строка
  с таймкодами внутри неё тоже.
- **Страница раздела** — пять вкладок-ссылок, активная помечена
  `aria-current="page"`, кнопок фильтра нет.

## Чего НЕ проверено — часть 3

Всё, что живёт в браузере. Ни одна страница глазами не открывалась.

- **Как выглядит новая главная.** Это самое заметное изменение сессии.
- **Переключение пяти режимов фильтра** — меняется ли сетка, а не только
  видимость; не вываливаются ли несколько сеток разом.
- **Кнопка «Показать ещё материалы»** — раскрывает подборку, той же кнопкой
  сворачивает, подпись меняется. Сейчас в подборке одна карточка.
- **Фильтры на узком экране** — переносятся ли на вторую строку.
- **Три редакционных ряда на узком экране** — складываются ли в один поток.
- **Фокус с клавиатуры на карточках.** Рамки больше нет, обводка общая.
- **Не поехала ли вёрстка вбок** — `overflow-x: hidden` снят в части 1.

## Пять проверок раздела 8.4 — после части 3

| Проверка | Было после части 2 | Стало |
|---|---|---|
| Размеры шрифта меньше 11 px | пусто | пусто |
| Пометки принудительного приоритета | пусто | пусто |
| Видимость и раскладка в одном селекторе | пусто | пусто |
| `auto-fit` вместо `auto-fill` | 2 строки, `BonusPaywall.astro` | **без изменений** — ждёт части 10 |
| Цвета мимо переменных | 5 строк | **3 строки**, все в `posts/[slug].astro` |

Две строки из пяти были в карточке и починены вместе с ней: белая стрелка ▶
поверх превью стала бумажной с чернильной тенью, а полупрозрачная подложка
под ней ушла вместе с плиткой. Оставшиеся три живут на странице материала —
это часть 5, и в этой сессии её трогать было нельзя.

**Осторожно с четвёртой проверкой.** Она ищет всё, что похоже на
`#0a1b2c`, — и числовая запись пробела `&#8201;` попадала под неё как цвет.
Сейчас в разметке стоит именная запись `&thinsp;`, и ложных срабатываний нет.

## Цифры — часть 3

- Файлов в сборке: **593 из 20 000**. Само переоформление их не добавляло —
  наоборот, убрало четыре вместе с баннером и обложкой (556 → 552). Прирост
  до 593 дали новые опубликованные материалы, которые заказчик завёл в админке
  в тот же день: постов на сайте стало 11 вместо 8, страниц 33 вместо 22.
- Вес сборки 38 МБ.
- Ссылок на старую шкалу отступов: **110 в 11 файлах** (было 127 в 16).
  В тронутых этой сессией файлах не осталось ни одной.

## Что сделано — часть 4, 9 августа 2026

### Страница `/archive/`

Всё, чего не должно быть на главной: полный список, переключение по типу,
листание страницами. Главная осталась витриной, которая не растёт.

**Показывается текстовой сеткой без обложек**, и причина не в экономии файлов.
Разные форматы дают разную геометрию медиа: квадрат у выпуска, кадр 16:9
у видео, заметка вообще без картинки. В общем ряду это читается рвано, и даже
разделительная линия рвётся на сегменты по зазорам сетки. Без обложек ряды
ровные, в экран помещается втрое больше, а тип считывается по технической
строке — она и так первая в карточке.

**Карточка та же самая, второго компонента не заведено** — это вариант `text`
у `PostCard.astro`, тот же, которым выложена подборка «Из архива» на главной.

**Шапка архива** — надзаголовок, заголовок и подводка со строкой счёта:
«6 выпусков · 1 видеоэссе · 4 заметки». Разделы с нулём в строку не попадают:
«0 статей» — это не сведения, а шум. Числительное согласуется
(`src/lib/plural.mjs`).

**Вкладки — обычные ссылки на разделы**, у каждого типа свой адрес. Своей линии
снизу у них нет: её рисует верхняя граница первого ряда карточек, иначе вышли
бы две линии в тридцати пикселях друг от друга (§5).

### Разделы стали отфильтрованным архивом

`/category/{type}/` рисует **тот же самый компонент**, что и `/archive/`, —
`ArchiveView.astro`. Отдельной вёрстки у раздела нет.

**Вторая ветка `PostFeed` убрана целиком.** До этой сессии тот же компонент умел
работать и лентой раздела: те же вкладки, но ссылками, и одна сетка с обложками.
Оставь мы её «на всякий случай» — в проекте было бы два места, где верстается
один и тот же список, и они разошлись бы молча.

Адреса: `/category/podcast/`, дальше `/category/podcast/2/`.
Страница `src/pages/category/[category].astro` заменена
на `src/pages/category/[category]/[...page].astro`.

### Пагинация

**По 24 материала на страницу.** 143 выпуска — это шесть страниц (проверено
счётом, не на глаз).

**Страницами, а не кнопкой «показать ещё»**, и это не вкусовое: на страницу
можно дать ссылку, вернуться кнопкой «назад» и найти её поисковиком. После
шести нажатий кнопки адрес остался бы тем же, а при возврате из материала
лента свернулась бы обратно.

Три решения внутри:

- **Первая страница живёт по короткому адресу**: `/archive/`, а не `/archive/1/`.
  Второго адреса у одной и той же страницы быть не должно — для поисковика
  это дубль.
- **Номер страницы стоит в заголовке**: «Архив, страница 2 — Бака!». Без него
  у шести страниц архива одинаковый `<title>`, и поисковик считает их копиями
  друг друга.
- **Неактивный край — не ссылка, а `<span>`.** Выключенная ссылка остаётся
  ссылкой: по ней можно кликнуть и попасть на неё табом, а `aria-disabled`
  только сообщает диктору, что делать этого не стоит.

Какие номера показывать, считает `pageNumbers` в `src/lib/archive.mjs`: первая,
последняя и окно из трёх вокруг текущей, пропуски — многоточием. Окно прижимается
к краю, поэтому страница 1 из 11 даёт `1 2 3 … 11`, как в макете и в справочнике
состояний.

**Ни строчки скрипта.** В этом весь смысл пагинации: появись здесь JS,
подменяющий содержимое без смены адреса, — и ссылка, «назад» и поисковик
отвалились бы разом.

### Ссылка «Весь архив» на главной

Стоит **в конце раскрытой подборки «Из архива»**, а не рядом с кнопкой
«Показать ещё». Кнопка разворачивает следующие девять материалов тут же, ссылка
уводит на другую страницу; стой они рядом, пришлось бы выбирать между двумя
похожими действиями, не понимая разницы. В конце подборки ссылка отвечает
на уже возникший вопрос — «а дальше?».

**Ссылка одна на все типы**: подпись и адрес меняются по текущему режиму
фильтра — «Весь архив» в режиме «Все», «Все подкасты в архиве» в режиме
подкастов. Пять отдельных ссылок в пять разделов пришлось бы читать и выбирать,
а нужна из них всегда ровно одна.

**Строки висят на самих вкладках** (`data-archive-label`, `data-archive-href`),
а не лежат отдельным списком в скрипте: они собраны на сервере из списка
категорий, и второго места, которое надо держать с ним в согласии, не появилось.

**Компонент кнопки общий, и он бывает и кнопкой, и ссылкой.** У ссылки явно
сняты подчёркивание и цвет ссылки — иначе два элемента одного вида разъехались бы.

### Что переехало в общий код

- **Сетка текстовых карточек** — из `PostFeed.astro` в `src/styles/global.css`.
  Ею выложены и подборка на главной, и весь архив, и разделы: трёх копий одной
  сетки быть не должно, они разошлись бы сначала числом колонок, потом
  вертикальными линиями.
- **Русское числительное** — `src/lib/plural.mjs`. Такая же функция была написана
  внутри карточки тайтла («упоминание / упоминания / упоминаний»), копия убрана.
  Правило одно, а копии разъезжаются молча — на числах вроде 111 и 114, которых
  в тестовых данных не бывает никогда.
- **Две новые подписи у категории** в `content.config.ts`: `countForms` (три формы
  для числа: «143 выпуска») и `labelMany` («подкасты» для ссылки в архив).
  Ни ту, ни другую из `label` не вывести: раздел подкаста называется «Подкаст»,
  в единственном числе. Все четыре подписи лежат рядом, у каждой ровно одно
  место применения.

### Карта сайта

**Страницы листания в неё попали.** Заявить одну первую значило бы спрятать
от поисковика весь архив, кроме свежих 24 материалов. Сколько страниц выходит,
считает та же функция, что нарезает сами страницы, — иначе карта однажды
пообещала бы страницу, которой нет.

В архив, в отличие от списка постов, попадают и посты-ссылки на чужие сайты:
своей страницы у них нет, но в ленте они стоят и на число страниц влияют.

### Четыре правки по замечаниям заказчика

Все четыре найдены глазами на живом сайте, все четыре настоящие.

**1. Сетка архива ехала в одну колонку на любой ширине** — и на главной,
в подборке «Из архива», и на самой странице архива, и в разделах.

Причина — вес селектора, и ровно тот же, что описан в `CLAUDE.md` про атрибут
`hidden`. Базовое правило написано как `.list-grid:not([hidden])`, а число
колонок в медиазапросах — как голый `.list-grid`. Первый весит больше, поэтому
его `grid-template-columns: 1fr` побеждал и на 621, и на 921. При этом
вертикальные линии между колонками применялись — у них селектор длиннее, —
и каждая вторая и третья карточка отодвигалась вправо с линией слева.
Выглядело как сломанная вёрстка, а разметка была правильная.

**Дефект жил с части 3**, а не появился с архивом: в старом коде сетка лежала
в стилях `PostFeed.astro`, и после расстановки меток Astro формы селекторов
были те же. Просто подборку «Из архива» никто не раскрывал.

Лечится одинаковой формой селектора во всех трёх правилах сетки. Проверено
не глазами: разбор собранного CSS считает вес всех правил `grid-template-columns`
и говорит, какое побеждает на каждой ширине — 400 px даёт одну колонку,
700 две, 1200 три.

**2. Шапка раздела меняла высоту при переключении вкладок**, и список под ней
прыгал. Две причины, обе убраны:

- **подводкой раздела стояло описание для поисковика.** Это разные тексты
  для разных читателей: `description` уходит в метатег и в выдачу (до 160 знаков),
  а на странице человек читает подводку. Длина у описаний разная — от 62 знаков
  до 100, — поэтому шапка выходила то на две строки, то на три. Заведено
  отдельное поле `intro`, у всех разделов в диапазоне 75–83 знака. **Диапазон
  55–95 знаков — часть вёрстки**, про это написано прямо в `content.config.ts`:
  в нём подводка занимает две строки на широком экране и три на узком у любого
  раздела. Описания для поисковика при этом не тронуты;
- **у пустого раздела пропадала строка счёта** — и шапка становилась на строку
  ниже соседних. Теперь блок выводится всегда, у пустого раздела он пустой,
  но место под одну строку технического слоя занимает.

**3. Шапка сайта съезжала вбок на короткой странице.** Раздел «Статьи» пуст,
страница не прокручивается, браузер убирает полосу прокрутки — и окно
становится шире на её ширину. Оболочка `.site` центрируется по окну, поэтому
логотип, меню и весь текст сдвигались. Лечится `scrollbar-gutter: stable`:
место под полосу занято всегда, даже когда прокручивать нечего.

**4. Подводка главного материала выравнивалась по метастроке, а не по заголовку.**
Заголовок и метастрока лежали в общей обёртке, а подводка была вторым элементом
сетки — и её первая строка вставала вровень с `[ ВИДЕОЭССЕ ] · 12:44`, то есть
на строку технического слоя выше заголовка. В макете так же, но там заголовок
в одну строку, и перекос не читался; на двухстрочном видно сразу.

Обёртка убрана: метастрока, заголовок и подводка лежат в сетке по отдельности,
метастрока одна в первом ряду, заголовок и подводка — вторым рядом бок о бок.
Верх подводки теперь совпадает с верхом заголовка. На узком экране всё
по-прежнему в один поток, расстояния те же.

Общего `row-gap` у сетки нет намеренно: метастрока и заголовок несут свои
отступы снизу, и зазор сетки сложился бы с ними, раздвинув то, что должно
стоять вплотную.

## Что проверено и чем — часть 4

- **Листание проверено на настоящем листании.** Порог временно снижен до трёх
  материалов на страницу, сайт собран, разобрана разметка всех четырёх страниц
  архива и обеих страниц раздела; порог возвращён на 24 и сайт собран заново.
  Иначе механизм остался бы непроверенным до дня публикации архива: на восьми
  материалах пагинации просто не на чем появиться.
- **Ряд номеров прогнан в node по всем позициям**, а не на паре примеров:
  для 11 страниц и для 6, с первой по последнюю. Совпадает с макетом
  (`1 2 3 … 11`) и со справочником состояний (`1 2 3 … 6` на второй странице).
- **Карта сайта сверена с собранными файлами в обе стороны**: у всех 37 адресов
  карты нашёлся файл в `dist/`, а из собранных страниц вне карты остались
  только `/admin/` и `/search/` — обе там быть и не должны.
- **Пустой раздел не пропал.** Опубликованных статей нет ни одной, и
  `/category/article/` собирается: шапка, вкладки, «Пока нет материалов»,
  без строки счёта и без пагинации.
- **Раздел «Подкаст» собирает и бонусы** — счёт идёт по списку категорий
  раздела, а не по одной.
- **Подписи ссылки в архив проверены у всех пяти режимов** в собранной
  разметке главной: «Весь архив», «Все подкасты в архиве», «Все видеоэссе
  в архиве», «Все заметки в архиве», «Все статьи в архиве».
- **Заголовки страниц листания различаются** — «Архив», «Архив, страница 2»;
  canonical у каждой свой.
- **Правило узкого экрана проверено в собранном CSS**: при `width<=620` номера
  и многоточие скрываются, «Страница 1 из 4» появляется. Селектор написан
  двумя классами намеренно — одиночный проиграл бы по весу правилу размера
  и не сработал бы ни в одну сторону.
- **Сборка проходит**, 33 страницы, ошибок нет.

## Чего НЕ проверено — часть 4

Всё, что живёт в браузере. Ни одна страница глазами не открывалась.

- **Как выглядит архив** — шапка, вкладки, текстовая сетка в три колонки
  с вертикальными линиями между ними.
- **Возврат из материала кнопкой «назад».** Механика для этого и выбрана:
  у каждой страницы свой адрес, ни строчки скрипта. Но переходы на сайте идут
  без перезагрузки (`ClientRouter`), и как поведёт себя история браузера
  на живом сайте — только руками.
- **Ряд листания на узком экране** — остаются ли края и «Страница 2 из 6».
  На сайте сейчас одна страница архива, и увидеть это можно будет,
  когда материалов станет больше 24.
- **Ссылка «Весь архив»** — раскрыть подборку на главной, долистать до конца.
  Подпись должна меняться при переключении вкладок.
- **Вкладки архива на узком экране** — переносятся ли на вторую строку.

## Пять проверок раздела 8.4 — после части 4

| Проверка | Было после части 3 | Стало |
|---|---|---|
| Размеры шрифта меньше 11 px | пусто | пусто |
| Пометки принудительного приоритета | пусто | пусто |
| Видимость и раскладка в одном селекторе | пусто | пусто |
| `auto-fit` вместо `auto-fill` | 2 строки, `BonusPaywall.astro` | **без изменений** — ждёт части 10 |
| Цвета мимо переменных | 3 строки, `posts/[slug].astro` | **без изменений** — ждут части 5 |

Ровно то, чего ждали: обе непустые проверки лежат в файлах, которые эта сессия
трогать не должна.

## Цифры — часть 4

- Файлов в сборке: **594 из 20 000** (было 593). Прибавилась одна страница —
  `/archive/`. Страниц листания сейчас нет ни одной: опубликовано 11 материалов
  при пороге 24, и всё умещается на первую страницу каждого вида.
- **Когда опубликуете весь архив, прибавится около 15 страниц:** шесть у архива
  (143 выпуска и остальное — по 24 на страницу), плюс по несколько у каждого
  из четырёх разделов. Запас по лимиту Cloudflare это не тронет.
- Вес сборки 38 МБ.
- Ссылок на старую шкалу отступов: **108 в 10 файлах** (было 110 в 11).
  В тронутых этой сессией файлах не осталось ни одной.

## Что сделано — часть 5, 9 августа 2026

### Пять раскладок в одном файле

`src/pages/posts/[slug].astro` рисует статью, заметку, видеоэссе, выпуск
подкаста и бонус. **Разводятся не пять веток, а две:**

| Ветка | Кто | Что общего |
|---|---|---|
| «Выпуск» | подкаст, бонус | шапка с обложкой 1:1, кикер, H1 |
| «Текст» | статья, заметка, видеоэссе | общий заголовочный блок на оси 700 |

Всё остальное — наличие или отсутствие блока, а не своя вёрстка. Пять копий
одной страницы в одном файле разошлись бы так же, как разошлись бы пять файлов.

### Заголовочный блок статьи, заметки и видеоэссе — один компонент

`src/components/ContentHeader.astro`. Ось 700, метастрока, H1, отступы —
одни и те же; отличается ровно одна вещь, необязательная подводка, и та
приходит свойством.

**Проверено разбором собранных страниц:** структура и все классы
заголовочного блока у заметки и у видеоэссе совпадают знак в знак,
метка области стилей у обеих одна (`data-astro-cid-zfycg6tt`) — то есть
и правила CSS у них физически одни. Различаются только адрес ссылки
«← назад» и дата.

### Шапка выпуска

Обложка квадратная и **не обрезается** (`object-fit: contain`), заголовок,
плеер. Ширина обложки 340 → 300 → 360 по брейкпоинтам, как в §13.

**Фиксированной высоты у текстовой колонки нет.** Плеер прижимается к низу
через `margin-top: auto` внутри флекса, высоту ряду задаёт обложка. Поставь
там `height: 360px` — длинный заголовок вылез бы за край, короткий оставил
бы дыру.

**Длительность на странице выпуска названа ровно дважды:** в верхней строке
рядом с датой и внутри плеера. Из кикера она убрана — у `PostKicker.astro`
появилось свойство `withTiming`, карточка в ленте зовёт его по-прежнему
с длительностью. Раньше число стояло в трёх местах подряд, и ни одно
из них не было главным.

**Подводки в hero выпуска нет вовсе.** У бонуса она есть — так в макете
`bonus.html`, и это не противоречие: у бонуса нет плеера, и блок иначе
кончался бы заголовком.

### Обложка выпуска ушла из текста

Робот сверки вставляет обложку первой строкой каждого выпуска — 141 файл.
Теперь она стоит в шапке, и в тексте оказалась бы второй копией подряд.

Убирается при сборке (`src/plugins/remark-episode-cover.mjs`), **тела постов
не правятся**: строка остаётся в файле и видна в админке. Перестанет шапка
показывать обложку — картинка вернётся в текст сама.

### Медиа трёх ширин

Контейнер текста широкий (1240), а колонка чтения задаётся детям. Отсюда
три ширины §12 без единого расчёта от ширины окна:

| Ширина | Что попадает |
|---|---|
| 700 | абзацы, списки, цитаты, одиночная картинка |
| 1000 | галерея, ролик YouTube, своё видео, транскрипт |
| 1240 | картинка «во всю ширину» |

**Внутри `.content` нигде не пишется сокращённый `margin`** — только
`margin-block`. Сокращённый сбросил бы `margin-inline: auto`, и весь текст
уехал бы к левому краю контейнера в 1240. Про это в файле стоит комментарий.

**До краёв экрана на телефоне медиа уходит от полей страницы, а не от `100vw`.**
Ширина окна включает место под полосу прокрутки (`scrollbar-gutter: stable`
из части 4), поэтому `100vw` вылезает за край документа и даёт тот самый
горизонтальный скролл, который на сайте запрещено прятать.

### Нумерация иллюстраций

`FIG. 01 — подпись`. Считается при сборке
(`src/plugins/remark-image-figure.mjs`), автор ничего не проставляет
и проставить не может: номер зависит от того, сколько картинок выше,
и сдвинулся бы от любой вставки.

Четыре развилки раздела 5.1 ТЗ:

- **номер получают только подписанные иллюстрации**, неподписанные в счёте
  не участвуют. Иначе в нумерации были бы дыры — `FIG. 01`, потом `FIG. 03`, —
  и читатель принял бы это за ошибку вёрстки: он не знает, сколько картинок
  пропущено;
- **нумерация появляется только с двух подписанных.** Единственной картинке
  в заметке номер не нужен: ссылаться в тексте не на что;
- **галерея занимает столько номеров, сколько в ней картинок**, и подписывается
  диапазоном. Общей подписи у галереи пока нет — она в части 9; счётчик
  прибавляет `images` целиком, а не по единице, и к этому уже готов;
- формат: ведущий ноль до `FIG. 09`, дальше без него; в диапазоне короткое
  тире (U+2013), проверено по коду знака.

### Блок «Упоминается» и горизонтальная марка

`AnimeBadge.astro` переписан в каноническую горизонтальную марку §11.3:
высота 168, постер 112×168 слева вплотную, перфорация по всем четырём
сторонам, `contain` — постер не обрезается никогда.

**Сетка на `auto-fill`.** Одинокая марка — самый частый случай (у выпуска
про один тайтл она ровно одна), и со схлопывающим вариантом она растянулась
бы на всю ширину блока.

**Заголовок блока одинаковый на всех четырёх страницах** — «Упоминается
в материале». В макетах он разный («в выпуске», «в видеоэссе»); это два
текста об одном и том же, и разойтись им проще, чем кажется.

**Цвет марки выбирается по имени тайтла**, а не по месту в ряду: ряд должен
быть разноцветным, но «Фрирен» обязан выглядеть одинаково везде, где
попадается. Четыре пастельных значения лежат в `tokens.css` — §3.3 разрешает
их прямым текстом, но только у марок и только через `--stamp`.

**Число фрагментов в подписи** («10 фрагментов») берётся из расшифровки тем же
кодом, что считает упоминания на странице тайтла: `animeMentionedIn` теперь
отдаёт таймкоды, а не только список.

### Соседние материалы

Один блок на все пять раскладок, ссылки — внутри РАЗДЕЛА: со страницы выпуска
логично попасть в соседний выпуск, а не в заметку, вышедшую в тот же день.
Раздел тот же, куда ведёт «← назад», поэтому бонус ходит по выпускам.
Внешние посты в соседи не попадают — у них нет страницы.

**Подписи одинаковые у всех пяти** — «Предыдущий / Следующий материал».
В макетах они разные («выпуск», «видеоэссе»), но пятая подпись у категории —
это пятая строка, которую придётся держать в согласии с остальными, а куда
ведёт ссылка, читателю говорит её собственный заголовок.

### Ещё две правки по ходу

- **«19 мин смотреть» у видеоэссе** вместо «слушать» — как в макете
  `video.html`. Число то же, из RSS; глагол нужен только на странице
  материала, в метастроке карточки стоит `12:44`.
- **Кнопки поста стали списком с линиями** вместо ряда плашек: плиток
  в языке сайта нет. Слева площадка, справа адрес.

### Три цвета мимо палитры закрыты

Стрелки карусели были белым знаком на полупрозрачной чёрной подложке —
расчёт на тёмную тему. На бумаге такая кнопка исчезла бы на светлом кадре;
стала обычным контролом системы: бумага, чернила, граница контрола, 44×44.
Подложка лайтбокса стала чернильной: полупрозрачных подложек в системе нет.

**Старые значения в комментариях не записаны намеренно.** Проверка «цвета
мимо переменных» из раздела 8.4 находит их в комментарии и с этого дня врёт —
ровно та ловушка, из-за которой `&#8201;` пришлось менять на `&thinsp;`.

## Что проверено и чем — часть 5

- **Сборка проходит**, 33 страницы, ошибок нет.
- **Заголовочный блок статьи и видеоэссе совпадает** — разбором собранных
  страниц: одинаковая структура, одинаковые классы, одна метка области стилей.
- **Вес правил разобран по собранному CSS, а не глазами.** Скрипт считает вес
  всех правил свойства и говорит, какое побеждает на 320, 620, 700, 900
  и 1200 px. Проверены три ширины медиа, сетка марок, сетка шапки выпуска,
  ряд соседних материалов и прижатие плеера к низу колонки — везде побеждает
  то, что задумано.
- **Нумерация прогнана пятью случаями в node**, а не на одном примере: одна
  подписанная (номеров нет), две одиночные, неподписанная между ними (дыры
  нет), одиночная плюс галерея из четырёх (`FIG. 02–05`), переход через
  девятый номер (`FIG. 09–11`, дальше без ведущего нуля). Функция для этого
  экспортируется — случая с галереей в живых данных пока не бывает.
- **Нумерация проверена и на живом посте временной подменой:** в заметку
  добавлены две подписанные картинки, собрано, получилось `FIG. 01`, `FIG. 02`,
  `FIG. 03`; галереи между ними номеров не заняли, как и должны.
- **Бонус проверен временной подменой** `ep-134`: шапка с обложкой, кикер
  `[ Подкаст ] · Бонус`, подводка, плеера нет, плашка подписки на месте.
- **Внешний пост проверен временной подменой** `test-zametka`: страница
  не создалась вовсе (`dist/posts/test-zametka` отсутствует), в карте сайта
  его нет, страниц в сборке стало 32 вместо 33.
- **Обложка выпуска в тексте больше не дублируется** — проверено в собранных
  страницах `ep-135` и `ep-134`.
- **Все посты возвращены как были**, `git status` по `src/content/posts/` чист.
- **Поисковый индекс собирается** — 34 страницы. Блоки марок и соседних
  материалов помечены `data-pagefind-ignore`: это навигация, и в выдаче
  она была бы шумом.

## Чего НЕ проверено — часть 5

Всё, что живёт в браузере. Ни одна страница глазами не открывалась.

- **Как выглядят все пять раскладок.** Это самое большое изменение переноса.
- **Плеер и транскрипт остались старыми** — они в части 6. Страница выглядит
  смешанно: новая раскладка и старый плеер внутри неё. Так и задумано.
- **Прижат ли плеер к низу колонки** на широком экране и не рвёт ли шапку
  длинный заголовок.
- **Медиа до краёв экрана на телефоне** — не вылезает ли что-то вбок.
- **Стрелки карусели** на светлом кадре и лайтбокс на чернильной подложке.
- **Марки упоминаний**: одинокая не должна растягиваться, постер не должен
  обрезаться, перфорация должна быть со всех четырёх сторон.
- **Галерея не трогалась** — она в части 9, и сейчас выглядит по-старому.

## Пять проверок раздела 8.4 — после части 5

| Проверка | Было после части 4 | Стало |
|---|---|---|
| Размеры шрифта меньше 11 px | пусто | пусто |
| Пометки принудительного приоритета | пусто | пусто |
| Видимость и раскладка в одном селекторе | пусто | пусто |
| `auto-fit` вместо `auto-fill` | 2 строки, `BonusPaywall.astro` | **без изменений** — ждёт части 10 |
| Цвета мимо переменных | 3 строки, `posts/[slug].astro` | **пусто** |

Ровно то, чего ждали: осталась одна непустая проверка, и она в файле части 10.

## Цифры — часть 5

- Файлов в сборке: **594 из 20 000** — без изменений. Переоформление страницы
  файлов не добавляет.
- Вес сборки 38 МБ.
- Ссылок на старую шкалу отступов: **74 в 8 файлах** (было 108 в 10).
  В тронутых этой сессией файлах не осталось ни одной. Остались:
  `EpisodePlayer`, `PlayerBar`, `Transcript` (часть 6), `BonusPaywall`
  (часть 10), `anime/[slug]`, `anime/index` (часть 7), `search` (часть 8),
  `about` (часть 10).

## Четыре правки по замечаниям заказчика — часть 5

Все найдены глазами на живом сайте, все настоящие.

**1. Постер занимал не всю высоту марки** — у «Сказаний Земноморья» правильно,
у «Наруто» нет.

Причина в данных, а не в вёрстке, и она отменяет утверждение самой дизайн-системы.
§9 говорит: «постеры в справочнике хранятся строго 2:3 — Shikimori и AniList
отдают такие», и из этого выведена ширина колонки 112 (168 × ⅔). **Замер всего
справочника показал другое:** Shikimori и AniList отдают 225×318, то есть 0.708;
ровно 2:3 оказался один постер из одиннадцати, разброс 0.666–0.717. В рамке
112×168 такой постер не дотягивал до верха и низа на 5–12 px, у каждой марки
по-своему, — ряд читался неряшливо.

Обрезать нельзя: это самый настойчиво повторённый запрет во всей системе.
Поэтому **колонка постера стала 120 = 168 × 0.717**, то есть шириной под самый
широкий постер справочника. Остаток в пару пикселей уходит вправо, к тексту,
где читается полем; постер стоит вплотную к левому краю (`object-position: left`).

**Проверено прогоном по всем одиннадцати постерам:** высоту марки занимают все,
десять на 100 %, «Наруто» на 99.7 % (полпикселя).

**2. Ссылка «← назад» и дата стояли на разной высоте.** Метастрока
выравнивалась `align-items: center`, то есть по КОРОБКАМ. Стоит их высотам
разойтись на пиксель — от другой гарнитуры у стрелки, от округления строки, —
и текст встаёт на разной высоте, хотя размер и кегль одинаковые. Выглядит как
съехавшая вёрстка, а причина невидима.

Стало `align-items: baseline` — по линии букв, как человек и читает.
Исправлено в обоих местах сразу: и в заголовочном блоке статьи, и в верхней
строке выпуска.

**3. При длинном названии плеер прилипал к заголовку.** `margin-top: auto`
прижимает плеер к низу колонки, но при трёхстрочном заголовке свободного места
не остаётся, auto схлопывается в ноль — и плеер встаёт вплотную к последней
строке.

Плеер убран в свою обёртку: она держит и прижатие (`margin-top: auto`),
и обязательный зазор (`padding-top`). **Отступ отдан padding'у намеренно** —
его auto-margin не съедает, поэтому зазор есть всегда, на любой длине названия.

**4. Стрелки рисовал системный шрифт.** Найдено по ходу третьей правки.
В `unicode-range` у Literata и Inter из стрелок стояли только ↑ и ↓, а сайт
рисует ещё `← → ↗ ▶` — 43 места в разметке. Диапазон решает, БРАТЬ ЛИ гарнитуру
для знака, ещё до того, как выяснится, что знак в ней нарисован, поэтому все
четыре молча уезжали в системный шрифт: чужая стрелка не того веса рядом
со своим текстом.

Список сверен с самими файлами шрифтов, а не взят на глаз: `↗` у Literata
не нарисован, поэтому в её диапазон он и не добавлен. Это ровно тот урок,
который уже записан в `CLAUDE.md` про `⏸` и `✕`, — только теперь он про знаки,
которые в гарнитурах есть, а до страницы не доезжают.

## Что сделано — часть 6, 9 августа 2026

### Плеер выпуска

Размеры из §13: play 54 круглый, таймлайн 3 с ручкой 14, ±15 визуально 36
без рамки и фона, скорость визуально 34. Тот же компонент стоит в блоке
«Аудиоверсия» у видеоэссе — второго плеера в проекте нет.

**Главное в этой части — цели нажатия, а не размеры.** Видимый размер меньше
44 у всех трёх контролов, и недостающее добирается невидимой зоной (§8):

| Контрол | Видно | Цель | Чем добрано |
|---|---|---|---|
| ±15 | 36 | 44 | псевдоэлемент `inset: -4px` |
| Скорость | плашка 34 | 44 | прозрачный `select` на всю ячейку поверх плашки |
| Таймлайн | полоса 3 | 44 | сам ползунок высотой 44, полоса внутри него |
| Таймкод в транскрипте | текст 11 | 44 | псевдоэлемент `inset: -15px -10px` |

**Ползунок остался настоящим `<input type="range">`**, а не собран из дивов,
как в макете. Своя ручка на дивах — это заново написанные стрелки клавиатуры,
роль для диктора, шаг и объявление позиции вслух; дизайну важен вид и размер
цели, а это достигается стилями. Проигранная часть красится градиентом
по переменной `--played`: у ползунка в вебките нет отдельного слоя заливки,
а два разных способа для двух браузеров разъехались бы.

**Три состояния** из справочника (блок 01):

- **пауза / идёт воспроизведение** — меняется только иконка, положение
  и размер нет;
- **загрузка** — по линии таймлайна ходит короткая заливка. Ложится ровно
  на линию: полоса в ползунке отцентрована по его высоте;
- **аудио не отдалось** — вместо плеера текст и ссылка на площадку. Случай
  не выдуманный: фид подкаста уже один раз не ответил.

**Иконки стали картинками, а не знаками.** Было `▶` и `⏸` текстом — а `⏸`
не нарисован ни в одной из трёх наших гарнитур и уезжал в системный шрифт.
Тот же случай был у крестика `✕` в панели.

### Мини-плеер

**Полоса прогресса стала верхней границей панели**, а не отдельной линией
над ней: своя линия плюс граница дали бы две линии подряд — то же правило,
что у секций (§5).

**На странице выпуска панель появляется, только когда основной плеер ушёл
за кромку экрана.** Следит `IntersectionObserver` за самим плеером, и он
заводится заново при каждом переходе между страницами: панель переход
переживает, а плеер — нет. До первого ответа наблюдателя плеер считается
видимым, иначе панель успевала бы мигнуть и спрятаться в момент нажатия play.

**До 621 px в панели остаются обложка, название, пауза и закрыть.** Перемотка
и скорость появляются выше: семь целей в полосе высотой 64 — это промахи
пальцем, а на самой странице выпуска они всё равно есть.

**Название в одну строку с обрезкой** — иначе панель растёт от длинных
заголовков, и вместе с ней прыгает вся страница под ней.

**Полоса прогресса показывает, но не управляет, и это решение.** Тащить
полосу в 3 px нельзя: цель обязана быть 44, а добрать её нечем — невидимая
зона накрыла бы название и кнопки под ней. Перемотка в панели — это ±15,
полноразмерный таймлайн есть там, где ему место: на странице выпуска.
В перечне того, что в панели есть (раздел 6.2 ТЗ), полосы среди органов
управления и нет. **Скажите, если хотите перемотку и здесь** — это осознанно
убранная возможность, а не забытая.

**Закрытие панели по-прежнему останавливает звук** — панель единственный
способ управлять воспроизведением на других страницах.

### Транскрипт

Весь текст в разметке, свёрнут только высотой окна. Три правила, и все три
легко нарушить:

- **окно прокручивается САМО.** Переход по таймкоду и «Следить за плеером»
  мотают его внутри, а не разворачивают: человек свернул окно намеренно;
- **прятать через `display: none` нельзя** — тогда поиск браузера (Ctrl+F)
  перестанет находить в свёрнутой части;
- **высота в `em`, а не в пикселях** (34em): при увеличении шрифта окно растёт
  вместе с текстом, и на зуме 200 % в нём видно несколько реплик, а не полторы
  строки.

`content-visibility: auto` снимает стоимость отрисовки нижних реплик,
не убирая их из документа.

**Из-за него пришлось поправить две вещи, и обе — про измерение высоты.**
Пока окно за кромкой экрана, его содержимое не разложено, и `scrollHeight`
в этот момент равен видимой высоте:

- проверка «свёрнут ли транскрипт» теперь **по классу, а не по измеренной
  высоте**. Иначе при переходе по ссылке дёргалась бы вся страница вместо
  прокрутки внутри окна;
- переходы по ссылке (со страницы тайтла и из поиска) **ждут кадра отрисовки**
  перед тем, как мотать окно. Два кадра, а не один: обработчики кадра
  выполняются до пересчёта раскладки.

**Таймкод без рамки** — подчёркнутый акцентный текст, цель 44 невидимой зоной.
Коробок в языке сайта нет, а в транскрипте их было бы по двести на страницу.

**Две колонки только от 821 px.** На телефоне спикер и таймкод стоят над
репликой, двух колонок там нет вовсе.

**Состояния:** транскрипта нет — блока нет вовсе (так было и раньше); спикер
не определён — так и написано в имени, курсивом и приглушённым, без
предупреждающих значков: это состояние данных, а не ошибка, которую читатель
может исправить; реплика без внутренних таймкодов идёт цельным текстом.

**Новое в шапке блока — строка «40:09 · 2 голоса».** Считается по самой
расшифровке, а не берётся из RSS: длительность звука и длина расшифровки —
разные числа, и подставить сюда чужое было бы неправдой.

### Общие контролы уехали в базу

`.play`, `.jump-btn`, `.speed-wrap` и `.speed-face` описаны в
`src/styles/global.css`, а не внутри плееров. Плееров два, контрол один:
два описания разъехались бы молча — сначала размером зоны нажатия,
потом цветом наведения.

## Что проверено и чем — часть 6

- **Сборка проходит**, 33 страницы, ошибок нет.
- **Вес правил разобран по собранному CSS**, а не глазами: скрипт считает вес
  всех правил свойства и говорит, какое побеждает на 360, 620, 700, 900
  и 1200 px. Проверено, что в панели кнопка play уменьшается до 44, что
  перемотка и скорость в ней прячутся до 621 и появляются выше, что у
  транскрипта две колонки только от 821, а метки колонок до 821 скрыты.
- **Состояние «аудио не отдалось» прячется атрибутом**, а не правилом display:
  проверено в собранном CSS, что у обёртки `.ep-live` своего правила display
  нет вовсе — значит `[hidden]` срабатывает без единого уточнения (§2.1).
- **Разметка разобрана в собранных страницах**: плеер выпуска с подписью
  «Бака! · выпуск №135», аудиоверсия видеоэссе с подписью «Бака! · аудиоверсия»,
  панель мини-плеера, шапка транскрипта со строкой «40:09 · 2 голоса», метки
  колонок, первый блок реплики, низ с кнопкой разворота.
- **Подсказка «Клик по таймкоду переносит плеер» появляется только там, где
  таймкоды есть** — она приходит свойством от страницы, а не написана
  в плеере.
- **Знаки сверены с гарнитурами:** `⏸` и `✕` не нарисованы ни в Literata,
  ни в Inter, ни в Plex Mono, поэтому иконки плеера и крестик панели стали
  картинками.

## Чего НЕ проверено — часть 6

Всё, что живёт в браузере, и в этой части этого больше, чем обычно: почти
вся часть — поведение.

- **Как выглядят плеер, панель и транскрипт.**
- **Появление панели на странице выпуска.** Прокрутить вниз — панель должна
  появиться, когда плеер уйдёт за верх экрана, и исчезнуть при возврате.
- **Звук не прерывается при переходе** на другую страницу и обратно.
- **Закрытие панели останавливает звук**, а не просто прячет кнопки.
- **Состояние загрузки** — видно ли бегущую полоску до начала звука.
- **Состояние «аудио не отдалось»** — увидеть его можно только при настоящем
  сбое сети, подменой данных оно не воспроизводится.
- **ПЕРЕХОДЫ ПО ССЫЛКЕ В ТРАНСКРИПТ — смотреть в первую очередь.** Из поиска
  по сайту и со страницы тайтла: окно транскрипта должно прокрутиться внутри
  себя к нужной реплике, а не дёрнуть всю страницу. Тут стоит `content-visibility`,
  из-за которого высота окна в момент перехода ещё не посчитана, и поправки
  сделаны вслепую.
- **«Следить за плеером»** на живом звуке, плавность автопрокрутки.
- **Зум 200 %** — в окне транскрипта должно оставаться видно не меньше трёх
  реплик.
- **Панель на телефоне** — остались ли только обложка, название, пауза
  и закрыть.

## Пять проверок раздела 8.4 — после части 6

| Проверка | Было после части 5 | Стало |
|---|---|---|
| Размеры шрифта меньше 11 px | пусто | пусто |
| Пометки принудительного приоритета | пусто | пусто |
| Видимость и раскладка в одном селекторе | пусто | пусто |
| `auto-fit` вместо `auto-fill` | 2 строки, `BonusPaywall.astro` | **без изменений** — ждёт части 10 |
| Цвета мимо переменных | пусто | пусто |

## Цифры — часть 6

- Файлов в сборке: **594 из 20 000** — без изменений.
- Ссылок на старую шкалу отступов: **47 в 5 файлах** (было 74 в 8).
  В тронутых этой сессией файлах не осталось ни одной. Остались:
  `BonusPaywall` (часть 10), `anime/[slug]` и `anime/index` (часть 7),
  `search` (часть 8), `about` (часть 10).

## Пять правок по замечаниям заказчика — часть 6

**1. Обложку выпуска вытягивало по высоте колонки.** При длинном заголовке
текстовая колонка перерастает обложку, ряд грида растягивает свои ячейки —
и заданная высота перебивала `aspect-ratio`: рамка становилась полосой,
а квадратная картинка болталась в ней по центру.

Растягиваться должна ТОЛЬКО текстовая колонка (иначе плееру нечего прижимать
к низу), поэтому обложке сказано `align-self: start`. На коротком заголовке
ничего не меняется.

**2. Плеер показывал «не удалось загрузить аудио», когда выпуск играл.**
Событие `error` у `<audio>` приходит не только когда всё пропало: браузер шлёт
его и на прерванную загрузку, и на неудачную первую попытку, после которой
спокойно берёт файл со второго раза.

Проверка стала придирчивой: ошибку признаём, только если сошлось всё сразу —
выпуск выбран, элемент правда в состоянии ошибки, ошибка не «прервано»,
и данных не набралось совсем. Плюс любое удачное событие (`loadedmetadata`,
`canplay`, `playing`) состояние снимает.

**3. Полосу прогресса в панели сделали активной.** В прошлой сессии она была
только указателем — из-за цели нажатия. Заказчик решил иначе, компромисс
записан прямо в файле: видимая полоса 3, прозрачный ползунок поверх неё
высотой 20. Полных 44 тут не добрать — невидимая зона такой высоты накрыла бы
название и кнопки под ней, то есть отняла бы две цели ради одной.
Полноразмерный таймлайн 44 остаётся на самой странице выпуска.

Ручка едет вместе с концом заливки (`::after` у заливки), а не считается
отдельно: разъезжаться нечему.

**4. Звучащая реплика подсвечивается всегда, а не только при слежении.**
Было так, что подсветка включалась вместе с кнопкой «Следить за плеером».
Это неверно: без подсветки, читая транскрипт под звук, невозможно понять,
где ты сейчас, — а это единственное, ради чего расшифровка стоит рядом
с плеером. **Слежение теперь управляет только автопрокруткой.**

Отмечается не одним цветом (§3.3): полоска слева, подложка под текстом
и акцентное имя. Под полоску у каждого блока занята пустая колонка в 12 px —
иначе при появлении полоска сдвигала бы текст.

**5. Перематывает весь блок, а не только плашка времени.** Читатель целится
в реплику, которую хочет услышать, а не в четыре цифры сбоку. Плашка осталась
кнопкой: она нужна тем, кто ходит с клавиатуры, и она же объясняет, что блок
кликабельный.

Две оговорки в обработчике: клик по ссылке на тайтл ведёт на страницу тайтла,
а не перематывает; и если в этот момент выделен текст — не перематываем вовсе,
иначе попытка скопировать цитату наказывалась бы прыжком плеера. Курсор-палец
появляется только когда плеер на странице есть.

### Ещё две правки: `hidden`, который не прятал

Обе — одни и те же грабли, и они уже описаны в `CLAUDE.md`: **атрибут `hidden`
весит один селектор, а правило компонента в Astro всегда длиннее**
(`.ep-error[data-astro-cid-…]`), поэтому `display` из компонента его перебивает.
Разметка правильная, атрибут на месте, ошибок нет — а элемент виден.

- **Плашка «не удалось загрузить аудио» висела всегда**, с самой загрузки
  страницы и без единого нажатия. Прошлая правка (придирчивая проверка ошибки)
  была не лишней, но лечила не то: плашку показывал не скрипт, а CSS.
- **Строка «Показаны все упоминания тайтла» висела всегда** — вместе с кнопкой
  возврата к обычному поиску, которому нечего было возвращать.

Обе лечатся `:not([hidden])` в самом правиле раскладки.

**Заведена проверка, чтобы это перестало находиться глазами.**
`node scripts/check-hidden.mjs` после сборки: разбирает собранный CSS, находит
в собранных страницах всё, помеченное `hidden`, и считает, перебивает ли его
какое-нибудь правило по весу. **Проверка проверена на живой ошибке:** со снятым
`:not([hidden])` она называет 7 страниц и виновное правило, с возвращённым —
молчит. Гонять вместе с пятью проверками раздела 8.4.

Это третий раз, когда те же грабли доходят до заказчика: до этого были сетки
режимов фильтра на главной (часть 3).

## Что сделано — часть 7, 9 августа 2026

### Вторая и последняя форма марки

`AnimeCard.astro` переписан в вертикальную каталожную марку §11.1: постер
сверху в рамке 2:3, текстовый блок снизу с `min-height` 144, перфорация
по всем четырём сторонам. Форм у марки теперь ровно две, и третьей не будет.

**Рамка здесь именно 2:3, в отличие от горизонтальной марки.** Там ширина
колонки посчитана под самый широкий реальный постер (0.717), потому что высота
марки фиксирована и постер обязан занять её целиком. Здесь высоту задаёт сама
рамка, и недостающие 5–12 px уходят в поля сверху и снизу: у соседних марок
текстовый блок всё равно начинается на одной высоте, и ряд читается ровно.
**Постер не обрезается ни в одной из двух форм.**

**Цвет марки уехал в общий `src/lib/animeStamp.mjs`.** Правило «цвет по имени
тайтла, а не по месту в ряду» нужно обеим формам, и второй копии у него быть
не должно: разъехавшись, они показали бы один тайтл двумя разными цветами
на двух страницах, и заметить это можно было бы только сравнив страницы глазами.

**Сетка каталога:** 6 колонок от 1101, 4 от 821, 3 от 621, ниже —
заполняющая от 140 px. Именно заполняющая: схлопывающий вариант растянул бы
два тайтла на всю строку.

### Каталог: поиск, сортировка, порции

**Панель под линией шапки, а не над ней.** Сначала читатель понимает, где он,
и только потом получает инструменты. Своей линии у панели нет — её рисует
шапка сверху.

**Поле поиска до 420 px**, не во всю ширину: запрос в каталог короткий,
а поле на 1300 пикселей выглядит как форма регистрации.

**Поиск идёт по русскому названию, оригинальному, студии и вариантам
написания.** «Е» и «Ё» — одна буква, тем же кодом, что и в поиске упоминаний.

**Тут нашлась настоящая дыра, и ровно в том месте, которое было названо в ТЗ.**
Студии и оригинальные названия хранятся латиницей — Shikimori других не отдаёт.
Запрос «гибли» не находил НИ ОДНОГО из трёх тайтлов Ghibli, хотя именно он
и назван в ТЗ самым вероятным запросом, не являющимся названием.

Лечится записью латинской части кириллицей в невидимое поле поиска
(`src/lib/translit.mjs`). Три решения внутри:

- **переводим данные, а не запрос.** «Гибли» → «gibli» не совпало бы
  с «ghibli»: по-русски «h» не пишется вовсе. В эту сторону правило работает,
  в обратную — нет;
- **записей две, с йотацией и без:** «Kyoto» по-русски пишут и «Кёто»,
  и «Киото». Обе уходят в поле поиска, выбирать одну «правильную» не из чего;
- **«й» = «и» только в каталоге.** В общее правило поиска эту замену добавлять
  нельзя: оно ищет упоминания тайтлов в живой речи по всему архиву, и «мой» =
  «мои» дало бы ложные упоминания сразу везде. В каталоге цена ошибки — одна
  лишняя марка в выдаче, зато «гайнакс» находит Gainax.

Проверено прогоном по собранной разметке: «гибли», «ghibli», «киото», «кёто»,
«kyoto», «гайнакс», «gainax», «наруто», «naruto», «кэйон», «k-on», «фрирен»,
«широбако», «унесенные» без ё — все находят то, что должны.

**Сортировка — тот же контрол, что скорость в плеере:** видимая плашка внутри
цели 44, нажатие ловит прозрачный родной `select`. Второго такого контрола
в проекте нет.

**Порядок добавления пришлось откуда-то взять.** Даты добавления у тайтла нет:
ни Shikimori, ни AniList такого поля не отдают, а заводит тайтлы робот
по меткам в тексте. Выдать за него алфавит — тихая ложь: номер `CAT. 001`
скакал бы у половины каталога от каждого нового тайтла. Завести поле в самой
карточке — риск потерять его при первом сохранении в админке: она переписывает
файл целиком и знает только те поля, что перечислены у неё в настройках.

Поэтому порядок собран **из истории git** (когда файл каждого тайтла впервые
попал в репозиторий) в отдельный список `src/data/animeOrder.js`, которого
админка не касается. **Чего в списке нет — встаёт в конец, по имени**, и это
не заглушка: новый тайтл всегда добавляется позже всех уже заведённых. Единственная
неточность — несколько тайтлов, заведённых между правками файла, встанут
по алфавиту, а не по минуте создания.

**Счётчик меняет вопрос:** без запроса «Всего: 13 тайтлов», с запросом
«Найдено: 3 тайтла». Числительное согласуется общим `plural.mjs`.

**Фильтров по параметрам нет намеренно** — так записано и в ТЗ, и в §16.1.

### Страница тайтла

**Заголовок и оригинальное название стоят на одной базовой линии**, техстрока
и ссылка на источник — на следующей. Это две строки ОБЩЕЙ сетки, а не два
столбика рядом: обёртки растворяются через `display: contents`, иначе каждая
пара живёт своей жизнью и выравнивать их нечем. По базовой линии, а не по
центру коробок: у заголовка и оригинала разные гарнитуры и кегли.

**Упоминания — обычные карточки материалов, второго компонента не заведено.**
Три колонки от 1101, две от 621, одна ниже. Минуты разговора живут внутри
карточки, в её `<slot />` — так было и раньше, потому что вложить ссылку
в ссылку нельзя.

**Таймкод без плашки:** подчёркнутый акцентный текст, цель 44 добирается
невидимой зоной. Коробок в языке сайта нет.

**После шести упоминаний — общая кнопка «Показать ещё».**

**Описание больше не показывает разметку Shikimori.** `[[Хокагэ]]` и
`[波風ミナト]` печатались на странице как есть. Чистит их одна функция —
та же, что готовит описание для поисковой выдачи. И описание разбивается
на абзацы: в данных они есть, а на странице шли одним куском.

**Все системные сообщения убраны.** Плашки «⚠ нет русского названия — впишите
titleRu в src/content/anime/…» на странице больше нет: это была записка самому
себе на видном месте у читателя. Нет описания — нет абзаца, нет упоминаний —
нет блока, нет русского названия — заголовком идёт оригинальное.

### Блок каталога на главной

Четыре горизонтальные марки внизу главной, под лентой. **Механизм
двухступенчатый:** при сборке в разметку кладётся 12 случайных марок,
в браузере показываются 4 из них — тоже случайные. Читатель видит новую
четвёрку при каждом заходе, а весит это двенадцать строчек текста: у скрытых
марок картинки помечены ленивой загрузкой и не запрашиваются вовсе.

**Без JavaScript показываются первые четыре** — блок не пустеет.

**Перемешиваем Фишером — Йетсом, а не сортировкой со случайным сравнением:**
та даёт заметный перекос к началу списка, и первые тайтлы справочника попадали
бы на главную чаще остальных. И при сборке, и в браузере.

**Из выборки исключаются тайтлы без единого упоминания:** перейдя по такому,
читатель попал бы на страницу, где нет ничего, кроме описания.

**Под названием в этом блоке стоит строка каталога** (`CAT. 012 · 2002 ·
12 упоминаний`), а не оригинал со студией, как в блоках «Упоминается»: здесь
марка приглашает в каталог, а не поясняет, о чём был выпуск.

### Что переехало в общий код

- **Кнопка «Показать ещё» стала компонентом** — `LoadMore.astro`, стили в базе,
  три состояния переключает `src/lib/loadMore.mjs`. Она стоит в четырёх местах
  (подборка «Из архива», ссылка «Весь архив», каталог, упоминания), и четыре
  копии разъехались бы сначала высотой цели, потом цветом наведения. Главная
  переведена на неё же — своей копии у неё больше нет.
- **Шапка большого раздела стала компонентом** — `SectionHead.astro`
  (надзаголовок, H1, подводка, строка счёта). Архив, разделы и каталог рисуют
  одну и ту же вёрстку вплоть до колонок сетки.
- **Цвет марки** — `animeStamp.mjs`, **приведение текста для поиска
  и запись латиницы кириллицей** — `translit.mjs`, **порядок, номера и счёт
  упоминаний** — `animeCatalog.mjs`.

**Указатель упоминаний теперь считается один раз на сборку, а не трижды.**
Он разбирает все расшифровки архива, а просят его три страницы: каталог,
страницы тайтлов и главная. Ответ запоминается, ключ — размеры трёх коллекций:
придёт страница с другим набором данных — посчитается заново, а не выдастся
чужой.

### Превью для соцсетей у страниц тайтлов (хвост №10 закрыт)

`/og/anime-<id>.jpg`, постер вписан целиком на бумажный лист. **Пространство
имён отдельное, и это не украшение:** идентификаторы тайтлов и материалов
живут в разных справочниках, и тайтл с именем вроде `ep-5` молча перебил бы
одноимённый выпуск — в телеграме у выпуска развернулась бы обложка аниме.

**У тайтла без постера своего превью нет**, страница берёт общую картинку
сайта. Решает это одна функция на страницу и на сборку (`animeSocialSource`):
две копии разошлись бы, и страница ссылалась бы на файл, которого никто
не нарисовал. Проверено — у двух тайтлов без постера в теге стоит
`/og/default.jpg`.

## Что проверено и чем — часть 7

- **Сборка проходит**, 33 страницы, ошибок нет.
- **Скрытое действительно скрыто** — `node scripts/check-hidden.mjs` молчит.
  Проверено отдельно разбором собранного CSS: у вертикальной марки правила
  `display` нет вовсе (ребёнок сетки и так блочный), у горизонтальной марки
  и у текстовой карточки оно написано через `:not([hidden])`.
- **Вес правил разобран по собранному CSS**, а не глазами: сетка каталога даёт
  1 колонку на 360 и 620, 3 на 700, 4 на 900, 6 на 1200; сетка упоминаний —
  1, 1, 2, 2, 3; подпись сортировки прячется до 520. Скрипт разбора пришлось
  чинить дважды, см. ниже.
- **Поиск прогнан по собранной разметке** восемнадцатью запросами — латиницей,
  кириллицей, с «ё» и без, по студии и по названию.
- **Сортировки прогнаны в node** на живых данных: по порядку, по алфавиту,
  по году (свежие сверху, без года — в конец), по числу упоминаний.
- **Порции проверены временным снижением порогов**, как листание архива
  в части 4: каталог с порогом 6 из 13 тайтлов скрыл семь и включил кнопку,
  страница тайтла с порогом 2 скрыла одну карточку из трёх и написала
  «Показать ещё упоминания (1)». Пороги возвращены на 24 и 6.
- **Тайтл без упоминаний, без постера и без русского названия проверен
  временной подменой** — в справочник добавлен и потом удалён тестовый файл.
  Блока упоминаний нет, кнопки нет, класс раскладки «без постера» на месте,
  превью общее, заголовком стоит оригинальное название, ни одного
  предупреждения на странице. **В блок каталога на главной он не попал** —
  проверено с временно поднятым размером пула, чтобы это не было случайностью.
  Справочник возвращён как был, `git status` по `src/content/` чист.
- **Порядок каталога сверен с историей git**: `CAT. 001` — «Ходячий замок»,
  первый заведённый тайтл; последний номер у «Хранителя камфорного дерева»,
  заведённого позже всех.
- **Архив и разделы не сломаны переездом шапки** — проверено в собранных
  страницах: надзаголовок, H1, подводка, строка счёта «6 выпусков ·
  1 видеоэссе · 4 заметки», у пустого раздела строка пустая, но место занято.
- **Карта сайта** содержит и каталог, и все 13 страниц тайтлов.
- **Превью тайтлов нарисованы** — 11 файлов, у всех по 1200×630.

## Чего НЕ проверено — часть 7

Всё, что живёт в браузере. Ни одна страница глазами не открывалась.

- **Как выглядят каталог, страница тайтла и блок каталога на главной.**
- **Поиск и сортировка в каталоге на живых нажатиях** — набор запроса
  по букве, смена сортировки, пустое состояние.
- **Порции в каталоге и в упоминаниях** — разметку разобрали, нажатия нет.
- **Обновление четвёрки марок на главной** — перезагрузить главную несколько
  раз, тайтлы должны меняться.
- **Панель поиска и сортировки на узком экране** — переносится ли, пропадает
  ли подпись «Сортировка» до 520 px.
- **Сетка каталога на телефоне** — марки от 140 px, не растягиваются ли.
- **Текст пустого состояния каталога написан кодом, а не заказчиком** —
  по ТЗ тексты пустых состояний пишет заказчик, это часть 9.

## Пять проверок раздела 8.4 — после части 7

| Проверка | Было после части 6 | Стало |
|---|---|---|
| Размеры шрифта меньше 11 px | пусто | пусто |
| Пометки принудительного приоритета | пусто | пусто |
| Видимость и раскладка в одном селекторе | пусто | пусто |
| `auto-fit` вместо `auto-fill` | 2 строки, `BonusPaywall.astro` | **без изменений** — ждёт части 10 |
| Цвета мимо переменных | пусто | пусто |

Плюс `node scripts/check-hidden.mjs` — молчит.

**Четвёртая проверка успела соврать по дороге.** Комментарии к новым сеткам
объясняли, почему тут `auto-fill`, а не `auto-fit`, — и проверка честно нашла
в них запрещённое слово. Это ровно та ловушка, из-за которой `&#8201;` пришлось
менять на `&thinsp;`. Комментарии переписаны словами: «сетка заполняющая,
а не схлопывающая».

## Скрипт разбора весов чинился дважды — и оба раза он врал молча

Тот самый скрипт, которым проверяются раскладки с части 5:

- он читал только `dist/_astro/*.css`, **а часть стилей Astro вставляет прямо
  в страницу**. На вопрос «есть ли у марки правило display» скрипт отвечал
  «нет» про правило, которое есть;
- он разбирал медиазапросы по `min-width`, **а сборщик отдаёт современную
  запись** `@media (width>=1101px)`. Ни одно условие не распознавалось,
  и скрипт уверенно докладывал, что шесть колонок каталога работают
  на телефоне.

Оба раза ответ выглядел правдоподобно. Урок записан в `CLAUDE.md`.

## Цифры — часть 7

- Файлов в сборке: **611 из 20 000** (было 594). Прибавили 11 картинок превью
  для страниц тайтлов и 6 файлов кода — скрипты каталога, страницы тайтла
  и блока на главной со своими общими кусками.
- Вес сборки 39 МБ, папка превью 1,3 МБ.
- **Когда справочник дорастёт до сотни тайтлов**, превью прибавится по файлу
  на тайтл, страниц — тоже по одной. Лимита это не тронет.
- Ссылок на старую шкалу отступов: **36 в 3 файлах** (было 47 в 5).
  Остались `BonusPaywall` и `about` (часть 10) и `search` (часть 8) — ровно
  те, что и должны были остаться.

## Пять правок по замечаниям заказчика — часть 7

Все найдены глазами на живом сайте, все настоящие.

**1. Над постером в каталоге оставалась пустая полоса.** У «Ходячего замка»
и «Наруто» между перфорацией и картинкой висело 10–12 пикселей пустоты,
у соседних марок её не было.

Причина ровно та же, что в части 5, только с другой стороны. §9 дизайн-системы
утверждает: «постеры хранятся строго 2:3», и рамка была сделана 2:3. **Замер
всего справочника это опровергает:** ширина у всех 225, а высота гуляет
от 314 до 338, то есть отношение от 0.666 до 0.717. Постер шире рамки
вписывается по ширине, и остаток уходит в полосы сверху и снизу — тем
заметнее, чем шире постер.

Рамка теперь считается от **самого широкого реального постера** (0.717,
токен `--poster-ratio`). Проверено расчётом по всем тринадцати: полосы сверху
не остаётся ни у одного, а поля по бокам у одиннадцати из тринадцати меньше
двух пикселей и только у двух доходят до 5–7. Поле сбоку залито цветом марки
и читается рамкой; полоса сверху упиралась в перфорацию и читалась дырой.

**Число живёт в одном месте на три вёрстки сразу** — вертикальную марку,
горизонтальную и постер на странице тайтла. Раньше горизонтальная считала
его сама (120 px), и копии могли разойтись молча.

**2. У двух тайтлов не подтянулась обложка, хотя на Shikimori она есть.**
Код отработал правильно — **врал источник**.

У Shikimori два API, и они расходятся. Старый (REST), которым мы пользуемся,
у «Нарисуй это, потом умри» и «Хранителя камфорного дерева» до сих пор отдаёт
служебную заглушку `missing_original.jpg`. Новый (GraphQL) на тот же вопрос
отдаёт настоящий постер, и он же стоит на самом сайте. Проверено запросом
к обоим API.

Заодно нашлось, что **домен переехал**: `shikimori.one` отвечает 301
на `shikimori.io`. Для обычных запросов это незаметно, а вот **POST редирект
превращает в GET и теряет тело** — из-за этого первые попытки спросить GraphQL
возвращали «Unexpected end of document». Адрес источника обновлён; старые
ссылки в справочнике продолжают работать через редирект.

Теперь при заглушке вместо обложки спрашивается новый API. Лишнего запроса
на каждый тайтл не делается: только когда старый сказал «картинки нет».

**3. Если обложки нет и у Shikimori — спрашиваем AniList** (по просьбе
заказчика). Ищем по оригинальному названию, берём ТОЛЬКО файл обложки,
остальные данные остаются от источника, где тайтл нашёлся.

**И сверяем, что нашлось то же самое.** Поиск по названию всегда рискует
взять похожий тайтл — сиквел, спешл, ремейк, — а чужая обложка хуже
отсутствующей: она выглядит правильной и потому не будет замечена никогда.
Название не совпало — обложку не берём и говорим об этом в лог.

Проверено на живых запросах: «Kore Kaite Shine», «Kusunoki no Bannin»
и «Sousou no Frieren» обложку получают; «Frieren» вместо «Sousou no Frieren»
и «Naruto: Dai Katsugeki» вместо полного названия — отклоняются с внятной
строчкой. Знаки препинания при сверке не считаются: «K-On» и «K-On!» — одно
и то же.

**4. При длинном названии ссылка на Shikimori уезжала вниз и повисала
в пустоте.** Заголовок, оригинал, техстрока и ссылка лежали в ОДНОЙ сетке
двумя строками: заголовок с оригиналом в первой, техстрока со ссылкой
во второй (в макете для этого обёртки растворяются через `display: contents`).
На однострочном заголовке это выглядит как в макете. На трёхстрочном первая
строка сетки становится высотой в три строки заголовка — и ссылка съезжает
на всю эту высоту вниз, отрываясь от названия, к которому относится.

Стало **две самостоятельные колонки**: слева заголовок с техстрокой, справа
оригинал со ссылкой. Выравнивание по базовой линии оставлено — первая строка
оригинала по-прежнему встаёт ровно на первую строку заголовка, — но дальше
каждая колонка живёт своей высотой, и ссылка стоит под оригинальным названием
при любой длине заголовка. Ширина правой колонки ограничена, иначе длинное
оригинальное название съедало бы место у заголовка.

**Заодно вернулась цель нажатия у ссылки.** В макете на широком экране
у неё обнулены вертикальные отступы, и при переносе это уехало к нам:
ссылка становилась строкой в двенадцать пикселей высотой вместо цели 44 (§8).
Отступы мешали выравниванию в старой сетке — в новой мешать нечему.

**5. Перед блоком каталога на главной шла вторая линия подряд.** Над блоком
всегда стоит кнопка «Показать ещё материалы», а она сама нарисована между
двумя линиями; блок добавлял свою границу сверху — и линии оказывались
в тридцати пикселях друг от друга, что §5 запрещает прямо. Граница снята.
Тот же случай уже был у сайдбара главной и у сеток режимов фильтра.

**6. В марке на главной число отрывалось от своего слова.** Строка
«CAT. 011 · 2014 · 1 упоминание» переносилась по любому пробелу, и получалось
«…· 2014 · 1» на одной строке и «упоминание» на другой. Теперь номер с годом
и число упоминаний — две отдельные строки, каждая переносится сама по себе.

### Седьмая правка: вёрстка архива поехала от части 7

**Что видел заказчик:** в архиве, в разделах и в подборке «Из архива»
на главной текст в колонках 2 и 3 стоял вплотную к вертикальной линии
между колонками.

**Что сломало.** В части 7 у карточки материала появилось свойство «скрыта» —
страница тайтла прячет им упоминания сверх первых шести. Чтобы атрибут
не проиграл по весу правилу `display: block`, правило было уточнено
до `.list-card:not([hidden])`.

Уточнение подняло вес ВСЕГО правила: `.list-card:not([hidden])[метка]` весит
ровно столько же, сколько `.list-grid > .list-card:nth-child(3n+2)` из общей
сетки, а стоит в документе позже. И его `padding` целиком победил
`padding-left`, которым сетка отодвигает текст от линии. Разметка правильная,
оба правила по отдельности правильные, а страницы три.

**Как починено.** Своего `display` у карточки больше нет вовсе: она всегда
лежит в сетке `.list-grid`, а ребёнок сетки и так становится блочным.
Правилу без `display` спорить с атрибутом нечему, уточнение не нужно, вес
вернулся к прежнему. Проверено разбором собранного CSS с раскрытием
сокращённых записей: `padding-left` и вертикальную линию снова задаёт сетка,
`display` не задаёт никто, `hidden` работает.

**Заодно выяснилось, что одно правило в общей сетке не работает вовсе:**
`.list-grid > .list-card { padding-right }` весит столько же, сколько правило
карточки, и всегда ему проигрывает. Так было и до части 7 — то есть в том
виде, который заказчик принял в части 4. Не трогаем, но записываем: правило
выглядит рабочим, а им ничего не задаётся.

### Восьмая правка: линии в архиве сходились углом

**Что видел заказчик:** на главной в сайдбаре линии выглядят просторно
и не соединяются, а в архиве и в подборке «Из архива» они встречаются углом
и превращают ленту в таблицу.

**Почему так вышло.** Это два разных устройства одного и того же:

- **в сайдбаре** вертикальную линию рисует САМА КОЛОНКА (`border-left`
  у сайдбара), а карточки лежат внутри её поля — их горизонтальные линии
  начинаются после отступа и до вертикальной не достают;
- **в архиве** обе линии рисует одна и та же карточка: `border-left` слева
  и `border-top` сверху. Рамка всегда идёт от края коробки, отодвинуть её
  нечем — линии обязаны встретиться в углу.

**Как унифицировано.** Горизонтальная линия карточки стала псевдоэлементом,
а не рамкой, и начинается после отступа. Величину отступа карточка знает
как `--card-inset`, и она стоит в общей сетке рядом с самим `padding-left` —
это одно и то же число, разъехаться им негде.

Проверено расчётом по собранному CSS для трёх ширин и шести позиций в ряду:
на 400 px колонка одна, вертикальных линий нет, линия сверху идёт во всю
ширину; на 700 и 1200 вертикальная линия стоит только между колонками,
а горизонтальная всегда начинается ровно там, где кончается отступ.

**Заодно выровнен воздух:** сверху у карточки было 16, стало 24 — столько же,
сколько снизу и сколько у карточек сайдбара. Линия перестала прижиматься
к следующей карточке.

**И убрано правило, которое ничего не делало:** `.list-grid > .list-card
{ padding-right }` весило столько же, сколько правило самой карточки, и всегда
ему проигрывало. Оно выглядело рабочим с части 4.

### Проверка веса технического слоя: менять нечего

Заказчик заметил, что подпись «[ короткие заметки ]» на главной набрана
полужирным, и предположил, что такого приёма больше нигде нет.

**Проверено по всему проекту и по утверждённым макетам — приём системный**,
просто он не про то, про что показалось. Полужирным набраны ПОДПИСИ БЛОКОВ:
`[ короткие заметки ]`, `[ из архива ]`, `Материалы` у фильтров, надзаголовки
архива и каталога, «Упоминается в материале», кикер `Аниме` на марке.
Обычным — ДАННЫЕ МАТЕРИАЛА: метастрока `[ подкаст ] · №147 · 31:27`,
`CAT. 001`, даты, счётчики. Ровно так же во всех макетах Сборки-18.

Дизайн-система вес технического слоя не задаёт вовсе: в таблице §3.2 стоят
только кегль, интерлиньяж, трекинг и капслок. То есть 600 пришёл из макетов
и ничему не противоречит.

Разнобоем это выглядит там, где на экране видна одна подпись блока и ни одной
второй: на главной рядом только «короткие заметки», а `[ из архива ]`
появляется лишь после нажатия «Показать ещё». **Заказчик посмотрел на разбор
и решил оставить как есть.** Правило записано в `CLAUDE.md`, чтобы его
не «починили» в следующей сессии.

---

## Что сделано — часть 8, 9 августа 2026

### Ноль: дизайн-система исправлена по данным

§9 утверждала «постеры хранятся строго 2:3». Замер справочника в части 7 это
опроверг, но документ остался как был — и следующий читатель поверил бы ему.
Теперь в `дизайн-система-1.5.md` есть §9.0 с таблицей: что фиксировано
в каждой форме марки, куда уходит остаток и почему рамка равняется на широкий
постер, а не на узкий. §11.1, §11.3 и §20 переписаны на токен `--poster-ratio`,
в §0.1 записано, откуда правка взялась.

### Одна страница, два состояния

`/search` — одна страница. До ввода поле и области поиска, после — те же
элементы плюс результаты. Второй страницы нет: состояние отличает `?q=…`,
и на него можно дать ссылку.

**Поле 64 высотой**, области **переносятся, а не скроллятся**.

**Один контрол с двумя вопросами** (§20): до запроса он спрашивает «Искать в:
Везде / Материалах / Аниме / Упоминаниях», после — «Показывать: Все /
Материалы / Аниме / Упоминания» со счётчиками. Это не два ряда кнопок, а один,
меняющий подпись вместе со смыслом; падежи разные, потому что вопросы разные.
Так в обоих макетах.

### Три группы, и у каждой свой счётчик

| Группа | Что в ней | Счётчик |
|---|---|---|
| Аниме | страницы тайтлов | точное число страниц |
| Материалы | посты и выпуски | точное число страниц |
| Упоминания в выпусках | реплики из расшифровок | число показанных совпадений |

**Пересечение групп намеренное:** один выпуск может быть и материалом
(нашлось в названии), и местом разговора. Ровно так в макете
`search-results.html`, где выпуск 22 стоит и карточкой, и двумя цитатами.

**Счётчик группы упоминаний живой, и это не лень.** Сколько раз слово
прозвучало ВНУТРИ выпуска, видно только в скачанном куске индекса: Pagefind
считает страницы, а не реплики. Поэтому число растёт вместе с кнопкой
«Показать ещё» — и **счётчик области показывает ровно то же число**, что
заголовок группы. Два счётчика рядом обязаны считать одно и то же.

**Порции по шесть.** На каждый показанный результат Pagefind качает свой кусок
индекса, и у выпуска с длинной расшифровкой это 38 КБ. «Показать всё сразу»
означало бы мегабайты на один запрос. Кнопка — общий `LoadMore.astro`,
своей копии у поиска нет.

### Подсветка: цвет плюс подчёркивание

`--accent-text`, полужирный и подчёркивание. **Цвет не может быть единственным
отличием**: при дальтонизме подсветка исчезла бы вовсе. Отрывок подсвечивает
сам Pagefind, заголовок и цитату — страница, тем же приведением текста
(«ё» = «е»), что ищет упоминания тайтлов.

### Постер в результате — та же геометрия, что в марке

Ширина колонки считается как `высота × --poster-ratio`, а не из 2:3. Число
живёт одним токеном на четыре вёрстки сразу: две формы марки, страница тайтла
и вот этот результат. Находка части 7 учтена: полосы над постером нет.

### Переход в транскрипт — механика части 6, второй нет

Ссылка вида `/posts/ep-102/#t=716&q=слово` разбирается тем же кодом
в `Transcript.astro`, что и переход со страницы тайтла: прокрутка ОКНА
транскрипта, подсветка, стрелки ↑↓ и перемотка плеера уже написаны там.

**И тут нашлась настоящая ошибка, которую видно только на живых данных.**
Pagefind ищет по основам слова: запрос «студия» находит «студии». А поиск
внутри расшифровки ищет буква в букву — получив «студия», он не подсветил бы
ничего, и читатель, пришедший по ссылке из поиска, увидел бы на нужной реплике
«ничего не найдено». Теперь в адрес уходит **слово, которое правда стоит
в реплике** («студии»), а не то, что набрал человек. Кавычки и запятые
с краёв снимаются.

### Состояния

- **ничего не найдено** — рисунок персонажа, метка «Ничего не нашлось»,
  «По запросу «…» ничего нет» и приглашение попробовать короче. Тексты
  из `состояния.html`, блок 02. Картинка подставляется скриптом в момент
  показа: висеть в разметке 96 КБ не должны;
- **запрос слишком короткий** — «Введите хотя бы два символа.»;
- **идёт поиск** — «Ищем по 7 расшифровкам…». Число настоящее и считается
  при сборке: не 141 файл расшифровки в репозитории, а те, у которых есть
  опубликованная страница. Опубликуете архив — число вырастет само.

### Латиница кириллицей теперь и в поиске по сайту

В части 7 этому научился каталог. Поиск по сайту про запись не знал, и «гибли»
не находил ни одного из трёх тайтлов Ghibli. Теперь та же строка
(`src/lib/translit.mjs`, один код на два поиска) уходит в невидимое поле
на странице тайтла — Pagefind разбирает разметку, а не стили, и слова из неё
в индекс попадают. Поле спрятано и от глаз, и от экранного диктора.

**И тут же нашлась дыра, которой в каталоге не было видно.** «Gainax»
переводится в «гаинакс», а человек набирает «гайнакс». В каталоге это работало
само: там и данные, и запрос проходят через `foldCatalog`, где «й» = «и».
Pagefind про наши замены не знает и сравнивает слова как есть — поэтому
вариант с «й» теперь уходит в индекс отдельной записью. Каталогу лишняя форма
не мешает: она сворачивается в ту же строку.

### Что ещё пришлось поправить в расшифровке

- **имена голосов больше не идут в индекс.** Они стоят перед каждой репликой,
  и цитата ловила их серединой: «не сделал ничего толкового после Наруто.
  Эд. Ну, справедливости ради…» — читатель принимает имя за часть речи.
  Плюс запрос «Эд» находил каждый выпуск архива, ничего этим не сообщая.
  В выдаче имя не потерялось: оно стоит над цитатой и приезжает туда
  отдельной меткой `speakers`;
- **подписи колонок «Голос · время» и «Реплика» тоже убраны из индекса** —
  слово «Реплика» вставало в начало расшифровки и попадало в цитату первого
  же результата;
- **цитата не выходит за границы своей реплики.** Окно в двадцать слов
  у совпадения в начале блока залезало в предыдущий текст страницы, и в цитату
  попадало «…53:20 — Эндинг. Транскрипт. Всем привет!». Границы блока — это
  соседние якоря, других данных не нужно.

### Метки страниц, из которых рисуется выдача

Результаты собираются в браузере, и текста страницы для них мало. Поэтому
страницы отдают факты по отдельности: у тайтла — постер, оригинальное
название, год со студией и число упоминаний; у материала — подпись типа,
«сколько это займёт», форма картинки и сама картинка; у выпуска ещё и номер
с картой голосов.

**Метастрока склеивается на стороне поиска, а не на странице.** Правило
«как она выглядит» живёт в `PostKicker.astro`; строкой в атрибуте оно стало бы
второй копией и разошлось бы с первой. Картинку выбирает тот же
`getCardMedia`, что и карточка в ленте, — заглушку не подставляем, нет
обложки, значит результат текстовый.

**Плашка «Бонус» переехала в `global.css`.** Её рисуют двое — кикер и результат
поиска, а результат собирается в браузере и до стилей компонента не
дотягивается. Это единственное, что предупреждает читателя о подписке
до перехода, и выглядеть в двух местах по-разному оно не имеет права.

## Что проверено и чем — часть 8

**Главное: движок Pagefind прогнан в Node против собранного индекса** — той же
последовательностью запросов, что делает страница. Это не «разметка на месте»:
искал настоящий поиск по настоящему архиву. Так нашлись три ошибки, которых
разбором разметки не видно вовсе:

1. **список значений у фильтра Pagefind — это «И», а не «ИЛИ».** Запись
   `type: ['Посты','Выпуски']` требует, чтобы страница была сразу и тем,
   и другим, и находила РОВНО НОЛЬ. Группа «Материалы» была бы пустой всегда.
   «Или» пишется через `any`;
2. **сводка по фильтрам на первом запросе сеанса приходит пустой** — файл
   фильтров к этому моменту ещё не скачан. «Наруто» показывал ноль в каждой
   области, найдя при этом три страницы. Счётчики теперь берутся из самих
   запросов;
3. **поиск по основам против поиска буква в букву** — та самая «студия»
   и «студии» выше.

Дальше:

- **50 переходов в расшифровку проверены на живых данных**: блок с таким id
  в собранной странице есть, `data-start` совпадает с секундами из адреса,
  и слово из `q=` действительно стоит В ТЕКСТЕ ЭТОГО БЛОКА — то есть подсветке
  и стрелкам будет что показать. Сбоев ноль.
- **Метки всех страниц проверены разбором индекса** — 145 проверок, сбоев нет.
- **Куски разговора разобраны по всем расшифровкам архива** четырьмя словами
  сразу: минута считается, имя голоса подставляется, якорь на странице есть.
- **Латиница кириллицей проверена пятью парами** («гибли»/«ghibli»,
  «киото»/«kyoto», «кёто», «гайнакс»/«gainax», «аниматион»/«animation») —
  русский запрос находит не меньше латинского.
- **Каталог не сломан новой формой** — 14 запросов по собранной разметке,
  все находят то же, что и раньше.
- **Вес правил раскладки разобран по собранному CSS** для пяти ширин: у всех
  трёх видов результата колонки меняются на 621 и 1101, форма селектора
  у базового правила и у медиазапросов одна и та же.
- **Сборка проходит**, 33 страницы, ошибок нет.
- **`node scripts/check-hidden.mjs` молчит.**
- **Файлы поиска не грузятся нигде, кроме `/search`** — проверено поиском
  ссылки на `pagefind.js` по всей сборке: она ровно одна, в скрипте страницы
  поиска.

## Чего НЕ проверено — часть 8

**СНЯТО: заказчик проверил страницу `/search` в браузере 9 августа 2026 —
после восьми правок, вместе с ними. Всё работает, включая переход в
расшифровку.** Список ниже оставлен как история.

- **Как выглядит поиск** — шапка, поле, ряд областей, три группы, марки
  тайтлов с перфорацией, цитаты.
- **Набор запроса по букве** — не дёргается ли выдача, не мигают ли счётчики.
- **Нажатия по областям** — переключение «Везде / Материалы / Аниме /
  Упоминания», смена подписи ряда.
- **Кнопки «Показать ещё» в каждой группе.**
- **Переход в расшифровку живым нажатием.** Адреса проверены по данным,
  прокрутка окна — нет. Это то самое место, что осталось непроверенным
  с части 6.
- **Пустое состояние и рисунок персонажа** — как он смотрится на бумаге.
- **Узкий экран** — переносятся ли области, не уезжает ли ряд вбок,
  как выглядит результат-упоминание в одну колонку.
- ~~**Текст «В выбранной области ничего не нашлось»** написан кодом, а не
  заказчиком~~ — **не хвост, а решение:** тексты пустых состояний пишет код,
  и написанные здесь признаны удачными. ТЗ (9.6) поправлено.

## Пять проверок раздела 8.4 — после части 8

| Проверка | Было после части 7 | Стало |
|---|---|---|
| Размеры шрифта меньше 11 px | пусто | пусто |
| Пометки принудительного приоритета | пусто | пусто |
| Видимость и раскладка в одном селекторе | пусто | пусто |
| `auto-fit` вместо `auto-fill` | 2 строки, `BonusPaywall.astro` | **без изменений** — ждёт части 10 |
| Цвета мимо переменных | пусто | пусто |

Плюс `node scripts/check-hidden.mjs` — молчит.

## Цифры — часть 8

**Файлов в сборке: 619 из 20 000** (было 617). Прибавились ровно два: рисунок
пустого состояния `character-empty.png` и общий кусок кода `plural.js` —
русское числительное понадобилось и поиску, и каталогу, и сборщик вынес его
в отдельный файл вместо двух копий.

**Вес индекса целиком — 1,0 МБ**, и вот из чего он состоит:

| Что | Файлов | Вес |
|---|---|---|
| куски текста страниц (`fragment`) | 24 | 220 КБ |
| словарь (`index`) | 4 | 160 КБ |
| фильтры | 2 | 8 КБ |
| движок `pagefind.js` и wasm | 3 | 188 КБ |
| **готовые виджеты Pagefind, которыми мы не пользуемся** | 8 | 464 КБ |

Последняя строка — половина всего индекса. Это чужие готовые интерфейсы
поиска, они кладутся в сборку сами. **Они никогда не скачиваются**, но место
в лимите Cloudflare занимают. Восемь файлов из двадцати тысяч — не горит.

**При заходе на главную скачивается НОЛЬ байт индекса.** Проверено: ссылка
на `pagefind.js` во всей сборке ровно одна, и она в скрипте страницы поиска.
Главная грузит три своих файла кода и ни одного файла поиска.

**Первый запрос на `/search` стоит около 132 КБ:**

| Что | Вес |
|---|---|
| движок `pagefind.js` (сжат сервером) | 12,6 КБ |
| `wasm.ru.pagefind` | 69,3 КБ |
| один кусок словаря | 42,2 КБ |
| оглавление и фильтры | 0,6 КБ |
| шесть кусков текста | 6,8 КБ |

Второй и следующие запросы дешевле: движок и словарь уже скачаны, платится
только за куски текста показанных результатов. Один и тот же выпуск,
попавший и в «Материалы», и в «Упоминания», качается ОДИН раз — страница
помнит уже скачанное.

**Прикидка на весь архив (141 расшифровка).** Кусок текста у выпуска сейчас
от 15 до 38 КБ, в среднем 22 КБ. Значит:

- куски текста всех выпусков — около **3 МБ**, словарь — около **3,5 МБ**;
- **но целиком это не скачивает никто**: словарь разбит на куски, и берётся
  только тот, где лежит искомое слово; кусок текста берётся только у
  показанного результата. Шесть результатов на группу — это 130–230 КБ
  на запрос вместо мегабайтов;
- в лимит файлов это не упирается: около 170 кусков текста против 20 000.

Ссылок на старую шкалу отступов: **20 в 2 файлах** (было 36 в 3). Остались
`BonusPaywall` и `about` — обе части 10, как и планировалось.

## Три правки по замечаниям заказчика — часть 8

Заказчик открыл поиск и нашёл два: полосу «Все тайтлы показаны» там, где ничего
не сложено, и мусорную выдачу по некоторым запросам. Второе оказалось тремя
разными причинами сразу.

### 1. Кнопки «Показать ещё» нет, когда показывать нечего

Полоса «Все тайтлы показаны» под единственным результатом ничего не сообщала:
группы и так разделены заголовками со счётчиками. Кнопка теперь появляется,
только если что-то действительно скрыто, и исчезает, когда всё показано.

**Прятать саму `.load-more-wrap` нельзя** — у неё в общих стилях стоит `display`,
и атрибут «скрыто» проигрывает ему по весу. Те самые грабли, что ловит
`scripts/check-hidden.mjs`. Поэтому кнопка обёрнута в пустой `div`, у которого
правил нет вовсе и спорить с атрибутом нечему.

Третье состояние компонента («Все … показаны») при этом не отменяется: оно
осталось в каталоге и в упоминаниях на странице тайтла, где список конечный
и виден целиком.

### 2. Варианты написания не попадали в поиск по сайту

**Запрос «кейон» через «е» не находил «Кэйон!» — хотя ровно этот вариант
записан у тайтла в поле «Варианты написания».** Каталог по ним искал с части 7,
а поиск по сайту про них не знал: в индекс уходили только название, оригинал
и описание.

Теперь варианты уходят в то же невидимое поле, где лежит латиница кириллицей.
Поле у тайтла одно, правило одно, ищут по нему оба поиска.

Это ровно то, ради чего поле заведено: заказчик вписывает вариант один раз,
и тайтл начинает находиться везде.

### 3. Pagefind обрезает ненайденное слово — и находит мусор

**Самое неприятное.** Запрос «кейон» до правки № 2 давал 16 страниц, и в них
светились «Ксюша», «Кэйго», «Королевские», «каждый». Прогон движка показал
причину: **не найдя слова, Pagefind отрезает у него хвост и ищет то, что
осталось.** «Кейон» → «кейо» → «кей» → «ке» → «к», и вот предлог «к» в индексе
есть — на шестнадцати страницах.

Ни настройки, ни способа это отключить у Pagefind нет. Поэтому сверяем сами.
Слово подходит в двух случаях, и только в них:

1. **оно начинается с запроса целиком** — «аниме-индустрия» на «аниме»;
2. **оно отличается только окончанием**: совпадает всё, кроме двух последних
   букв запроса, и своего хвоста у него не больше трёх букв.

Второе условие — про хвост — и есть главное. Движок ищет ПО ОСНОВАМ слова,
и терять это нельзя: «студия» обязана находить «студии», «студией», «студию»,
а «ходячий» — «ходячего» и «ходячему». Но общее начало само по себе ничего
не значит, если дальше идёт другое слово.

**Первый порог был мягче — «половина длины» — и заказчик его пробил сразу.**
Запрос «Мотоко» выдавал «мотив»: три общих буквы из шести, ровно половина.
Половина растёт вместе с длиной слова, а окончание — нет: в русском оно почти
всегда короче трёх букв, поэтому считать надо от конца, а не от середины.
Заодно ушли «студенческое» на «студия» и «анимация» на «аниме».

**И слово разбирается на части, а не чистится по краям.** В расшифровке
попадаются слова, слипшиеся со знаком: `что"Фрирен`. Кавычка внутри, снятие
знаков по краям не помогает, и настоящее упоминание отбрасывалось целиком.
По частям находится — и в адрес перехода уходит именно найденная часть.

**Хвост режется глобально:** не нашлось слово — не нашлось нигде. Значит
достаточно посмотреть один результат: не подтвердилось хоть одно слово
запроса — вся выдача мусорная, и честный ответ на неё «ничего не нашлось».
Стоит это один кусок индекса, который всё равно был бы скачан.

Проверено: «зюзюкамбра» теперь отвечает «ничего нет», а не показывает четыре
страницы.

### 4. Память кусков индекса пережила запрос — и показывала чужие подсветки

**Это была моя ошибка, и заметить её со стороны почти невозможно.** По запросу
«мусор» в карточке материала светились «какой-то», «кажется», «коммерчески» —
слова, оставшиеся от набранного до этого.

Один и тот же выпуск попадает и в «Материалы», и в «Упоминания», и чтобы
не качать его кусок дважды, я запомнил уже скачанное. Но **в куске лежит
не только текст страницы, а ещё и МЕСТА СОВПАДЕНИЙ с отрывком — они посчитаны
под конкретный запрос.** Ключ у куска один и тот же на все запросы, поэтому
подмена была не видна: адрес, заголовок и картинка правильные, а подсветка
и цитаты — от прошлого запроса.

Память теперь живёт ровно один запрос.

### 5. Под заголовком материала стоит описание, а не отрывок Pagefind

Отрывок берётся вокруг найденного слова и цепляет вместе с ним служебное начало
страницы. Заказчик увидел это в выдаче: «] · №135. Королевские космические
силы… О выпуске. Пампам. 12:04 — Новый блок. Транскрипт. Всем привет!»

Теперь там **то же описание, что в карточке ленты, в RSS и в превью ссылки** —
`postDescription`, один факт в одном месте. Подсветку рисуем сами.

**Цена решения записана честно:** если слово нашлось в середине текста статьи,
отрывка с ним в карточке не будет. У выпусков это закрывает группа «Упоминания»,
ради которой она и заведена; у длинных статей — нет.

## Что проверено после правок

- **Прогон движка по девяти запросам**: «кейон», «кэйон», «мусор», «наруто»,
  «фрирен», «гибли», «киото», «студия», «хаяо миядзаки», плюс заведомая чушь
  «зюзюкамбра». Мусорной выдачи нет ни в одном, стемминг не сломан.
- **50 переходов в расшифровку** — по-прежнему без сбоев.
- **`node scripts/check-hidden.mjs` молчит**, пять проверок раздела 8.4 — без
  изменений (непустая только `auto-fit` в плашке подписки).
- Файлов в сборке: 619, без изменений.

## Ещё две правки по замечаниям заказчика — часть 8

### 6. Под заголовком материала видно, ГДЕ нашлось слово

Описание отвечает на вопрос «что это за материал», но не отвечает на «почему
он в выдаче». Пока слово есть в названии или в описании, одно совпадает
с другим. А если оно прозвучало только в разговоре — описание молчит, материал
стоит, и непонятно, за что.

Теперь под заголовком стоит одно из трёх, в этом порядке:

| Где нашлось | Что показываем |
|---|---|
| в названии или описании | описание, слово подсвечено |
| в расшифровке | **минута и сама фраза**, как в упоминаниях |
| в теле статьи или заметки | фраза вокруг слова, без минуты |

**Минута тут не ссылка:** вложить ссылку в ссылку нельзя, а карточка целиком
уже ведёт на материал. За точной минутой — группа «Упоминания» ниже, там
таймкод живой.

Считает всё это ТА ЖЕ функция `moments`, что рисует упоминания, — просто
просим у неё один кусок вместо трёх. И фраза вокруг слова собирается одним
общим `quote()`: у цитаты в упоминаниях границы реплики, у материала границ
нет, всё остальное одинаково. Второй копии окна в двадцать слов нет.

**Заодно нашлась ошибка в данных.** У выпуска без своего описания и без
подходящего первого абзаца `postDescription` возвращает пустоту, и метка
уезжала в индекс строкой «undefined» — ровно так она и печаталась в выдаче
под заголовком выпуска про Gainax. Теперь пустая метка не ставится вовсе,
и такой материал показывает минуту.

### 7. Подводка выровнена по нижней линии заголовка

В макете она прижата к верхней кромке, и рядом с крупным «Поиск» читалась как
две несвязанные надписи. По решению заказчика встала на нижнюю линию.

Сделано двумя слоями: `align-items: end` равняет нижние края коробок и работает
везде, а `@supports (align-items: last baseline)` сажает ПОСЛЕДНЮЮ строку
подводки ровно на базовую линию заголовка — кегли и интерлиньяж у них разные,
и нижний край коробки базовой линией не является (то же правило, что
в `CLAUDE.md`).

**Проверка поддержки здесь нужна не только ради старых браузеров.** Сначала
запасная запись стояла просто строкой выше, в том же правиле, — и сборщик
схлопнул две `align-items` в последнюю, оставив в собранном файле только
`last baseline`. Поймано разбором собранного CSS; в `@supports` схлопывать
нечего.

## Правка марки аниме: номер вместо слова «Аниме»

Заказчик заметил, что две формы марки пишут об одном и том же по-разному,
и что слово «Аниме» в надзаголовке дублирует название блока над ней
(«[ каталог аниме ]», «Упоминается»).

**Стало так, и одинаково в обеих формах:**

| Место | Было | Стало |
|---|---|---|
| Надзаголовок горизонтальной марки | `АНИМЕ`, полужирный, акцентный | `CAT. 004`, обычный, приглушённый |
| Мета в блоке каталога на главной | `CAT. 004 · 2006` | `2006 · Studio Ghibli` |
| Мета в блоке «Упоминается» | `K-On! · 2009 Kyoto Animation` | `K-On! · 2009 · Kyoto Animation` |

Три уточнения, каждое не косметическое:

- **Номер считает общий `animeCatalog.mjs`, а не сама марка.** Свой счёт
  разъехался бы с каталожным молча, и один тайтл получил бы два разных номера
  на двух страницах. Для этого из каталога выделена `catalogCodes()`: она
  считает только порядок и номер и НЕ разбирает расшифровки — марке указатель
  упоминаний не нужен, а он проходит по 141 файлу. `buildCatalog` берёт номера
  оттуда же.
- **Вес номера — обычный, а не полужирный.** Полужирным было слово «Аниме»,
  и это правильно: подписи блоков в техническом слое набираются полужирным.
  Но номер — это ДАННЫЕ материала, а данные полужирными не набираются
  (правило разобрано и записано в `CLAUDE.md` в части 7). Цвет приглушённый,
  как у номера в каталожной марке; акцент в марке остаётся один — стрелка.
- **Год со студией пишется через точку.** В блоке «Упоминается» они стояли
  через пробел, а в каталоге и на странице тайтла — через точку. Два факта
  на соседних страницах писались по-разному; раз уж унифицируем, унифицируем.

**Это расхождение с макетом**, и осознанное: в `Сборка-18` в надзаголовке
горизонтальной марки стоит «АНИМЕ». Решение заказчика, причина записана выше.

Проверено по собранным страницам: на главной марки показывают `CAT. 012`,
`CAT. 011`, `CAT. 003`, `CAT. 004` и год со студией; в блоке «Упоминается»
у выпуска про K-On — `CAT. 006`. Нумерация каталога от правки не сдвинулась:
`/anime/` по-прежнему выдаёт `CAT. 001`…`CAT. 013` подряд.

## Три несоответствия между разделами, найденные заказчиком

Все три — про то, что одно и то же на разных страницах выглядело по-разному.

### 1. Подводка справа от заголовка была двух видов

В архиве и в каталоге она набрана **Body L (18) чернильным**, в поиске —
**Body S (14) приглушённым**. **Так и в макетах**: `page-archive.css` пишет
одно, `page-search.css` — другое. Дизайн-система подводку не описывает вовсе,
то есть спорить документу тут не с чем, и решает заказчик — он выбрал вариант
поиска.

Правило теперь одно и лежит в `src/styles/global.css` (`.section-intro`),
а не в стилях четырёх страниц. Компонентам осталось только место в сетке.

**Проверено опасение заказчика — со строкой счёта под ней подводка не спорит.**
Счёт набран моноширинным, капслоком, в 11 px и с трекингом: это другой слой,
а не тот же текст помельче. Различаются они гарнитурой и регистром, а не
одним лишь кеглем.

### 2. В поиске подводка висела посередине

Она стояла в колонках 6–11 из двенадцати — то есть не доходила до правого края
страницы, и рядом с полем поиска во всю ширину это читалось как ошибка вёрстки.
**Так в макете**, но макет тут проигрывает: у архива и каталога та же подводка
стоит в 8–13 и кончается ровно у края. Поиск переведён на те же колонки,
заголовок — на те же 1–8.

### 3. Вкладки главной были моноширинными, а вкладки архива — нет

У кнопок фильтра на главной стоял класс технического слоя, и он приводил
с собой ГАРНИТУРУ: вкладки главной набирались IBM Plex Mono, а точно такие же
вкладки архива — Inter. Кегль, капслок и трекинг при этом совпадали, поэтому
разница читалась как «другое написание», а не как явная ошибка.

**В макетах гарнитура не задана ни у `.filter-btn`, ни у `.archive-tab`** —
обе берут основную. Класс снят, вкладки совпали.

Моноширинной осталась подпись «Материалы» слева от вкладок: она подпись блока,
а не вкладка, и в макете у неё гарнитура задана явно.

---

## Что сделано — часть 9, первая половина, 9 августа 2026

Спойлер и галерея. Таймкоды блоком, заглушка изображения и пустые состояния —
вторая половина, отдельной сессией.

### Спойлер: слово внутри фразы

**Текст остаётся на месте и только становится невидимым.** Это и есть вся
суть правки: раскрытие ничего не переверстывает — ни строку, ни абзац.
Затирает его плашка брендового цвета ровно своей длины.

Требования тут два, и они тянут в разные стороны:

- содержимое не должно попадать ни поисковому роботу, ни в поиск по сайту —
  значит его не может быть в разметке;
- строка не должна прыгать — значит текст обязан стоять на месте.

**Сходятся они в одной точке.** В собранной странице лежит `<template>`
(Pagefind разбирает разметку, а внутрь template не заглядывает), а браузер
при загрузке переносит текст ВНУТРЬ кнопки и снимает временную ширину.
Дальше нажатие меняет только `visibility`. Прежний код подменял плашку текстом
на том же месте — ширины не совпадали, и строка дёргалась.

**Ссылка внутри спрятанной фразы разворачивается в текст.** Ссылка внутри
кнопки — недопустимая разметка, и браузер по ней всё равно не переходит.
Разворачиваем и говорим об этом строкой в сборку: тихо оставленная мёртвая
ссылка хуже.

### Спойлер: блок

Кнопка между двумя линиями, технический слой, высота 56. **Ширина по читальной
колонке**, а не по экрану: спойлер часть текста, и кнопка во всю ширину
страницы читалась бы как отдельная секция. Прежний вид — плашка с рамкой —
языку сайта противоречил: плиток в нём нет.

**Подпись собирается из двух частей.** Автор пишет существительное («финал»,
«твист 12 серии»), глагол системный: «Показать финал» / «Скрыть финал».
Отдай мы автору всю строку — подписи разъехались бы по формулировкам,
и кнопка перестала бы читаться как один и тот же элемент. По умолчанию —
«спойлер».

**У старых постов глагол из поля снимается.** До этой сессии в поле лежала вся
строка целиком, и в двух постах архива записано «Показать спойлер» — без чистки
вышло бы «Показать Показать спойлер». Правило одно, но копий у него две:
`spoilerNoun` в `src/plugins/remark-spoiler.mjs` и такая же строчка в админке
(она не умеет импортировать из `src`). В обоих файлах стоит пометка «меняете
тут — поправьте и там».

**В админке** поле называется «Подпись кнопки», необязательное, с подсказкой.
Пустое поле пишет маркер без подписи, а не выдуманное слово: «спойлер»
подставит сайт, и второго места с этим словом не заводится.

### Спойлер и описание материала

**Уже было сделано в части 3** и в этой сессии только проверено заново:
`excerptFromBody` обрывает описание на первом спойлере — и абзац со спойлером
режется до его начала, и всё, что ниже, не смотрится вовсе.

Прогнано по всем 147 постам: спрятанных слов в описаниях ноль.

**И проверено, что спрятанное по-прежнему не отдаётся никому.** Куски
спрятанного текста искались во всей сборке: в собранных страницах вне
`<template>` — ноль, в индексе Pagefind — ноль, в `rss.xml` — ноль, в ленте
главной и архива — ноль. Это важно было пересмотреть: с части 2 переписаны
и поиск, и карточка.

**Заодно из индекса убрана подпись самой кнопки** — блок помечен
`data-pagefind-ignore`. Слово «финал» в выдаче ничего не сообщает, а запрос
по нему приводил бы на страницу без видимого совпадения.

### Галерея: сетка

От двух до четырёх картинок. **Одна колонка на телефоне, две выше.** Раньше
колонок было две на любой ширине без единого медиазапроса: на 320 px плитка
выходила 138 px — картинка в ней уже не читается.

**Подпись одна на весь блок**, а не у каждой картинки, и лежит она под сеткой
во всю её ширину. Поля под это заводить не пришлось — берём первые заполненные
у картинок группы. **Если подписей несколько, лишние показать негде, и сборка
говорит об этом строкой:** тихо потерянный текст автор не найдёт никогда.

**Альт остался у каждой картинки свой.** Это единственное, что читает вслух
экранный диктор, и общий альт на четыре картинки не описывает ни одной.

**Плитка сетки кадрируется, слайд карусели — нет.** Плитка это миниатюра,
целиком картинка открывается по нажатию; слайд — сама работа, и §9 требует
от него исходного соотношения.

### Галерея: карусель

Пять картинок и больше. **Единственное место на сайте с горизонтальным
скроллом** (§4.4), и скролл заперт внутри трека: слайд занимает всю его
ширину, поэтому на 320 px читается целиком и своего скролла внутри не заводит.

Прежний код листал `transform`'ом при `overflow: hidden` — то есть пальцем
галерея не листалась вовсе.

**Счётчик `01 / 06` живой** и считается по положению прокрутки, а не по числу
нажатий: иначе после свайпа он показывал бы одно, а на экране стояло бы другое.
Край ленты — выключенная кнопка: у бесконечной ленты не видно, где она
кончилась.

**Стрелки 44×44 выносятся за трек с 1121 px, а не с 1101.** Это посчитано.
От 1101 оболочка сайта шириной `100% − 64px`, трек — 1000, значит сбоку
свободно `32 + (ширина окна − 1064) / 2`; стрелке нужно 58, и набирается это
только с 1116 px. Возьми мы 1101 из системы — в полосе 1101…1115 стрелка
вылезала бы за край страницы и сама создавала горизонтальный скролл, который
на сайте запрещено прятать.

**Шапка карусели в поиск не идёт** — «[ галерея ]01 / 06 ‹ ›» это подписи
органов управления, и в цитате выдачи они читались бы мусором.

### Нумерация: галерея наконец занимает свои номера

Счётчик подписей умел это с части 5 (`images` прибавляется целиком), но
подписи у галереи не было — и она в нумерации не участвовала. Теперь галерея
из четырёх картинок занимает четыре номера и подписывается `FIG. 02–05`,
коротким тире.

## Что проверено и чем — часть 9, первая половина

- **Сборка проходит**, 33 страницы, ошибок нет.
- **Оба вида галереи есть в живых данных** — у поста про Такэсигэ подряд идут
  четыре картинки (сетка) и пять (карусель). Разметка обеих разобрана
  в собранной странице.
- **Подписи, диапазоны номеров и предупреждения проверены временной подменой**
  поста: одиночная подписанная плюс галерея из четырёх плюс карусель из шести
  дали `FIG. 01`, `FIG. 02–05`, `FIG. 06–11`; вторая подпись внутри галереи
  вызвала строчку в сборке; ссылка внутри спрятанной фразы развернулась
  в текст, тоже со строчкой. Пост возвращён, `git status` по `src/content/`
  чист.
- **Нумерация прогнана семью случаями в node**, включая две галереи подряд
  и переход через девятый номер. Код короткого тире сверен — U+2013.
- **Сборка подписи кнопки прогнана семью случаями**, включая старую запись
  «Показать спойлер» и голый глагол без существительного.
- **Обрезка описания прогнана по всем 147 постам** — спрятанных слов
  в описаниях ноль.
- **Спрятанное не отдаётся никому** — куски спрятанного текста искались
  по всей сборке: вне `<template>` ноль, в индексе Pagefind ноль, в `rss.xml`
  ноль, в ленте главной и архива ноль.
- **Вес правил разобран по собранному CSS** для пяти ширин, вместе со стилями,
  вставленными прямо в страницу: сетка галереи даёт одну колонку до 620
  и две выше, стрелки уходят за трек только с 1121, у раскрытого куска
  спойлера правила `display` нет вовсе — значит атрибут «скрыто» сработает
  без уточнений.
- **Место под стрелки посчитано для девяти ширин окна** — от 320 до 1400.
- **`node scripts/check-hidden.mjs` молчит.**

## Чего НЕ проверено — часть 9, первая половина

**СНЯТО: заказчик проверил всё это в браузере 9 августа 2026, всё работает.**
Список ниже оставлен как история — пригодится, если эти места снова тронем.

- **Как выглядят спойлер и галерея.**
- **Нажатие на спрятанное слово** — главное: строка НЕ ДОЛЖНА прыгать,
  а абзац переверстываться. Ради этого всё и переделано.
- **Кнопка блока-спойлера** — подпись меняется с «Показать …» на «Скрыть …»,
  знак с `+` на `×`, содержимое появляется и убирается.
- **Листание карусели** — стрелками, пальцем на телефоне и колесом мыши;
  меняется ли счётчик, гаснут ли стрелки на краях.
- **Не открывается ли лайтбокс от свайпа** по карусели на телефоне. Браузеры
  гасят нажатие после прокрутки сами, но проверить это можно только пальцем.
- **Сетка галереи на телефоне** — одна колонка во всю ширину экрана.
- **Не вылезает ли что-то вбок** на ширине около 1100–1200 px: стрелки стоят
  ровно на границе свободного места, арифметика сошлась, но проверял её код.
- **Поле подписи спойлера в админке** — сохраняется ли, и что показывает
  предпросмотр у старого поста, где записано «Показать спойлер».

## Пять проверок раздела 8.4 — после части 9 (первая половина)

| Проверка | Было после части 8 | Стало |
|---|---|---|
| Размеры шрифта меньше 11 px | пусто | пусто |
| Пометки принудительного приоритета | пусто | пусто |
| Видимость и раскладка в одном селекторе | пусто | пусто |
| `auto-fit` вместо `auto-fill` | 2 строки, `BonusPaywall.astro` | **без изменений** — ждёт части 10 |
| Цвета мимо переменных | пусто | пусто |

Плюс `node scripts/check-hidden.mjs` — молчит.

## Цифры — часть 9, первая половина

- Файлов в сборке: **619 из 20 000** — без изменений. Переоформление блоков
  файлов не добавляет.
- Ссылок на старую шкалу отступов: **20 в 2 файлах** — без изменений,
  `BonusPaywall` и `about`, обе части 10.

## Грабли сессии: правку плагина не было видно

Первые две сборки после переписанного `remark-spoiler.mjs` отдавали **прежнюю
разметку** — и проходили успешно. Astro держит разбор постов в двух папках
сразу, `.astro/` и `node_modules/.astro/`, и чистки одной не хватает.

Опаснее всего то, как это выглядит: разбор собранной страницы честно
показывает старое, и первое объяснение — «правка не работает». Урок записан
в `CLAUDE.md`.

## Две правки по замечаниям заказчика — часть 9, первая половина

Обе найдены глазами на живом сайте, обе про главную в режимах фильтра.

### 1. Под панелью фильтров шли две линии подряд

Панель закрывается своей линией снизу, первая карточка открывается своей
сверху — и между ними только отступ. §5 системы запрещает это прямо.

**Это чинилось в части 3, но только до 761 px.** Тогда решили, что выше линии
над карточками первого ряда идут в ряд и так и задумано, — и в макете они
действительно есть. Макет тут проигрывает документу: заказчик увидел на живом
сайте ровно то же нарушение.

Теперь линия снята у ВСЕГО ПЕРВОГО РЯДА на всех ширинах. Сколько в ряду
карточек, столько и столбцов, поэтому число стоит в том же медиазапросе, что
и сама сетка: разъедься они, линия пропала бы не у того ряда.

**Проверено расчётом по собранному CSS** для четырёх режимов и четырёх ширин:
число столбцов и число карточек без верхней линии совпадают везде —
1/1, 2/2, 3/3.

### 2. Блок каталога аниме оставался в режимах фильтра

По решению заказчика он теперь показывается только в режиме «Все»: блок про
аниме вообще, а не про выбранный тип материалов, и в отфильтрованной ленте
читается чужим хвостом. **Это расхождение с макетом** — там он остаётся во всех
режимах.

**Лента не знает чужих блоков по именам.** Раньше в её скрипте стоял
`getElementById('all-layout')`; теперь блок сам помечает себя атрибутом
`data-only-mode="all"`, а лента прячет всё помеченное. Так странице не нужно
лезть в скрипт компонента, чтобы добавить свой блок, — а именно это и пришлось
бы сделать во второй раз.

Ни у блока каталога, ни у витрины «Все» правила `display` нет вовсе —
значит атрибут «скрыто» срабатывает без уточнений (§2.1). Проверено разбором
собранного CSS; `scripts/check-hidden.mjs` их не видит, потому что в разметке
атрибут не стоит — его ставит скрипт.

### Чего НЕ проверено — правки главной

**СНЯТО: заказчик проверил обе правки в браузере 9 августа 2026, всё работает.**
Список ниже оставлен как история.

- **Одна ли линия под панелью** в каждом из пяти режимов и на трёх ширинах.
  Столбцы и снятые линии посчитаны по собранному CSS, но глазами не видены.
- **Уходит ли блок каталога и возвращается ли он** на «Все».
- **Не сломались ли соседи** — кнопка «Показать ещё материалы» и подборка
  «Из архива» стоят вплотную к тому, что менялось.

### Что осталось нерешённым и почему

- **Без JavaScript спойлер не раскрывается вовсе** — так было и до этой
  сессии: содержимое лежит в `<template>`, и достать его может только скрипт.
  Это цена того, что спрятанное не отдаётся роботам, и она принята.
  **Новое:** стрелки карусели без скрипта тоже мертвы, но сам трек листается
  пальцем и колесом — то есть галерея без JS работает лучше, чем работала.
- **У галереи нет своего поля подписи** — она берётся у первой картинки
  группы. Заводить отдельный блок «Галерея» в админке нельзя: списки внутри
  своего компонента CMS уже дважды теряла. Если подписей несколько, лишние
  показать негде, и сборка говорит об этом строчкой.
- **`scripts/check-hidden.mjs` не видит блоков, которым атрибут «скрыто»
  ставит скрипт** — а это витрина «Все», блок каталога и раскрытый кусок
  спойлера. У всех трёх проверено разбором собранного CSS, что своего
  правила `display` у них нет вовсе. Проверку это не чинит: она разбирает
  разметку, а таких блоков в разметке нет.

## Выложено на сайт — 9 августа 2026

Первая половина части 9 и обе правки главной **задеплоены на боевой сайт**
(`npx wrangler deploy`, 13 изменившихся файлов). Проверено запросом к живому
`bakapodcast.com`: на главной оба блока с `data-only-mode`, на странице поста
новая разметка спойлера и обеих галерей.

---

# Часть 9, раздел 9.4 — блок ссылок в произвольное место, 9 августа 2026

## Что сделано

Список ссылок перестал быть привязан к одному месту страницы. Теперь автор
ставит его туда, где стоит курсор, — как картинку или спойлер.

### Сначала разобрались, что вообще рисует список площадок

ТЗ говорит: «сейчас список площадок рисуется в одном фиксированном месте».
**Это оказалось поле `buttons`** — то самое, которое трогать нельзя.

Оно рисуется в `src/pages/posts/[slug].astro` сразу под всем текстом поста,
перед плашкой подписки: врезка шириной в читальную колонку, линия сверху
и снизу, внутри строки «слева площадка — справа домен со стрелкой». Сделано
в части 5. Автор на место этого блока не влияет никак.

**`buttons` и его фиксированный вывод не тронуты.** ТЗ разводит их прямым
текстом: `buttons` — кнопки материала целиком, место фиксированное; новый
блок — ссылки внутри повествования, место произвольное. У нового блока взят
только внешний вид, и взят переиспользованием, а не копией.

### Данные — маркером в тексте, один маркер на одну ссылку

```
::link{label="Кинопоиск" url="https://www.kinopoisk.ru/film/402/"}
::link{label="Шикимори" url="https://shikimori.one/animes/199-…"}
```

Два маркера подряд → одна врезка с двумя строками. **Ровно тот же приём, что
у галереи**, и по той же причине: списка внутри своего компонента CMS у нас
быть не может — Sveltia такие списки дважды теряла и портила. Это ограничение,
а не выбор.

Побочная выгода: каждая ссылка редактируется как обычный блок — видна
в редакторе отдельной строкой, переставляется и удаляется вместе с абзацем.
Отдельный компонент со списком внутри пришлось бы открывать формой ради
перестановки двух строк.

Разбирает `src/plugins/remark-link-list.mjs`. В `astro.config.mjs` он стоит
**до спойлера** — чтобы внутри спрятанного блока врезка была уже одним узлом,
а не россыпью маркеров.

### Ломается громко

- **Нет адреса** — строка не рисуется, в лог сборки идёт строчка с подписью
  этой строки. Подпись, которая никуда не ведёт, — тихая ложь читателю.
- **Все строки без адресов** — врезки нет вовсе. Пустая рамка из двух линий
  посреди текста читается поломкой вёрстки.
- **Кривой адрес сборку не роняет.** Он лежит в тексте поста, а не в схеме,
  и `hostOf` на непонятной строке возвращает `null` — тогда показывается сам
  адрес целиком. Урок про `z.string().url()` тут не срабатывает, но проверка
  на `https://` в админке всё равно стоит: мёртвая ссылка молчит.

### Подпись ставится всегда, даже пустая

Строка — двухколоночная сетка (118 px под площадку). Не поставь мы первую
ячейку, адрес уехал бы в узкий левый столбец и встал не под адресами соседних
строк. В админке поле обязательное; пустым оно бывает только у маркера,
поправленного руками в файле.

### Стили — одни на два блока, копии нет

Врезку теперь рисуют ДВА разных места: разметка страницы (кнопки материала)
и плагин (ссылки в тексте). Разметку плагина обычный селектор компонента
не видит — Astro метит своей меткой только то, что написано в самом файле.

Поэтому все правила врезки переписаны через `:global`. **Форма селектора
у всех пяти правил одна**, включая медиазапрос: смешай мы `:global(.platform-row)`
с голым `.platform-row`, правила разошлись бы по весу и часть перестала бы
применяться молча (§2.1 и урок про одну форму селектора).

Заодно `margin: … auto` заменён на пару `margin-block` + `margin-inline`:
внутри `.content` сокращённая запись сбросила бы боковые поля, и врезка уехала
бы к левому краю колонки.

### Линии с обеих сторон — тут это законно

Единственное место на сайте, где так можно (§5 дизайн-системы). Правило «одну
линию рисует следующая секция» здесь не работает: после врезки текст
продолжается, и без нижней линии у неё нет конца.

### В нумерации иллюстраций блок не участвует

Ничего для этого делать не пришлось: счётчик `FIG.` живёт
в `remark-image-figure.mjs` и считает только то, что пришло из `::image`.
Врезка ему не видна — она не иллюстрация.

### В поиск не идёт — и теперь оба блока одинаково

Названия площадок и голые домены в цитате выдачи читаются мусором — ровно как
шапка карусели («[ галерея ]01 / 06»). Врезка помечена `data-pagefind-ignore`.

**По решению заказчика тем же помечен и фиксированный список `buttons`.**
Расхождение было бы неприятным именно своей невидимостью: два одинаковых
на вид блока вели бы себя в поиске по-разному, и объяснить это читателю нечем.
Это единственная строка, которой мы коснулись в выводе `buttons`, — сам вывод
и само поле не менялись.

## Что проверено и чем

- **Сборка проходит**, 33 страницы, ошибок нет. Перед каждой сборкой стирались
  ОБЕ папки разбора (`.astro` и `node_modules/.astro`) — иначе Astro отдаёт
  прежнюю разметку, на этом в прошлой сессии потеряли две сборки.
- **Врезка собирается правильно — временной подменой поста.** В живых постах
  блока ещё нет, поэтому в пост «Знакомьтесь: Ёдзи Такэсигэ» временно
  подложены четыре маркера: две ссылки подряд, одна в одиночку и одна битая
  без адреса. В собранной странице оказалось ровно две врезки — склеенная
  из двух строк и одиночная; битая строка выброшена, маркер `::link` в тексте
  не остался, пустого `href=""` нет. Файл возвращён, `git status` чист.
- **Предупреждение о битой строке напечатано** в лог сборки, с подписью строки.
- **В поиск не попало — спрошено у самого движка, а не прочитано в разметке.**
  Локальный сервер плюс копия `pagefind.js` с `basePath` на него. Запросы
  «кинопоиск», «бусти», «телеграм», «шикимори» страницу с врезками
  НЕ находят; «такэсигэ» — находит. (Другие страницы в выдаче есть: там эти
  слова правда звучат в разговоре.)
- **Кнопки материала тоже не индексируются** — в тот же прогон посту временно
  добавлено непустое поле `buttons` с телеграмом, и по «телеграм» страница
  не нашлась.
- **Разбор собранного CSS** (вместе со стилями, вставленными прямо в страницу):
  пять правил врезки, все одной формой, без меток компонента; правило
  с колонками лежит внутри `@media (width>=621px)` — современный синтаксис,
  разбор по `min-width` его бы не увидел.
- **Пять проверок раздела 8.4** и `node scripts/check-hidden.mjs` — ниже.

## Чего НЕ проверено

Всё, что живёт в браузере.

- **Как врезка выглядит внутри текста** — отбивка сверху и снизу, не спорит ли
  она с абзацами вокруг, читается ли одна линия как конец блока.
- **Кнопка «Ссылка врезкой» в админке** — вставляется ли по курсору,
  сохраняется ли, что показывает предпросмотр.
- **Проверка адреса на `https://` в админке** — `pattern` у поля ВНУТРИ
  компонента редактора у Sveltia не щёлкан ни разу. Она уже расходилась
  с документацией Decap; не сработает — кривой адрес просто станет мёртвой
  ссылкой, сборку это не уронит.
- **Не сломалась ли вставка текста из соцсетей.** Предпросмотр новой кнопки
  на пустых данных отдаёт строку без HTML-тега — та самая мина, на которой
  уже подрывались. Проверяется только вставкой большого куска текста
  в редактор.
- **Вид на узком экране** — ниже 621 px строка становится в две строки,
  подпись над адресом.
- **Врезка внутри спойлера** — по коду она попадает туда одним узлом,
  но глазами не видена.

## Пять проверок раздела 8.4 — после раздела 9.4

| Проверка | Было после первой половины части 9 | Стало |
|---|---|---|
| Размеры шрифта меньше 11 px | пусто | пусто |
| Пометки принудительного приоритета | пусто | пусто |
| Видимость и раскладка в одном селекторе | пусто | пусто |
| `auto-fit` вместо `auto-fill` | 2 строки, `BonusPaywall.astro` | **без изменений** — ждёт части 10 |
| Цвета мимо переменных | пусто | пусто |

Плюс `node scripts/check-hidden.mjs` — молчит.

## Цифры — раздел 9.4

- Файлов в сборке: **619 из 20 000** — без изменений. Блок не создаёт файлов.
- Ссылок на старую шкалу отступов: **20 в 2 файлах** — без изменений,
  `BonusPaywall` и `about`, обе части 10.

## Что осталось нерешённым и почему

- **Цель нажатия у строки врезки меньше 44×44 — РЕШЕНО ЗАКАЗЧИКОМ, оставляем
  исключением.** Строка — текст Body S 14 px с интерлиньяжем 1.45, то есть
  около 20 px высотой, между строками 12 px. Правило приёмки требует 44,
  и здесь оно сознательно не выполняется. Причины: врезка сделана в части 5
  по утверждённому макету и с тех пор не менялась; поднять отступы можно
  только в обеих её формах разом (правила общие, копии стилей нет), то есть
  задев кнопки материала. **В части 11 это не чинится** — в приёмке отметить
  как известное исключение, а не как найденное нарушение.
- **У врезки нет своей подписи** вроде `[ ссылки ]`. В ТЗ её нет, в макете
  части 5 тоже: блок опознаётся линиями и техническим столбцом слева.

## Грабли сессии: кнопки не было в меню админки

Код был правильный, выложен и проверен — а кнопки «Ссылка врезкой» в меню
не появилось. Ошибки при этом не было никакой.

**Причина.** У поля «Текст» в `config.yml` задан явный список
`editor_components`. Пока он есть, Sveltia показывает в меню ТОЛЬКО
перечисленное в нём, а всё прочее зарегистрированное просто не зовёт.
Список появился давно и ради другого — убрать встроенный простой блок
картинки, который не умеет ни подпись, ни альт. С тех пор **каждая новая
кнопка невидима по умолчанию**, и об этом ничто не напоминает.

**Как нашли.** Не гаданием, а чтением кода самой Sveltia: в её сборке
список компонентов поля берётся как
`fieldConfig.editor_components ?? [...встроенные, ...зарегистрированные]` —
то есть заданный список ПЕРЕКРЫВАЕТ всё зарегистрированное, а не дополняет.
Заодно там же видно, что регистрация требует всего шести полей (`id`, `label`,
`pattern`-регулярка, `toBlock`, `toPreview`, `fields`-массив), и наша кнопка
им отвечала — значит подозрение на отказ регистрации отпало сразу.

**Побочно поймана врущая проверка.** Первый же запрос к живому сайту
(`curl` на `/admin/index.html`) вернул НОЛЬ БАЙТ, и из этого вышло, что новый
код не выложен вовсе. На самом деле этот адрес отвечает переадресацией
на `/admin/`, а тело у переадресации пустое. Проверка, читающая тело
и не глядящая на код ответа, соврала ровно в ту сторону, в которую страшнее.
Оба урока записаны в `CLAUDE.md`.

**Порядок в списке заодно объяснил меню.** Пункты в админке идут не в порядке
регистрации, а в порядке этого списка — поэтому «Видео» и стояло вторым,
хотя регистрируется пятым. Судить по порядку меню о содержимом файла нельзя.
