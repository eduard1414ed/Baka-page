// Посты, перенесённые из телеграма РУКАМИ, и номера сообщений, из которых они
// сделаны. Сайт этот файл не читает вовсе — он нужен одному только импорту
// (scripts/telegram-import.mjs), чтобы не завести их второй раз.
//
// ЗАЧЕМ ОТДЕЛЬНЫЙ ФАЙЛ, А НЕ ПОЛЕ В САМИХ ПОСТАХ. Импорт узнаёт «своё»
// по полю `tgId` в посте, и для всего, что он завёл сам, этого довольно.
// Но эти три поста заведены раньше импорта и поля не носят, а дописать его
// туда нельзя: они опубликованы, а опубликованное правило проекта трогать
// запрещает. Значит соответствие живёт снаружи — ровно так же, как порядок
// тайтлов (`animeOrder.js`) и журнал обложек (`coverCutoutLog.mjs`).
//
// «ДЕВУШКИ-ПОНИ» ЗДЕСЬ НЕ ЛИШНИЕ, хотя их сообщение и так отсеивается
// правилом анонса (в нём ссылки на Boosty и Patreon). Отсев — это совпадение
// обстоятельств: уберите завтра Boosty из списка площадок, и дубль появится.
// Строчка здесь говорит про факт, а не про побочное действие другого правила.
//
// Дописывать сюда руками нужно только новые ручные переносы. Всё, что завёл
// импорт, узнаётся само.
export const manualTelegramPosts = {
	4135: 'znakomtes-yodzi-takesige',
	4142: 'devushki-poni-seraya-zolushka-v-chem-sekret-populyarnosti',
	4143: 'narisuy-eto-potom-umri',
};
