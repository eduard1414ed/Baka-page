# BAKA! — Frontend Design System v1.5

Нормативная спецификация фронтенда. Заменяет v1.4.

**Статус.** Этот документ — единственный источник истины по токенам, размерам,
цветам, отступам, доступности и мобильному reflow. HTML-макеты — источник истины
по композиции страницы: какие блоки есть и в каком порядке. При расхождении
размера или цвета побеждает документ; при расхождении композиции побеждает макет.

## 0. Что изменилось относительно v1.4

Шесть противоречий v1.4 решены. Это единственные смысловые правки; визуальный
язык не менялся.

| Противоречие в v1.4 | Решение v1.5 |
|---|---|
| Кнопка play: 56×56 в §8 и 54×54 в §13 | **54×54** на страницах. Живой плеер собран под 54, менять компонент ради круглого числа незачем. **Слово «везде» снято 10 августа 2026:** в закреплённой панели кнопка 44, и это осмысленно — панель не страница выпуска, а «везде» объявляло её нарушением |
| `--wide` 1000 в токенах и 980 в макетах | **1000** |
| «Упоминания» означало и марки аниме, и карточки материалов | Разведены: **марка** (аниме как сущность) и **упоминание** (материал, где о нём говорили) |
| §28.2 предписывал брать геометрию из макетов, а макеты нарушали шкалу | Макет даёт композицию, документ даёт размеры. См. §28 |
| §10.1 запрещал серую строку под заголовком, макеты её рисовали | Запрет подтверждён и реализован |
| `--accent-soft` .10 в документе, .12 в макетах | **0.10** |

Плюс одна техническая правка, не бывшая противоречием, но ломавшая вёрстку:
**`auto-fit` заменён на `auto-fill` во всех сетках.** См. §4.5.

## 0.1. Правка 9 августа 2026: пропорция постера аниме

Документ утверждал, что постеры хранятся строго 2:3, и из этого выводил ширину
колонки 112 в §11.3 и рамку 2:3 в §11.1. **Замер справочника это опроверг**
(подробности и таблица — §9.0): ширина у всех 225, высота гуляет, ровно 2:3
оказался один постер из одиннадцати. Обе рамки теперь считаются от токена
`--poster-ratio`, а не от 2:3.

Правка внесена **по факту, а не по решению**: на живом сайте над постерами
в каталоге висела пустая полоса, и её видел заказчик. Записано сюда, чтобы
следующий читатель поверил данным, а не документу.

## 1. Принципы

Редакционное медиа с каталогом аниме, а не набор UI-карточек. Иерархия строится
типографикой, сеткой, воздухом и тонкими линиями.

Три гарнитуры и только три: Literata — заголовочный слой, Inter — текст
и интерфейс, IBM Plex Mono — технический слой.

Одна типографическая шкала. Локальных размеров «на глаз» нет.

Консистентность на уровне повторяемого компонента, а не через одинаковую сетку
всех страниц. Один и тот же плеер, марка или SocialStrip выглядит одинаково
везде; композиция главной, статьи и каталога различна и зафиксирована отдельно.

Контент лежит на бумажном фоне. Плиток, скруглений и теней в языке сайта нет.

Аниме — отдельная сущность и единственный носитель метафоры почтовой марки.

На мобильном текст всегда внутри полей. В full bleed выходят только явно
обозначенные медиа.

Горизонтального скролла нет нигде, кроме фотогалереи. Не помещается —
перестраивается, а не сжимается.

## 2. Архитектура CSS

**`!important` запрещён.** Единственное исключение — утилита `.visually-hidden`.
Патч-слой `shared-overrides.css` расформирован: его правила перенесены в общий
CSS и в страничные, встроенные устаревшие стили удалены.

Порядок каскада ровно такой:

1. **Токены** — `:root`, переменные из §3.
2. **База** — сброс, типографика элементов, фокус, медиа.
3. **Общие компоненты** — шапка, подвал, марка, плеер, SocialStrip,
   SupportStrip, LoadMore, карточка материала.
4. **Композиция страницы** — под классом `body.page-*`, только раскладка:
   гриды, колонки, вертикальный ритм.

Правило четвёртого уровня не имеет права задавать размер шрифта, цвет
или геометрию общего компонента. Если компоненту нужен вариант — заводится
модификатор (`.player--podcast`, `.archive[data-mode="podcast"]`), а не
локальное переопределение.

Один компонент — одна реализация. Двух живых версий одного компонента
в кодовой базе быть не может.

### 2.1. Видимость и раскладка живут в разных селекторах

**Селектор, который показывает или прячет элемент, не задаёт ему раскладку.**
И наоборот.

Пример, на котором это выстрелило. Режим фильтра на главной одновременно
показывал карточку и назначал ей `display: block`:

```css
/* так делать нельзя */
.filtered-layout[data-mode="note"] .archive-card[data-type="note"]{ display:block }
```

Четыре селектора дают вес, который перебивает почти любое правило раскладки.
Карточка заметки с обложкой объявляла себе `display: grid`, но `block`
побеждал по весу, и сетка не включалась никогда — при этом в коде она была,
а в браузере её не было.

Правильно — разделить ответственность:

```css
/* режим отвечает только за видимость */
.archive-card{ display:none }
.filtered-layout[data-mode="note"] .archive-card[data-type="note"]{ display:revert }

/* тип карточки отвечает только за раскладку */
.archive-card[data-type="note"].has-cover{ display:grid }
```

Признак беды: чтобы правило заработало, к нему приходится дописывать
селекторы «для веса». Это значит, что два правила спорят за одно свойство,
и лечить надо разделением ответственности, а не наращиванием веса.

На главной пять режимов фильтра и четыре типа карточек — двадцать пересечений.
Без этого разделения каждое станет отдельной битвой.

## 3. Токены

### 3.1. Гарнитуры

| Роль | Гарнитура | Где |
|---|---|---|
| Display | Literata | H1–H3, названия материалов и аниме, lead, deck |
| Body / UI | Inter | Текст, описания, навигация, кнопки, подписи, формы, подвал |
| Technical | IBM Plex Mono | Типы материалов, номера, даты и длительности в метастроке, таймкоды, счётчики, `CAT. 001`, `FIG. 01` |

Literata подключается как переменный шрифт с `font-optical-sizing: auto`.
Fallback-стэки — только страховка загрузки.

### 3.2. Типографическая шкала

Шкала выверена под Literata и Inter, а не под подстановки Georgia/Arial.

| Токен | Desktop ≥1101 | Tablet 621–1100 | Mobile ≤620 | Гарнитура |
|---|---|---|---|---|
| H1 | 56 / 1.02 / −0.025em | 48 / 1.04 | 40 / 1.05 / −0.02em | Literata |
| H2 | 34 / 1.08 / −0.015em | 32 / 1.08 | 30 / 1.10 | Literata |
| H3 | 24 / 1.12 | 24 / 1.12 | 22 / 1.15 | Literata |
| Card title | 20 / 1.15 | 20 | 20 | Literata |
| Body L | 18 / 1.65 | 18 | 18 | Inter |
| Body M | 16 / 1.60 | 16 | 16 | Inter |
| Body S | 14 / 1.50 | 14 | 14 | Inter |
| Caption | 12 / 1.45 | 12 | 12 | Inter |
| Technical | 11 / 1.35 / 0.08em / uppercase | 11 | 11 | IBM Plex Mono |
| Home feature L | 46 / 1.02 | 46 | 36 / 1.05 | Literata |
| Home feature M | 31 / 1.08 | 31 | 27 / 1.10 | Literata |

Размеров вне таблицы не существует. Home feature L/M — единственное исключение,
и оно кодирует редакционный приоритет главной, а не новый уровень заголовка.

**Технический слой — ровно 11 px.** Не 8, не 9, не 10. Он и так отличается
моноширинным начертанием, капслоком и трекингом; уменьшение кегля не добавляет
служебности, а убирает читаемость.

**Кегль обязателен всегда, капслок — нет.** Прописные снимаются там, где
в строке стоит значение, которое человек будет читать или переписывать буква
в букву: время в закреплённой панели плеера и маркировка рекламы (§12.1,
там в коде `erid` различаются строчные и прописные). Технический слой этим
не перестаёт быть техническим — его несут гарнитура, кегль и трекинг.
Двух этих мест на весь сайт достаточно; третье заводится не по вкусу,
а по такому же доводу.

Lead статьи и страниц «О проекте» / «Поддержать» — H3-размер, но остаётся
абзацем. Deck выпуска — Card title 20 / 1.45. Оригинальное название аниме —
Card title 20, Literata italic.

### 3.3. Цвета

| Токен | Значение | Назначение |
|---|---|---|
| `--paper` | `#F8F6F0` | Единственный фон страницы |
| `--ink` | `#151515` | Заголовки, основной UI, иконки |
| `--text` | `#3F3B36` | Длинный текст |
| `--muted` | `#69645D` | Вторичная информация |
| `--line` | `#CBC6BC` | Декоративные разделители |
| `--line-soft` | `#DFDBD2` | Строки внутри транскрипта |
| `--line-strong` | `#8F8A82` | Не рамки: заливка непройденной части полосы прогресса, подсветка реплики, номера-заглушки в листании. Рамки контролов рисует `--line` — см. §5 |
| `--accent-brand` | `#D88F9F` | **Только заливки**: play, прогресс, декоративные линии |
| `--accent-text` | `#A64E65` | **Весь акцентный текст**: ссылки, кикеры, таймкоды, стрелки, активные состояния |
| `--accent-soft` | `rgba(216,143,159,.10)` | Мягкая подсветка активного |

**Тёмной темы нет.** Сайт светлый, `prefers-color-scheme` не поддерживается:
добавить вторую палитру позже — значит заново проверять контраст всех
сочетаний, а их в системе больше тридцати.

Контраст на `#F8F6F0`: ink 16.9:1, text 10.3:1, muted 5.4:1, accent-text 5.0:1.
Брендовый `#D88F9F` даёт 2.3:1 — **на тексте не используется никогда**, включая
кикеры, таймкоды и стрелки.

Палитра закрыта. Внесистемных серых быть не может: всё, что раньше писалось
как `#38342f`, `#4a4640`, `#49453f`, `#4d4943`, `#55514b`, `#5f5b55`, `#625d55`,
`#5a554f`, приводится к `--text` или `--muted`; `#b7b2aa` — к `--line-strong`.

Активное состояние никогда не обозначается только цветом: фильтр получает
подчёркивание, ссылка — подчёркивание или стрелку, текущая реплика — фон плюс
изменение технического маркера.

Пастельные цвета марок допустимы только у карточек аниме и задаются
через `--stamp` на самой карточке.

### 3.4. Ширины и сетка

| Токен | Размер | Где |
|---|---|---|
| `--site` | 1320 | Максимальная ширина оболочки |
| `--full` | 1240 | Крупные editorial-блоки и paywall |
| `--wide` | 1000 | Видео, транскрипт, галерея |
| `--reading` | 700 | Статьи и длинный текст |
| `--grid-gap` | 24 | Основной desktop-зазор |

Имя `--body` для читальной колонки не используется — только `--reading`.

### 3.5. Отступы

`space-1` 4 · `space-2` 8 · `space-3` 12 · `space-4` 16 · `space-6` 24 ·
`space-8` 32 · `space-12` 48 · `space-16` 64 · `space-18` 72.

Промежуточные значения не вводятся. Исключение — фиксированные размеры медиа
и контролов, названные в этом документе.

## 4. Адаптивность

### 4.1. Брейкпоинты

| Состояние | Viewport | Поля |
|---|---|---|
| Mobile | ≤ 620 | 18 |
| Compact tablet | 621–820 | 24 |
| Tablet | 821–1100 | 32 |
| Desktop | ≥ 1101 | 32 |

CSS пишется mobile-first: база — узкий экран, затем `min-width: 621px`, `821px`,
`1101px`.

### 4.2. Обязательные правила

Текстовый контейнер всегда внутри полей страницы.

Изображения и видео не деформируются: `aspect-ratio` + `object-fit` либо
`width:100%; height:auto`.

Дети grid и flex, содержащие текст, получают `min-width: 0`.

Длинные слова, адреса и оригинальные названия: `overflow-wrap: anywhere`.

`overflow-x: hidden` на `body` запрещён — он маскирует дефект и режет фокусную
рамку. Страница должна помещаться по-настоящему.

`white-space: nowrap` разрешён только коротким атомарным токенам вроде `11:56`.
У навигации, заголовков и метастрок запрещён.

**Фиксированная высота текстовых контейнеров запрещена.** Ни у карточек,
ни у колонок, ни у навигации, ни у paywall. `height` допустим только у контролов
и медиаповерхностей с заданным соотношением сторон. Текстовый блок рядом
с квадратной обложкой выравнивается гридом, а не заданной высотой.

Компонент перестраивается **до** того, как колонка станет уже своего
минимального содержимого.

### 4.3. Full bleed на мобильном

Основное изображение статьи, видеоэссе и галереи может выходить до краёв.
Подписи возвращаются внутрь полей.

```css
.media--bleed-mobile{
  width:100vw; max-width:none; margin-inline:calc(50% - 50vw);
}
```

**Считается это от ПОЛЕЙ СТРАНИЦЫ, а не от `100vw`:** ширина окна включает
место под полосу прокрутки (`scrollbar-gutter: stable`), поэтому `100vw` шире
документа и сам создаёт горизонтальный скролл, который запрещён §4.2.
На сайте это `margin-inline: calc(var(--gutter) * -1)`.

**ЗАМЕНЯЕМОМУ ЭЛЕМЕНТУ ОДНИХ ОТРИЦАТЕЛЬНЫХ ПОЛЕЙ МАЛО.** Обычный блок имеет
ширину `auto`, и поля раздвигают её сами. У `<video>` и `<img>` ширина своя,
и на сайте им проставлено `width: 100%` — без него `<video>` встаёт
стандартными 300×150. Эти сто процентов считаются от колонки: коробка
остаётся шириной с колонку, а поля просто увозят её ВЛЕВО — слева элемент
упирается в край экрана, справа не дотягивает ровно на одно поле страницы.
Таким элементам ширина называется числом: `calc(100% + var(--gutter) * 2)`.
Наступили 10 августа 2026 на видео внутри текста; **замер краёв этого показать
не мог** — он обрезал ширину по контейнеру, и настоящий вынос выглядел
так же, как сломанный сдвиг.

### 4.4. Галерея

Единственный компонент с горизонтальным скроллом, и только внутри трека.
Каждый слайд целиком читается на 320 px без вложенного скролла.

### 4.5. `auto-fill`, а не `auto-fit`

**Во всех сетках карточек используется `auto-fill`.**

`auto-fit` схлопывает пустые колонки, и единственная карточка растягивается
на всю ширину контейнера — марка на 1000 px вместо 280 выглядит сломанной.
`auto-fill` пустые дорожки сохраняет.

```css
grid-template-columns: repeat(auto-fill, minmax(280px, 1fr));
justify-content: start;
```

Правило распространяется на сетку марок, каталог на мобильном и любые будущие
адаптивные сетки.

## 5. Разделители

| Тип | Цвет | Когда |
|---|---|---|
| Section | `--line` | Между крупными секциями |
| Soft row | `--line-soft` | Строки транскрипта и плотные списки |
| Control | `--line` | Поля ввода, кнопки, контролы |

**Линия на сайте одна: `--line`, 1 px.** Строки «Strong `--ink` — начало
транскрипта или плеера» здесь больше нет. Она была акцентом начала блока,
но на живой странице рядом с ней всегда стоят обычные разделители, и
чернильная читалась не акцентом, а сбоем — заказчик снял её 9 августа 2026
вместе с тёмными рамками контролов.

Исключение из «1 px» одно, и оно не разделитель: полоса слева у цитаты —
2 px `--accent-brand`, это акцент, а не линия.

**Линейная графика внутри контролов подчиняется тому же правилу.** Дуги
кнопок перемотки ±15 — `--line` толщиной 1 px, ровно как рамка кнопки скорости
рядом с ними. Обводка задаётся `vector-effect: non-scaling-stroke`: рисунок
живёт в системе 32×32 и показывается в 30 px, поэтому число в `stroke-width`
дало бы на экране не ту толщину, что у соседней рамки. Цифра «15» внутри
остаётся чернильной — это текст, а не линия.

**Контрол рисуется той же линией, что и разделитель, — решение заказчика
от 9 августа 2026.** Раньше здесь стоял `--line-strong` (#8F8A82), подобранный
под контраст 3:1 для границ органов управления. Заказчик посмотрел живую
страницу «О проекте» и сказал верное: кнопка и разделитель — одна сущность,
а выглядят разными. Ему была названа цена — `--line` даёт 1.57:1, порог 3:1
рамки контролов больше не проходят, — и решение принято с ней.
**В части 25 это исключение, а не найденное нарушение.** Середины не было:
самый светлый тёплый серый, ещё дающий 3:1, — #938E86, от прежнего тёмного
неотличим.

**Состояние наведения и фокуса у рамки всегда `--accent-text`.** Раньше три
места чернели вместо этого (`--ink`): кнопка скорости в плеере и ДВА поля
поиска — причём второе поле поиска розовело, то есть два одинаковых поля вели
себя по-разному. Приведено к одному тогда же.

Между двумя соседними секциями всегда **одна** линия.

Границу рисует начало следующей секции: `border-top`. `border-bottom`
у секционных компонентов не используется — кроме шапки.

Секция не может иметь одновременно `border-top` и `border-bottom`. Это прямая
причина двойных линий при стыковке.

**Врезка внутри текста закрывается линией СНИЗУ, и только снизу.** Список
ссылок, таблица или оглавление выпуска внутри читальной колонки: после врезки
текст продолжается, это не стык двух секций, и без нижней линии у врезки
не было бы конца.

**Это правка от 10 августа 2026, и она отменяет исключение, написанное здесь
специально под врезку.** Прежняя редакция требовала линии С ОБЕИХ сторон:
нижняя закрывает блок, верхняя отмечает его начало. Первая половина довода
верна и осталась, вторая оказалась неверной. Начало врезки отмечает её
собственная подпись (`[ таймкоды ]`, `[ приобрести артбук … ]`, §13) —
линия делала это во второй раз. **Увидеть это на одном блоке нельзя:**
заказчик открыл страницу выпуска, где врезка ссылок и оглавление стоят одно
под другим, и увидел четыре горизонтальные линии подряд на неполный экран.

**НАЧАЛО БЛОКА ОТМЕЧАЕТСЯ РОВНО ОДИН РАЗ — ПОДПИСЬЮ ЛИБО ЛИНИЕЙ.** Врезка
без подписи верхнюю линию получает: отмечать её начало иначе нечем.
Уточнение того же дня, решение заказчика.

| Врезка | Линия сверху | Линия снизу |
|---|---|---|
| с подписью над ней (`[ таймкоды ]`, `[ … ]`) | нет — начало отмечает подпись | есть |
| без подписи | **есть** | есть |

Правило пишется «от общего к частному»: линия сверху по умолчанию, снимает
её соседство с подписью. Наоборот в CSS не выразить — «предыдущего соседа»
не спросишь.

Внутри списка `border-top` ставится только на `.item + .item`.

Вертикальная линия допустима только между равноправными колонками.

Карточка материала не получает рамку со всех четырёх сторон.

## 6. Шапка

Один компонент на всех страницах, локальных копий CSS нет.

| Viewport | Композиция | Параметры |
|---|---|---|
| ≥1240 | Логотип + линия + тэглайн │ навигация │ поиск | высота 142; логотип ≤300; линия 76; тэглайн ≤175; зазор навигации 52; поиск 44×44 |
| 821–1239 | Бренд + поиск, навигация рядом с брендом и переносится | min-height 126; логотип ≤270; тэглайн ≤250 под логотипом; зазор 24; перенос, не скролл |
| ≤820 | Логотип + поиск + кнопка меню; разделы одной колонкой по кнопке | поля 18; логотип ≤190; поиск 44×44; пункт ≥44 по высоте |

**Широкая раскладка начинается с 1240, а не с системного 1101** — это
единственное место, где шапка отступает от брейкпоинтов §4.1, и отступает
по счёту, а не на вкус. Четырём пунктам меню с зазором 52 в худшем случае
(активен «Поддержать», он полужирный) нужно 514.6 px по метрикам самого файла
Inter, а колонка на 1101 отдавала 491 — и 476 там, где видна полоса прокрутки.
Не хватало 24–39 px, и `overflow-wrap: anywhere` рвал пункты ПОСРЕДИ СЛОВА:
«ГЛАВНА / Я». Тем же местом был зажат тэглайн: ему оставалось 93 px при самом
длинном неразрывном куске «СКРЫВАЕТСЯ» в 92.3 — подпись вставала столбиком
на семь строк. С 1240 навигации хватает с запасом 98 px при любой полосе
прокрутки, а тэглайн получает свои 175. Правка 10 августа 2026.

**НАВИГАЦИЯ НА УЗКОМ ЭКРАНЕ РАСКРЫВАЕТСЯ КНОПКОЙ.** До 821 px разделы
свёрнуты, и открывает их кнопка меню в шапке; поиск при этом остаётся
снаружи на любой ширине — это главная функция сайта, прятать её на два
касания нельзя. Раскрытые пункты идут ОДНОЙ колонкой строками во всю ширину,
а не сеткой 2×2.
**Это правка документа, а не сайта, и вот почему.** До 10 августа 2026 здесь
стояло «Гамбургера в системе нет» и «≤620 — навигация 2×2», а кнопка при этом
стоит во ВСЕХ утверждённых макетах Сборки-18 до единого. Спорили не сайт
с документом, а два документа между собой; по §26 композиция берётся из макета.
Решение заказчика 10 августа 2026: сайт верен, документ отстал.

Высота шапки на desktop — 142 на **всех** страницах, включая главную.

Тэглайн: Inter, Caption 12, uppercase, кегль ниже Caption не опускается.
От 1240 стоит сбоку от логотипа за вертикальной линией, в колонке ≤175;
на 821–1239 переезжает под логотип, в колонку ≤250; **до 821 не показывается
вовсе** — там в шапке только логотип, поиск и кнопка меню.
**Это уточнение, а не правка:** прежняя строка обещала, что на мобильном он
«переносится на свою строку», а он там скрыт с самой части 5. Сверено с кодом
10 августа 2026.

**Длина тэглайна — часть вёрстки.** В колонке 175 он встаёт в четыре строки,
в 250 — в две. Считается это не на глаз: короткие слова связаны с соседними
неразрывным пробелом (`bindShortWords` в `Header.astro`), поэтому неразрывных
кусков всего семь, и самый длинный из них — «СКРЫВАЕТСЯ», 92.3 px. Колонка уже
этого числа означает столбик в семь строк. Меняя текст, пересчитать.

Навигация: Inter, Caption 12, uppercase, трекинг 0.08em. Активный пункт —
чернильный полужирный плюс черта: цвет, начертание и черта разом, как
у активной вкладки фильтров.

**Навигация никогда не скроллится** — ни на какой ширине. Запрет остаётся
в силе и после правки про кнопку меню выше: кнопка про то, СВЁРНУТЫ ли
разделы, а этот запрет — про то, что видимые разделы нельзя прятать
за горизонтальную прокрутку. Это два разных правила.

## 7. Подвал

`border-top: 1px --line`. Отступы 32 сверху, 40 снизу. Inter Caption 12, muted.
**Содержимое — одна строка копирайта, и больше ничего.**

**Ссылок в подвале нет, и это правка от 10 августа 2026.** Здесь стояло
«копирайт слева, ссылки справа, зазор 20; ≤820 — одна колонка»: справа шли
площадки подкаста и лента RSS. Заказчик посмотрел живую страницу и снял их —
те же площадки на сайте уже в трёх местах (блок «Ещё немного нас»,
«О проекте», «Поддержать»), и четвёртый мелкий список внизу КАЖДОЙ страницы
ничего не добавлял. Колонок вместе с ними не стало: содержимое одно.

**Цена названа и принята: видимой ссылки на `/rss.xml` на сайте больше нет.**
Лента жива и находится программами чтения — тег `<link rel="alternate">`
стоит в `<head>` каждой страницы.

**Правый угол подвала занят кнопкой «наверх» (§8.0), и место под неё
резервируется отступом.** Без резерва на ширинах окна 478–562 строка
копирайта встаёт одной строкой во всю колонку и заезжает кнопке под низ.
Полосы этой нет ни в одном списке ширин §25 — она найдена замером по метрикам
гарнитуры, а не глазами.

Подвал не содержит собственных social- и support-призывов: SocialStrip,
SupportStrip и подвал — три независимых компонента.

## 8. Контролы

| Компонент | Размер | Где |
|---|---|---|
| Текстовая ссылка | Caption 12; подчёркивание на hover/focus; → внутрь, ↗ наружу | Редакционные и внешние ссылки |
| Outline button | min-height 44; padding 0 14; border `--line` | Тулбар, вторичное действие |
| Large action | height 56; padding 0 16; Inter Caption uppercase | Boosty, Patreon, TG, VK |
| Icon button | 44×44 | Поиск, стрелки |
| Play | **54×54**, круг, фон `--accent-brand`, иконка `--ink` | Единственная круглая primary-кнопка |
| Filter / tab | min-height 44; без рамки; активный — подчёркивание 1.5 | Фильтры материалов, области поиска |
| Search input | height 64; padding 0 56 0 16; Inter Body M | Глобальный поиск |

### 8.0. Кнопка «наверх»

**Этого элемента нет ни в одном макете Сборки-18.** Решение заказчика
от 10 августа 2026: шапка сайта не закреплена, и на архиве, каталоге или длинном
выпуске вернуться к разделам можно только промотав всю страницу руками.
Записан сюда отдельным пунктом именно потому, что макета у него нет, — иначе
следующая приёмка принесёт его как самодеятельность.

**Вид не перенесён, а собран из токенов.** Ни одного нового размера, цвета
или формы: Technical 11 прописными, фон `--paper`, контур 1 px `--line` —
та же линия, что у рамок контролов (§5), наведение и фокус `--accent-text`.
**Скругления нет, тени нет** — плиток в языке сайта не бывает (§1).

**Фон непрозрачный, и это единственная уступка.** Кнопка висит поверх текста,
и просвечивающие сквозь неё буквы читаются браком печати, а не прозрачностью.

Цель нажатия — не меньше 44×44 целиком, без добора невидимой зоной.
**Под исключение «любая ссылка внутри строки текста» (§25) не подпадает:**
это отдельная кнопка, а не слово в абзаце.

| Свойство | Значение |
|---|---|
| Положение | закреплена в правом нижнем углу, отступ `--gutter` от обоих краёв |
| Появляется | когда шапка ушла за верхний край экрана |
| Исчезает | когда шапка вернулась; больше ни от чего не зависит |
| При открытом плеере | поднимается над панелью: `bottom` 98 (≤620) и 102 (≥621) |
| Прокрутка | плавная; при `prefers-reduced-motion` мгновенная |
| Место в подвале | резервируется отступом, число живёт у самой кнопки |

**Порог — видимость шапки, а не число экранов и не число пикселей.** Порог
в пикселях означал бы разное на телефоне и на мониторе; «шапка ушла за край»
проверяется однозначно и объясняет себя сам.

**Условия «прячется, когда виден подвал» здесь больше нет, и это правка
того же дня.** Сначала кнопка исчезала при появлении подвала: в том же правом
углу стояли ссылки на площадки и RSS, и она накрыла бы их собой. Заказчик
посмотрел живую страницу и убрал ссылки из подвала целиком (§7) — угол
освободился, прятаться стало не от чего. Осталась одна забота: строка
копирайта не должна доезжать до кнопки, и место под неё подвал резервирует.

**Что кнопка всё-таки перекрывает.** По дороге к подвалу она проезжает над
блоками SocialStrip и SupportStrip, а у них правый нижний угол занят ссылками.
Закреплённый элемент перехватывает нажатия по тому, что под ним; цена названа
заказчику 10 августа 2026 и проверяется руками.

**Числа 98 и 102 посчитаны, а не подобраны.** Мешает не сама панель плеера,
а невидимая зона перемотки: ползунок растёт на 34 px НАД панелью во всю её
ширину (§8, «что тащат — накладкой не добирается»). Панель — 65 px до 621
и 69 от 621, плюс эти 34. Встань кнопка в полосу — отняла бы у перемотки
правый край, то есть конец выпуска.

Это `<button>`, а не ссылка на якорь (§22): адрес страницы от неё не меняется
и записей в истории браузера не появляется. В поисковый индекс и в печать
не идёт.

### 8.1. LoadMore

Один компонент на весь сайт: главная, каталог, страница тайтла, архив, поиск.
Всегда по центру, между двумя линиями, без рамки, цель не меньше 44.

У него ровно три состояния, и путать их нельзя:

| Состояние | Когда | Подпись | Поведение |
|---|---|---|---|
| **Активное** | Показаны не все элементы | «Показать ещё <тип> ↓» | По нажатию догружает следующую порцию |
| **Развёрнутое** | Всё показано после нажатия | «Свернуть <тип> ↑» | Та же кнопка на том же месте сворачивает список |
| **Выключенное** | Показывать нечего изначально | «Все <тип> показаны» | `disabled`, цвет `--muted`, курсор обычный |

**Свернуть — это та же кнопка, а не вторая рядом.** Меняются подпись и стрелка,
положение остаётся. Отдельная кнопка сбоку — отдельный элемент, который придётся
отдельно верстать, отдельно чинить и который на мобильном окажется в третьем
месте.

Состояние отражается в `aria-expanded`, чтобы это было слышно, а не только видно.

Подпись называет то, что грузится: «материалы», «тайтлы», «упоминания».
Не «ещё» без существительного и не «Load more».

Счётчик рядом с кнопкой не ставится: число уже в подписи или не нужно вовсе.

**Все самостоятельные цели — не меньше 44×44.** Это внутренний стандарт,
строже WCAG 2.2 AA.

Визуальный размер может быть меньше цели: недостающее добирается невидимой
зоной. Канонический приём:

```css
.control{ position:relative; }
.control:after{ content:""; position:absolute; inset:-4px; }
```

Так сделаны перемотка ±15 (36 → 44), выбор скорости (34 → 44) и таймкод
в транскрипте.

Ручка таймлайна — 14 px визуально при зоне захвата 44 px по высоте: её тащат
пальцем, 11 px для этого мало.

**ЧТО ТАЩАТ — НАКЛАДКОЙ НЕ ДОБИРАЕТСЯ.** Канонический приём выше кладёт
прозрачный псевдоэлемент ПОВЕРХ контрола, и для нажатия этого хватает,
а для перетаскивания нет: накладка перехватит его первой, и ползунок умрёт.
Значит у `input type="range"` расти обязан сам элемент — прозрачными полями,
при этом видимая дорожка остаётся тонкой. Так сделан ползунок в закреплённой
панели: дорожка 3, зона 44, и растёт она вверх, над панелью, чтобы не отнять
высоту у кнопок под собой (§6.2 ТЗ, решение 10 августа 2026).

`focus-visible`: `outline: 2px solid --accent-text; outline-offset: 2px`.
Убирать фокус без равноценной замены нельзя. **У полей ввода замена своя
и она ДВОЙНАЯ** — цвет рамки плюс внутренняя тень 1 px тем же цветом: рамки
контролов бледные (1.57:1), и одного перекрашивания человек с клавиатуры
не видит. Вид один на все три поля сайта, третьего не бывает.

Hover не может быть единственным способом увидеть действие.

При `prefers-reduced-motion` декоративные переходы отключаются.

## 9. Медиа

| Тип | Соотношение |
|---|---|
| Обложка подкаста | 1:1, `object-fit: contain` |
| Карточка статьи и видео | 16:9, `cover` допустим |
| Постер аниме | **`object-fit: cover`.** Обрезается высота, поровну сверху и снизу. Пропорция рамки — не 2:3, см. ниже |
| Портрет ведущего | 1:1, круг |
| Слайд галереи | исходное соотношение, контейнер не искажает |

**Постер аниме заполняет рамку целиком, и это отмена прежнего правила.**
До 10 августа 2026 здесь стояло «не обрезается никогда»: свободное место
оставалось внутри рамки и заливалось цветом марки. На живой странице это
читалось не полем, а дырой — пустота упирается в перфорацию и видна первым
же взглядом.

**Цена названа числами и принята заказчиком.** Замер всех 14 постеров
справочника: обрезается высота, поровну сверху и снизу, у худшего
(«Белый ящик») 3.6 %, у десяти из четырнадцати меньше 1.3 %, у самого широкого
обрезки нет вовсе. Ни название, ни лицо при таком запасе не страдают —
именно поэтому рамка равняется на широкий постер (§9.0), а не на узкий:
так режется только высота и никогда бока.

### 9.0. Пропорция постера: сверено с данными, 2:3 не подтвердилось

**До версии 1.5 здесь было написано «постеры хранятся строго 2:3» — это
неправда, и из неё выведены оба числа, которые пришлось потом чинить.**
Замер всего справочника (13 тайтлов, часть 7): ширина у всех **225**, высота
гуляет от **314 до 338**, то есть отношение от **0.666 до 0.717**. Ровно 2:3
оказался один постер из одиннадцати. Ни Shikimori, ни AniList одинаковой
высоты не гарантируют.

Отсюда правило: **рамка считается от самого широкого реального постера
(0.717), а не от 2:3.** Разница между самым узким и самым широким — 5–12 px,
и уходит она по-разному в зависимости от того, что в рамке фиксировано:

| Где | Что фиксировано | Куда уходит остаток |
|---|---|---|
| Горизонтальная марка, §11.3 | высота 168 | в поле СБОКУ, к тексту |
| Вертикальная марка каталога, §11.1 | ширина колонки | в поля СВЕРХУ и СНИЗУ |

**Пустота сбоку читается полем марки, пустота сверху — дырой:** она упирается
в перфорацию, и её видно первым же взглядом. Поэтому в обеих формах рамка
равняется на ШИРОКИЙ постер, а не на узкий, и в вертикальной марке высота
рамки считается как `ширина / 0.717`, а не как `ширина × 1.5`.

Число живёт **одним токеном `--poster-ratio`** на все места, где рисуется
постер: две формы марки, страница тайтла, результат поиска. Копий у него быть
не должно — грабли уже срабатывали дважды, сначала в горизонтальной марке,
потом в каталоге.

**Появятся постеры шире 0.717 — число перемерить**, а не подгонять вёрстку
на месте. Квадратная картинка в поле постера остаётся ошибкой данных.

**С `cover` этот выбор стал важнее, а не менее важен.** Пропорция рамки теперь
решает не «сколько останется пустоты», а **в какую сторону режется постер**:
рамка от широкого (0.717) режет только высоту, симметрично; рамка 2:3 (0.666)
резала бы по бокам десять постеров из одиннадцати, то есть по краям названия
и лица. И замер «худший случай 3.6 %» сделан ИМЕННО при рамке от 0.717 —
сменив её, придётся мерить заново.

Магические значения `object-position` вроде `center 32%` запрещены: это
подгонка под одну картинку, на остальных даст случайный результат.

`srcset`/`sizes` для контентных изображений. `width`/`height` или
`aspect-ratio` — против прыжка вёрстки. Ниже первого экрана — `loading="lazy"`.

## 9.1. Когда показывать заглушку вместо изображения

| Случай | Что показываем |
|---|---|
| У материала нет обложки в данных | Карточка остаётся текстовой. Ни пустого места, ни заглушки |
| У аниме нет постера в данных | **Заглушка обязательна.** Каталог и марка держатся на постоянной пропорции рамки (`--poster-ratio`, §9.0), дыра ломает ряд |
| Картинка есть, но не загрузилась | Заглушка везде |
| У человека в блоке «Команда» нет фото | **Заглушка обязательна, обрезанная по кругу и без подписи.** 96 px — узкое место, подпись туда не помещается. Пустой кружок не используется: рядом с двумя лицами он читается дырой, а не отсутствием данных |

Заглушка — иллюстрация персонажа на фоне `--accent-soft` и подпись
«Изображения нет» техническим слоем. В узких местах (постер в марке,
результат поиска, кружок человека) подпись скрывается, остаётся только рисунок.

Пустой серый прямоугольник не используется: читатель принимает его
за поломку вёрстки, а не за отсутствие данных.

## 10. Карточка материала

Без фона, тени, скругления и общей рамки.

**Метастрока сверху.** Technical 11, `--accent-text`. Формат:
`[ ТИП ] · ДОП. МЕТА · ДЛИТЕЛЬНОСТЬ ИЛИ ВРЕМЯ ЧТЕНИЯ`. Дата, если нужна, входит
в эту же строку.

Под заголовком отдельной серой строки со временем чтения, просмотра или
прослушивания **не существует**. Один факт — одно место.

**Это правило для карточек в лентах.** На самой странице материала
длительность живёт в верхней строке рядом с датой, а кикер несёт только тип
и номер: `[ ПОДКАСТ ] · №108`. Третьего места нет — см. §13.

Заголовок — Card title 20. Описание — Body S 14.

Отступы: изображение → метастрока 16; метастрока → заголовок 8; заголовок →
описание 12.

В списке соседние карточки разделяет одна линия.

Примеры: `[ СТАТЬЯ ] · 12 МИН ЧТЕНИЯ` · `[ ВИДЕОЭССЕ ] · 24:18` ·
`[ ПОДКАСТ ] · №128 · 01:06:33` · `[ ПОДКАСТ ] · БОНУС · 01:30:18`.
Формат одинаков на главной, в архиве, в поиске и в упоминаниях.

### 10.1. Контексты

| Тип | Desktop | Mobile |
|---|---|---|
| Подкаст, главная | обложка ≤390 квадрат, 4/12 | 1 колонка, обложка на ширину колонки |
| Подкаст, архив | ≤420, 2 в ряд | 1 колонка |
| Подкаст, поиск | 120×120 + текст | 120×120, если тексту остаётся ≥160, иначе стопкой |
| Статья и видео | 16:9, 2 в ряд | изображение над текстом |
| Статья и видео, поиск | 120×68 + текст | 16:9 над текстом |
| Заметка | текст без изображения, 3 в ряд | 1 колонка |

### 10.2. Главная

Режим «Все» — не карточная сетка. Desktop: первый ряд — главный материал 8/12
плюс сайдбар 4/12; второй — подкаст 4/12 плюс статья 8/12; третий — видео 7/12
плюс указатель заметок 5/12.

При ≤760 три ряда складываются в один поток, вертикальные разделители
превращаются в один горизонтальный.

Фильтр «Подкасты»: ≥1101 — 3 колонки; 761–1100 — 2; ≤760 — 1.
«Видеоэссе» и «Статьи»: ≥761 — 2 колонки, 16:9; ≤760 — 1.
«Заметки»: ≥761 — 3 компактные колонки; ≤760 — 1.
Старый архив: ≥921 — 3; 621–920 — 2; ≤620 — 1.

Режимы реализуются модификатором, меняющим `grid-template` и обработку медиа,
а не одной сеткой с переключением видимости.

## 10.3. Длинные ленты: пагинация и «Показать ещё»

Два механизма с разными задачами. Смешивать их нельзя.

| Где | Механизм | Почему |
|---|---|---|
| **Главная, режим «Все»** | ничего | Витрина не растёт: три редакционных ряда, и всё |
| **Главная, режимы фильтра** | 12 материалов и одна кнопка «Весь архив →» | Смысл главной в отборе; бесконечная лента отбор отменяет |
| **Страница архива** | вкладки по типу и пагинация по 24 | Всё, чего не должно быть на главной: полный список, переключение, листание |

**Архив показывается без обложек, одинаковой текстовой сеткой.** Разные
форматы дают разную геометрию медиа: квадрат подкаста, кадр 16:9 у видео,
заметка вообще без картинки. В общем ряду это читается рвано, а первая же
линия над карточками рвётся на сегменты по зазорам сетки.

Без обложек в экран помещается втрое больше, ряды становятся ровными,
а тип материала считывается по технической строке — она и так первая
в карточке. Обложки остаются там, где они работают: на главной, где
материалов мало и каждый отобран.
| **Упоминания на странице тайтла** | LoadMore после шести | Список конечный и короткий |
| **Каталог** | LoadMore | То же |

**Почему архив отдельной страницей, а не подгрузкой на главной.** Страница,
которая может показать всё, перестаёт что-либо отбирать — а витрина нужна
именно для отбора. Плюс с главной ведёт одна кнопка на все типы материалов,
а не пять ссылок в пять разделов: подпись меняется по текущему фильтру
(«Весь архив», «Все подкасты в архиве»), адрес — один.

**Почему архив листается страницами, а не кнопкой.** На страницу можно дать
ссылку, вернуться к ней кнопкой «назад» и найти её поисковиком. Кнопка
«Показать ещё» ничего этого не умеет: после шести нажатий адрес тот же,
а при возврате из материала лента снова свёрнута.

Вид: ряд по центру, технический слой, каждая цель не меньше 44, текущая
страница подчёркнута акцентным. На ≤620 номера скрываются, остаются края
и «Страница 2 из 6».

## 10.4. Обрезка длинных значений

Обрезаем только там, где ряд держится на общей высоте. **На собственной
странице материала не обрезается ничего** — там название и есть содержание.

| Что | Предел |
|---|---|
| Заголовок карточки в ленте | 3 строки |
| Описание карточки | 3 строки |
| Заголовок в марке аниме | 2 строки (высота марки фиксирована — 168) |
| Мета в марке | 3 строки |
| Название в мини-плеере | 1 строка |
| Оригинальное название, студия | 1 строка |

Однострочные значения обрезаются многоточием, многострочные — по строкам.
Оригинальные названия и студии атомарны: обрывать их на второй строке
смысла не добавляет.

## 11. Марка аниме

Марка обозначает **аниме как сущность каталога**, а не материал про аниме.
Статья о «Наруто» — обычная карточка материала. «Наруто» как тайтл — марка.

Форм ровно две: вертикальная каталожная и горизонтальная. Третьей не бывает.

### 11.1. Вертикальная (каталог)

| Viewport | Колонки | Зазор |
|---|---|---|
| ≥1101 | 6 | 20 |
| 821–1100 | 4 | 20 |
| 621–820 | 3 | 16 |
| ≤620 | `auto-fill minmax(140px,1fr)` | 12 |

Постер сверху, без внутренних полей, **`cover`: заполняет рамку целиком,
обрезается высота, поровну сверху и снизу** (§9). Рамка считается от ширины
колонки и `--poster-ratio`, **не 2:3** — см. §9.0. Текстовый блок снизу,
`min-height` 144.

`CAT. 001` — Technical 11. Русское название — Card title 20. Оригинальное —
Inter Caption 12 italic. Мета — Inter Caption 12. Стрелка 20 px, `--accent-text`.

Перфорация по всем четырём сторонам. Без градиентов, теней и текстур.

### 11.2. Постер на странице тайтла

| Viewport | Ширина | Раскладка |
|---|---|---|
| ≥1101 | 350 | постер 4/12, текст 8/12 |
| 821–1100 | 310 | то же, если помещается |
| 621–820 | 310 | стопкой |
| ≤620 | 280 | стопкой, по левому краю |

### 11.3. Горизонтальная марка

Один компонент для главной и для блоков «Упоминается» после материалов.
Выглядит одинаково везде; меняются только контент, количество и цвет марки.

Высота 168. Левая колонка постера на всю высоту, вплотную к левому краю
без полей, **`cover`: постер заполняет колонку целиком** (§9).
**Ширина колонки — `168 × --poster-ratio` ≈ 120, а не 112.**

Число 112 выведено из «постеры строго 2:3», и это не подтвердилось замером
(§9.0). Раньше довод был такой: в рамке 112 самые широкие постеры не дотягивали
до верха и низа на 5–12 px, и над картинкой оставалась полоса у самой
перфорации. **С `cover` полосы не бывает в принципе, и довод сменился
на более крепкий:** пропорция рамки теперь решает, в какую сторону режется
постер. Рамка 120 равна самому широкому реальному постеру — значит режется
только высота, поровну сверху и снизу. Рамка 112 (то есть 2:3) резала бы
по бокам десять постеров из одиннадцати, по краям названия и лица.
И замер «худший случай 3.6 %» сделан именно при 120.
Текст справа: отступы 20 сверху, 30 справа, 18 снизу, 18 слева.

Кикер Technical 11; заголовок Card title 20 / 1.15; мета Inter Caption 12 / 1.45;
стрелка 20.

Перфорация по всем четырём внешним сторонам, включая участок над постером.
Внутренних декоративных рамок нет.

Контейнер: `repeat(auto-fill, minmax(280px, 1fr))`, зазор 14,
`justify-content: start`. На ≤620 — одна карточка в строке.

**Одинокая карточка не растягивается на ширину блока.** Это прямое следствие
`auto-fill` и главная причина, по которой `auto-fit` в системе запрещён.

### 11.4. Ссылка на тайтл внутри текста

Марка внутри текста не используется. Упоминание остаётся ссылкой, но должно
отличаться не только от обычного текста, но и от обычной ссылки:

- цвет `--accent-text`;
- **вместо подчёркивания — перфорация**: зубчатая линия под текстом,
  собранная тем же радиальным градиентом, что края марки. Шаг 6 px, высота 4.
  На hover цвет меняется на брендовый;
- отдельного знака после текста нет;
- ссылка ведёт **на страницу тайтла**, а не на блок упоминаний внизу страницы.

Почему так, а не значком после текста: значок — лишний объект в строке, он
спорит с кавычкой и запятой, у длинных названий отрывается от текста, а на
девяти пикселях зубцы всё равно неразличимы. Перфорация же работает на всей
длине названия и растёт вместе с ним. Один приём на двух масштабах — это
система; два разных приёма — набор украшений.

## 12. Статья и видеоэссе

Общий text-first заголовочный блок: одинаковые ширина, метастрока, H1, отступы
и reflow.

Desktop: ширина 700, отступ сверху 64, снизу 48.

Метастрока: flex, `space-between`, зазор 16, отступ снизу 24; Technical 11.
Слева возврат к типу материала, справа дата и длительность.

H1 — общий токен, всегда по левому краю.

У статьи может быть lead: Literata H3 24 / 1.35 desktop и 22 mobile; расстояние
от H1 — 16. У видеоэссе lead по умолчанию нет.

Видеоэссе не получает отдельной широкой обёртки заголовка: заголовок и мета
на той же оси 700. Плеер 16:9 шириной 1000 начинается после заголовка.

Mobile: метастрока вертикальная, зазор 8, отступ снизу 20.

| Элемент | Desktop | Mobile |
|---|---|---|
| Колонка текста | 700 | 100% внутри полей |
| Медиа normal | 700 | full bleed разрешён |
| Медиа wide | 1000 | full bleed |
| Медиа full | 1240 | full bleed |

Подпись: `FIG. NN` Technical 11 плюс текст Inter Caption 12.

Сетка 2×2 медиа: зазор 8; на мобильном — одна колонка.

Prev/next: 2 колонки desktop, стопка на мобильном с одной линией между.

### 12.1. Маркировка рекламы

**Этого блока тоже нет ни в одном макете** — он появился 10 августа 2026 вместе
с полем поста, и записан сюда по той же причине, что кнопка «наверх» в §8.0.

Technical 11, цвет `--muted`, **без капслока** (§3.2): в строке стоит `erid`,
и в нём различаются строчные и прописные — капслок увёл бы переписывающего
код читателя не туда. Чёрным не набирается: это служебная строка, а не часть
материала. **Но и нечитаемой быть не имеет права** — закон требует видимой
пометки, и «неброско» с «незаметно» тут разные вещи.

| Viewport | Где стоит |
|---|---|
| ≥1101 | выносным блоком в правом поле, вровень с первой строкой текста; ширина `(колонка страницы − 700) / 2 − 24` — это 246 на 1440 и 137 на 1101 |
| ≤1100 | обычным блоком на оси читальной колонки, перед текстом |

**Привязана к первой строке ТЕКСТА, а не к началу материала.** Первым блоком
у поста бывает медиа шире читальной колонки — галерея и видео идут на 1000,
картинка «во всю ширину» на 1240, — и правым краем они заезжают ровно в то
поле, где висит пометка. Поэтому маркировка пропускает медиа и встаёт у первого
абзаца, а широкое медиа под ней получает `clear`: прятаться под картинку
видимой по закону пометке нельзя.

Строке разрешено переноситься посреди слова: поле узкое (137 px в худшем
случае), и длинное название рекламодателя иначе вылезло бы прямо на колонку
чтения. Это единственное место на сайте, где разрыв слова — меньшее зло.

В поисковый индекс не идёт, в описание для поисковика и превью ссылки
не попадает, на карточке в ленте не показывается.

## 13. Страница подкаста

| Viewport | Обложка | Раскладка | H1 |
|---|---|---|---|
| ≥1101 | 360×360 | 360 + 48 + текст и плеер | 56 |
| 821–1100 | 300×300 | 300 + 32 + текст | 48 |
| 621–820 | ≤340 | стопкой | 48 |
| ≤620 | min(100%, 340) | стопкой | 40 |

**Длительность на странице выпуска называется дважды и не больше:** в верхней
строке рядом с датой и внутри плеера. В кикере её нет. До правки она стояла
в трёх местах подряд, и ни одно из них не было главным.

**Подводки в hero нет.** Заголовок и плеер — всё. Подводка утяжеляла блок,
и написать её к каждому выпуску всё равно невозможно; описание выпуска идёт
ниже, в читальной колонке.

**Текстовая колонка не получает фиксированной высоты.** Плеер прижимается к низу
через `align-self: end` или `margin-top: auto` внутри грида, а не через
`height: 360px`.

Плеер — один компонент. На странице подкаста `border-top` и `border-bottom`
1 px `--ink`. Внутренний грид 54 / 1fr / auto, зазор 16.

Play 54×54, фон `--accent-brand`, иконка `--ink`.

Таймлайн 3 px; заливка `--accent-brand`; ручка 14 px при зоне захвата 44
по высоте.

±15: визуально 36×36, без рамки и фона, круглый символ; зона не меньше 44.

Скорость: визуально 34 высотой, рамка 1 px `--line`; зона не меньше 44.

Подпись плеера — отдельная строка под контролами, технический слой.

Mobile: главный грид 50 / 1fr, инструменты переходят на следующую строку.
Внешний вид ±15 и скорости не меняется.

Описание — колонка 700, Body L 18.

Транскрипт — 1000; desktop колонка спикера 142 плюс реплика.
На мобильном строка «спикер + таймкод» всегда над репликой; двух колонок нет.

Таймкод — Technical 11, `--accent-text`, подчёркивание, **без рамки**.
Цель 44 добирается невидимой зоной. Рамка превратила бы таймкод в плитку,
а плиток в языке сайта нет; в транскрипте из двухсот реплик их было бы двести.

Таймкод внутри текста абзаца — исключение из технического слоя: он наследует
кегль и гарнитуру абзаца, потому что является частью предложения. Моноширинный
11 px рядом с Inter 18 даёт высоту знака вдвое меньше текста — у Plex Mono
строчная 0.52 em против 0.73 em у Inter. Машинную природу в этом случае несут
цифры фиксированной ширины (`tabular-nums`), а не гарнитура.

**Транскрипт целиком лежит в разметке, свёрнут только показом.** Вес
несопоставим с аудио: 900 реплик это около 120 КБ, из которых по сети уедет
примерно 30.

Из этого следуют три правила:

- **окно прокручивается само.** Переход по таймкоду и «Следить за плеером»
  мотают транскрипт внутри окна, а не разворачивают его. Разворот —
  отдельное действие читателя, подменять его автоматикой нельзя: человек
  свернул окно намеренно;
- **поиск браузера находит и в свёрнутой части** — поэтому нижние реплики
  ограничены высотой окна, а не спрятаны через `display:none`;
- **кнопка разворота остаётся** — она для тех, кто хочет читать
  транскрипт как текст, а не как спутник плеера.

Стоимость отрисовки снимается `content-visibility: auto`: браузер не рисует
то, что за экраном, но документ остаётся целым.

Ограничение по высоте задаётся в `em`: при `zoom 200%` должно оставаться
видно не меньше трёх реплик.

## 14. Видеоэссе

Заголовок — общий блок из §12 без локальных изменений.

Плеер YouTube: ширина 1000, 16:9. На мобильном допустим full bleed.

Описание — 700, Body L 18. Аудиоверсия использует тот же плеер, что подкаст.
Транскрипт идентичен подкастному.

## 15. Бонусный выпуск

Вариант страницы подкаста, а не отдельная система.

Тип упоминается один раз — `BONUS` в кикере. Дата слово «бонус» не повторяет.

Обложка и H1 — размеры страницы подкаста. Плеера и транскрипта нет.

После описания — paywall шириной 1240. Рамка 1 px `--accent-text`, фон
`--accent-soft`, отступы 48 desktop и 24 mobile. Заголовок H2.
Кнопки: 4 колонки desktop, 2 tablet, 1 mobile, высота 56.

`min-height` 400 допустим, фиксированная высота — нет.

## 16. Каталог

H1 общий. Сетка и марка — только по §11.1.

### 16.1. Поиск и сортировка

Каталог растёт: при сотне тайтлов сетка без поиска превращается в свалку.
Над сеткой — панель на всю ширину, отделённая линией снизу.

**Поиск слева.** Поле высотой 44, ищет по русскому названию, оригинальному
и студии: половина тайтлов известна читателю в латинице. «е» и «ё» считаются
одной буквой — так же, как в упоминаниях.

**Сортировка справа.** Тот же контрол, что скорость в плеере: видимая плашка
внутри цели 44. Четыре варианта:

| Вариант | Порядок |
|---|---|
| **По порядку добавления** (по умолчанию) | Как заводили в справочник. Это же порядковый номер `CAT. 001` на марке |
| По алфавиту | По русскому названию |
| По году выхода | Свежие сверху |
| По числу упоминаний | Часто обсуждаемые сверху |

**Фильтров по параметрам нет намеренно.** У аниме их слишком много — жанр,
студия, год, тип, статус, — и любой отобранный набор окажется произвольным.
Поиск закрывает ту же задачу без интерфейсного долга.

**Счётчик под описанием отвечает на текущий вопрос:** без запроса «Всего: N»,
с запросом «Найдено: N». Числительное согласуется.

Ничего не нашлось — пустое состояние внутри блока, с текстом запроса
и подсказкой попробовать оригинальное название.

Счётчик тайтлов — Technical 11.

«Показать ещё» — общий компонент LoadMore из §8.1 со всеми тремя состояниями.
Своего варианта у каталога нет.

## 17. Страница аниме

Hero: перфорированный постер и текст, размеры из §11.2. Мета — Technical 11.
Оригинальное название справа на desktop, под H1 на узких.

**Упоминания** — карточки материалов, где обсуждали тайтл (не марки).
3 колонки desktop, 2 на 621–820, 1 на ≤620. Заголовок 20, описание 14,
медиа 16:9.

У упоминания подкаста обязателен активный таймкод.

После шести — общий LoadMore из §8.1. Счётчика, дублирующего число в кнопке, нет.

## 18. «О проекте»

| Компонент | Desktop | Mobile |
|---|---|---|
| Hero | заголовок 4/12, текст 6–12 | 1 колонка |
| Ведущие | заголовок слева, карточки справа | заголовок → карточки |
| Портрет | 120×120 круг | 96×96 |
| Карточка ведущего | фото 120 + 24 + текст | фото над текстом |
| Площадки | 5 в строку | ≤820 — 3; ≤620 — 2; <330 — 1 |
| SocialStrip | intro + 3 колонки | стопкой |
| Контакты | заголовок слева, почта справа | стопкой |

Линия только между соседними карточками ведущих.

Площадка подкаста — иконка 28×28 и название, без описания и плитки.

Порядок: ведущие → площадки → соцсети → поддержка → контакты → подвал.

## 19. «Поддержать»

Hero: заголовок слева, текст и кнопки справа. Кнопки принадлежат hero,
отдельного заголовка «выберите площадку» нет.

Large action: 2 колонки desktop и tablet, 1 mobile.

«Что даёт подписка»: 2 колонки desktop, 1 при ≤820. Номер — Technical 11
`--accent-text`; текст — Body L 18. Разделителей между пунктами нет.

SocialStrip на этой странице отсутствует.

## 20. Поиск

Eyebrow: «ПО КАТАЛОГУ „БАКИ!“». Поле: высота 64, Inter Body M, плейсхолдер muted.

До ввода — области: Везде / Материалы / Аниме / Упоминания. После поиска тот же
контрол становится фильтром и показывает счётчики.

Контролы переносятся, не скроллятся.

Результаты всегда сгруппированы: Аниме / Материалы / Упоминания в выпусках.
Объясняющего блока на пустой странице нет.

Ширина колонки постера в результате — те же `высота × --poster-ratio`, что
в марке (§9.0). Своего числа у поиска нет.

| Тип | Desktop | Mobile |
|---|---|---|
| Аниме | постер высотой 168 + текст + стрелка | постер высотой 132 + текст |
| Подкаст | 120×120 + текст | стопкой, если тексту меньше 160 |
| Статья и видео | 120×68 + текст | 16:9 над текстом |
| Упоминание | выпуск │ таймкод │ цитата | спикер и таймкод сверху, цитата ниже |

Найденное выделяется `--accent-text` плюс подчёркиванием или полужирным —
цвет не может быть единственным отличием.

Метастрока результата-материала следует формату §10.

## 21. SocialStrip и SupportStrip

**SocialStrip.** Eyebrow Technical 11 `--accent-text`. Заголовок семантически H2,
визуально H3 24 / 22. Desktop (≥761): intro 3/12 плюс три ссылки по 3/12.
Каждая: платформа Inter Caption 12 semibold uppercase, описание Body S 14,
разделены вертикальными линиями.

**Mobile (≤760) — три ссылки В РЯД, без описаний.** Это правка от 10 августа
2026; здесь стояло «одна колонка, между ссылками одна горизонтальная линия».
Столбиком из трёх строк с описаниями блок занимал почти весь экран телефона,
а сказать ему нужно немного: вот три места, где нас можно найти. Решение
заказчика по живой странице.

| Что | Как |
|---|---|
| Колонки | три равных, но с полом по содержимому; зазора нет |
| Подпись и стрелка | одно неразрывное целое, зазор `space-1` |
| Описание площадки | не показывается |
| Линия | одна, непрерывная, под шапкой блока |
| Вертикальные линии между столбцами | нет — они встретились бы с той углом (§5) |

**Стрелка не отрывается от подписи ни на какой ширине.** Сначала пара стояла
`space-between`, то есть расталкивалась по краям своего столбца, а перенос
между подписью и стрелкой был разрешён — и на узком экране стрелка падала
под подпись, у разных ссылок по-разному. Разнобой заказчик увидел первым же
взглядом. Пара держится `nowrap`, включая пробел внутри «Наш чат».

**Столбцы гибкие, а не доли грида, и это тоже про поломку.** Доля грида
сжимает ячейку насильно, ниже содержимого; гибкая ячейка ниже своего
содержимого не сжимается, и не поместившаяся ссылка переносится на вторую
строку ЦЕЛИКОМ. То есть предел ширины ломает ряд, а не слово.

**Числа мерены в начертании 600, а не в обычном.** Пара «TELEGRAM ↗» — 90 px,
все три вместе 238 при 284 доступных на экране 320. Первый расчёт дал 95 px
(мерили начертание по умолчанию) и увёл в ложное решение — разрешённый
перенос. **Меряя подписи, берите то начертание, которым они набраны.**

**SupportStrip.** Desktop: изображение 144×144, текст, кнопки. Заголовок H3 24,
описание Body S 14, кнопки outline ≥44. ≤820 — изображение и текст сверху,
кнопки ниже. ≤620 — одна колонка, если две дают элемент уже 140.

## 22. Реализация

Mobile-first. CSS Grid и Flexbox, не абсолютное позиционирование.

Порядок DOM совпадает с порядком чтения. Смысловые блоки не переставляются
через `order`.

`<a>` для перехода, `<button>` для действия. Кликабельных `div` нет.

Один H1 на страницу. H2 — крупные секции, H3 — вложенные.

Все изображения имеют `alt`; декоративные — пустой.

Шрифты — `font-display: swap`, первый рендер не блокируется.

`viewport` — `width=device-width, initial-scale=1`. Зум не отключается.

## 23. Соответствие стандартам

| Проверка | Требование | Стандарт |
|---|---|---|
| Reflow | Нет горизонтального скролла на 320 px, кроме галереи | WCAG 1.4.10 |
| Цели | Все ≥44×44 | Строже 2.5.8, соответствует 2.5.5 |
| Контраст текста | ≥4.5:1 | 1.4.3 |
| Контраст UI | ≥3:1 | 1.4.11 |
| Масштаб | Нет фиксированных высот текста, 200% не ломает | 1.4.4 |
| Интервалы | Контейнеры растут | 1.4.12 |

## 24. CSS-контракт

```css
:root{
  --font-display:"Literata",Georgia,serif;
  --font-body:"Inter",Arial,sans-serif;
  --font-tech:"IBM Plex Mono",ui-monospace,monospace;

  --paper:#F8F6F0;
  --ink:#151515;
  --text:#3F3B36;
  --muted:#69645D;
  --line:#CBC6BC;
  --line-soft:#DFDBD2;
  --line-strong:#8F8A82;
  --accent-brand:#D88F9F;
  --accent-text:#A64E65;
  --accent-soft:rgba(216,143,159,.10);

  --site:1320px;
  --full:1240px;
  --wide:1000px;
  --reading:700px;
  --grid-gap:24px;

  --gutter:18px;
  --h1:40px;  --h1-lh:1.05;
  --h2:30px;
  --h3:22px;
  --card-title:20px;
  --body-lg:18px;
  --body-md:16px;
  --body-sm:14px;
  --caption:12px;
  --technical:11px;
}
@media (min-width:621px){ :root{ --gutter:24px; --h1:48px; --h1-lh:1.04; --h2:32px; --h3:24px; } }
@media (min-width:821px){ :root{ --gutter:32px; } }
@media (min-width:1101px){ :root{ --h1:56px; --h1-lh:1.02; --h2:34px; } }

*{box-sizing:border-box}
img,picture,video{max-width:100%;height:auto}
.grid>*,.flex>*{min-width:0}
a,.long-text{overflow-wrap:anywhere}
:focus-visible{outline:2px solid var(--accent-text);outline-offset:2px}
```

## 25. Чек-лист перед сдачей

Ширины 320, 360, 390, 430, 620, 768, 820, 1024, 1100, 1280, 1440.

**Плюс ширина сразу за каждой точкой перелома — это отдельный пункт, а не
уточнение к предыдущему.** Раскладка меняется на переломах, и разваливается
она в первых же пикселях после переключения; круглые числа этой полосы
не касаются. Наступили 10 августа 2026: шапка на 1101–1239 переносила пункты
меню посреди слова и сжимала подзаголовок в столбик на семь строк, а в списке
выше стоят 1100 и 1280 — ровно до и ровно после. Полоса прожила незамеченной
до тех пор, пока её не увидел заказчик.
Переломы берутся из собранного CSS, а не отсюда: сборщик пишет `width>=821px`,
а не `min-width`, — искать надо обе записи. На 10 августа 2026 их семь
(**621, 761, 821, 921, 1101, 1121, 1240**) плюс три обратных (**520, 620,
760**), то есть проверять **521, 621, 761, 821, 921, 1101, 1121, 1240**
и по пикселю до каждого.

На каждой: `documentElement.scrollWidth === clientWidth`, кроме трека галереи.

Зум 200% и 400%.

Переопределение интервалов: `line-height 1.5`, отступ абзаца 2em,
трекинг .12em, межсловный .16em — контент не режется.

Клавиатура: фокус виден, порядок совпадает с DOM.

Цели ≥44×44, включая ручку таймлайна, **ползунок перемотки в закреплённой
панели** (у него зона растёт вверх, над панелью, — см. §8) и **кнопку «наверх»**
(§8.0: она держит 44 сама, без добора невидимой зоной).
**Исключение: любая ссылка ВНУТРИ строки текста.** Строки врезок, строки блока
таймкодов, метки времени в тексте выпуска (22–30 px) и ссылки на тайтл внутри
абзаца (29.7) — высоту им задаёт строка, и 44 px они не получат никогда,
иначе разорвут абзац. Формулировка расширена 10 августа 2026: прежняя называла
поимённо только врезки и таймкоды, и приёмка принесла остальные как нарушения.

Формы: label, фокус, ошибка. **Граница контролов — 1.57:1, а не ≥3:1**, и это
принятое решение от 9 августа 2026: рамка кнопки и разделитель на странице
рисуются одной линией `--line`, а прежний отдельный `--line-strong` под 3:1
читался разнобоем. Середины между двумя цветами не существует. Содержимое
контролов порог держит с запасом: текст на кнопках 16.9:1.
**Фокус у полей — двойной:** цвет рамки плюс внутренняя тень в 1 px тем же
цветом. Одного цвета мало ровно потому, что рамка бледная. Вид один на все три
поля сайта (поиск по сайту, поиск в каталоге, поиск по расшифровке), третьего
не бывает.

Транскрипт на ≤620: спикер и таймкод над репликой, скролла нет.

Шапка: навигация не скроллится ни на какой ширине; до 821 разделы свёрнуты
и раскрываются кнопкой меню ОДНОЙ колонкой, а не сеткой 2×2 (правка §6
от 10 августа 2026 — сайт был верен, документ отстал); широкая раскладка
начинается с 1240, и на 1240 ± 60 пункты меню не переносятся, а подзаголовок
стоит в четыре строки, а не в семь.

Главная в каждом режиме фильтра отдельно.

Плеер: play 54, ±15 без рамок, скорость 34, таймлайн 3, подпись отдельной
строкой, все цели ≥44. **В закреплённой панели кнопка play 44, а не 54** —
панель это не страница выпуска, и «54 везде» из §0 к ней не относится.
Уточнено 10 августа 2026: прежняя строка объявляла нарушением осознанный
размер.

Марки: 168 высотой, постер **120.5×168** слева (`168 × --poster-ratio`),
одинаковая перфорация и типографика на главной, после статьи, подкаста
и видеоэссе. **Не 112:** число 112 выводилось из «постеры строго 2:3»,
а замер справочника это опроверг — см. §0.1 и §9.0. Ширина живёт одним
токеном на все места, где рисуется постер.

**Одна марка в блоке не растягивается** — проверить отдельно на странице
подкаста и на странице тайтла.

**Постеры аниме: заполняют рамку и обрезаны симметрично** — `cover` в каталоге,
в марке, на странице тайтла и в выдаче поиска; режется высота, поровну сверху
и снизу, по бокам не режется нигде. **Это НЕ «обрезки нет нигде»:** прежнее
правило отменено решением заказчика 10 августа 2026 вместе с замером (худший
случай 3.6 % высоты, §9). Найдя обрезку, приёмка отмечает её как принятое
решение, а не как нарушение. Нарушение здесь другое — обрезка по бокам,
несимметричная обрезка или `object-position` с подогнанным числом.

**Кнопка «наверх» (§8.0):** появляется ровно тогда, когда шапка ушла за край,
исчезает при её возврате; над открытой панелью плеера поднимается и ни в одном
состоянии её не задевает — ни саму панель, ни полосу перемотки над ней.
Ни скругления, ни тени, ни цвета вне палитры она не вводит.
**Строка копирайта в подвале до неё не доезжает ни на одной ширине** — включая
полосу 478–562, которой в списке выше нет.

**Маркировка рекламы (§12.1):** на широком экране в правом поле у первой
строки текста, на узком — перед текстом; под широкую картинку не подлезает.
Технический слой без капслока — это тут не нарушение, а §3.2.

В собранном CSS нет ни одного `font-size` меньше 11 px.

В собранном CSS нет `!important`.

Ни в одном селекторе не совмещены видимость и раскладка (§2.1). Проверяется
поиском: если рядом с `display:none` или `display:block` в селекторе режима
стоит `grid-template`, `flex-direction` или `gap` — это ошибка.

В собранном CSS нет цветов вне палитры §3.3.

Загружены ровно три гарнитуры, локальных `font-family` нет.

## 26. Приоритет при расхождениях

1. Семантика, доступность и mobile reflow из этого документа — выше всего.
2. **Композиция страницы** — какие блоки и в каком порядке — берётся
   из последнего утверждённого макета.
3. **Все размеры, цвета, отступы и типографика** берутся из токенов этого
   документа. Макет не может ввести новый размер.
4. Внешний вид повторяемого компонента — из канонической реализации: плеер —
   страница подкаста; SocialStrip и SupportStrip — главная; марка — две формы
   из §11. Третьих вариантов не бывает.
5. Правка вносится сначала в этот документ, затем в общий CSS, затем
   проверяется на всех страницах, где компонент используется.
